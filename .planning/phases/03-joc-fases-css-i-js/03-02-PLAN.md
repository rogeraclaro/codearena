---
phase: 03-joc-fases-css-i-js
plan: 02
type: execute
wave: 2
depends_on: [03-01]
files_modified:
  - test/effects.test.js
  - test/jsPhase.test.js
  - src/shared/robotTemplate.js
  - src/shared/effects.js
  - src/server/gameState.js
  - src/server/events.js
  - src/server/socketHandlers.js
  - src/client/client.js
  - src/client/client.css
autonomous: true
requirements: [GAME-05, GAME-07]
must_haves:
  truths:
    - "Un equip construeix una regla 'Quan [event] a [origen] → a l'element [destí] → Fes [acció]' amb desplegables i, en prémer 'Veure', el robot de la preview reacciona a la interacció del ratolí (SC2)"
    - "El catàleg és tancat: cap text lliure esdevé mai codi; els valors dels desplegables són claus a taules frozen, sense eval/Function (GAME-06/D-11)"
    - "Una regla amb origen o destí absent (DOM incomplet) és un no-op silenciós: cap error de consola, cap bloqueig del panell (GAME-07/D-19)"
    - "Les regles construïdes sobreviuen un F5/reconnexió (recuperades dirigides del servidor, CORE-03)"
    - "No es pot repetir la mateixa parella (event, origen); el límit és 5-6 files; una acció composta desactiva el destí (D-15/D-12/D-17)"
  artifacts:
    - "src/shared/robotTemplate.js: JS_EVENTS, JS_ELEMENTS, JS_ACTION_KEYS, JS_COMPOSITE_KEYS, JS_ROW_LIMIT (frozen)"
    - "src/shared/effects.js: ACTIONS, COMPOSITES, applyAction(doc, rule), attachRule(doc, rule) (pur, testable amb doc stub)"
    - "src/server/gameState.js: team.jsRules, setJsRules() (mutation-returns-bool), getTeamRules()"
    - "src/server/events.js: TEAM_SET_RULES, TEAM_JS_STATE"
    - "src/client/client.js: renderJsPanel(); TEAM_JS_STATE handler; reattach de regles en rebuild de srcdoc"
    - "test/effects.test.js (no-op GAME-07 pur) + test/jsPhase.test.js (round-trip + validació + F5)"
  key_links:
    - "desplegables plens → TEAM_SET_RULES (whole-array) → setJsRules (valida vocab frozen + anti-repeat + límit + composite⇒destí null) → TEAM_JS_STATE dirigit"
    - "TEAM_JS_STATE / F5 → rebuild srcdoc UNA vegada + reattach de totes les regles a `load` (evita listeners obsolets, Pitfall 3)"
    - "attachRule/applyAction null-guarden cada querySelector → no-op natural sobre element absent (GAME-07/D-19)"
---

<objective>
Entrega la **Fase JS jugable** de CodeArena com a llesca vertical completa: sobre el robot Bender ja estilitzat (Pla 01), l'equip construeix regles obertes en llenguatge natural ("Quan passi X a l'element Y → a l'element Z → Fes W") amb desplegables tancats, i en veu l'efecte reaccionant a interaccions del ratolí sobre el robot a la preview.

Purpose: Compleix GAME-05 (constructor de regles JS amb desplegables) i la part JS de GAME-07 (opera sobre DOM incomplet sense trencar-se). El model és obert/variety-based (D-11): no hi ha una única resposta correcta; la puntuació real arriba a la Fase 4.

Output: Vocabulari frozen JS (events/elements/accions), un intèrpret parent-driven sense `eval` sobre l'iframe scriptless (`src/shared/effects.js`), estat autoritatiu per equip (`jsRules`) amb validació de vocabulari tancat, i el panell de files de regles amb botons "Veure" i "Afegir JavaScript".
</objective>

## Phase Goal (MVP user story)

**As a** alumne d'FP sense experiència prèvia (en equip), **I want to** construir regles "Quan [event] a [origen] → a l'element [destí] → Fes [acció]" triant de desplegables tancats i prémer "Veure", **so that** entengui que el JS és el comportament — veient el robot reaccionar (canviar color, amagar-se, girar, créixer) quan hi passa el ratolí.

> Story derivada del `**Goal:**` de ROADMAP.md §Phase 3 i del Success Criterion 2 (rol = equip/alumne del PROJECT.md Core Value, capacitat = regles JS GAME-05, outcome = "JS = comportament" del Core Value). No inventada.

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-joc-fases-css-i-js/03-CONTEXT.md
@.planning/phases/03-joc-fases-css-i-js/03-RESEARCH.md
@.planning/phases/03-joc-fases-css-i-js/03-PATTERNS.md
@.planning/phases/03-joc-fases-css-i-js/03-UI-SPEC.md

# El Pla 01 (fase CSS) ja ha afegit el patró CSS_HOLES / setCssValue / TEAM_CSS_STATE
# i el wrapPreview reescrit — aquest pla el reutilitza i hi construeix a sobre:
@.planning/phases/03-joc-fases-css-i-js/03-01-PLAN.md

# Analog directes:
@src/shared/robotTemplate.js
@src/server/gameState.js
@src/server/socketHandlers.js
@src/client/client.js
@test/placement.test.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Test-first — intèrpret no-op (GAME-07) + round-trip de regles (RED)</name>
  <files>test/effects.test.js, test/jsPhase.test.js</files>
  <read_first>
    - test/placement.test.js (harness real-server, helpers, casos REJECT amb `onceOrTimeout`) — copia'n el harness a jsPhase.test.js
    - src/server/events.js (importa noms d'event)
    - .planning/phases/03-joc-fases-css-i-js/03-RESEARCH.md §Code Example 4 (test pur amb doc stub), §Pattern 4 (validació setJsRules)
    - .planning/phases/03-joc-fases-css-i-js/03-CONTEXT.md D-11..D-19
  </read_first>
  <action>
    (1) test/effects.test.js — test pur (sense navegador) de l'intèrpret que crearà src/shared/effects.js. Amb un doc stub `{ querySelector: () => null, querySelectorAll: () => [] }`, assereix `assert.doesNotThrow(...)` per: una acció simple sobre element absent (`applyAction(emptyDoc, { origen:'nas', desti:'nas', accio:'canviar-color' })`) i una acció composta amb destí null (`applyAction(emptyDoc, { origen:'nas', desti:null, accio:'<clau composta>' })`). Aquests casos han de FALLAR ara (effects.js no existeix) — RED.
    (2) test/jsPhase.test.js — copia el harness de placement.test.js però inicia `phase: 'js'`. Importa EVENTS (cap literal). Casos que han de FALLAR ara:
    - RULES-OK: un `TEAM_SET_RULES` amb un array d'1 regla vàlida retorna `TEAM_JS_STATE` dirigit amb `jsRules` igual.
    - RULES-ANTIREPEAT-REJECT: dues files amb la mateixa parella (event, origen) → cap `TEAM_JS_STATE` (D-15).
    - RULES-LIMIT-REJECT: un array de 7 regles → cap `TEAM_JS_STATE` (límit ≤6, D-11/D-12).
    - RULES-VOCAB-REJECT: event/origen/acció fora del vocabulari frozen → cap `TEAM_JS_STATE` (V5).
    - COMPOSITE-DESTI-REJECT: una acció composta amb `desti` no null → cap `TEAM_JS_STATE` (D-17).
    - RULES-WRONG-PHASE: `TEAM_SET_RULES` mentre `phase==='css'` → cap `TEAM_JS_STATE`.
    - NO-SESSION-BROADCAST: un segon equip no rep res.
    - F5-JS-RECOVERY: reconnectar amb el token recupera `jsRules` via `TEAM_JS_STATE` dirigit en connectar.
    Usa `onceOrTimeout` per als casos REJECT (resol `undefined` = cap broadcast = passa).
  </action>
  <verify>
    <automated>node --test test/effects.test.js test/jsPhase.test.js  # FALLA ara (RED): effects.js i els events/mutador no existeixen</automated>
  </verify>
  <done>test/effects.test.js i test/jsPhase.test.js existeixen i fallen (RED) amb els casos d'intèrpret i round-trip definits.</done>
  <acceptance_criteria>
    test/effects.test.js i test/jsPhase.test.js existeixen, importen EVENTS (cap literal), i fallen amb els casos definits pendents d'implementació (RED verificable). El harness d'integració arrenca i tanca el servidor net.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Backend + intèrpret — vocabulari frozen, effects.js, estat autoritatiu (GREEN)</name>
  <files>src/shared/robotTemplate.js, src/shared/effects.js, src/server/gameState.js, src/server/events.js, src/server/socketHandlers.js</files>
  <read_first>
    - src/shared/robotTemplate.js (SLOTS/CONTAINERS — deriva els 8 selectors JS; `.antena`/`.ull` sense id → classe/`:nth-of-type`)
    - src/server/gameState.js (setCssValue/getTeamStyle afegits al Pla 01 — calca'n la forma per setJsRules/getTeamRules; team init amb cssValues)
    - src/server/socketHandlers.js (handler TEAM_SET_CSS del Pla 01 + bloc reconnexió amb TEAM_CSS_STATE — calca per TEAM_SET_RULES/TEAM_JS_STATE)
    - .planning/phases/03-joc-fases-css-i-js/03-RESEARCH.md §Pattern 2 (intèrpret parent-driven, taules frozen), §Pattern 4 (setJsRules), §JS Element→Selector Map, §Code Example 4
    - .planning/phases/03-joc-fases-css-i-js/03-CONTEXT.md D-13..D-18 (events, elements, accions simples/compostes)
  </read_first>
  <behavior>
    Fa passar test/effects.test.js i test/jsPhase.test.js:
    - applyAction/attachRule no llencen mai sobre un doc sense elements (querySelector→null, querySelectorAll→[]).
    - setJsRules accepta un array vàlid i el retorna via getTeamRules; rebutja anti-repeat, >6 files, vocabulari desconegut, composite amb destí no null, i fora de fase js.
    - reconnexió emet TEAM_JS_STATE amb les regles emmagatzemades.
  </behavior>
  <action>
    (1) robotTemplate.js — afegeix vocabulari frozen (03-RESEARCH §Pattern 2 / §JS Element→Selector Map):
       • `JS_EVENTS = Object.freeze({ click:'click', hover:'mouseover', mouseleave:'mouseleave', dblclick:'dblclick' })` (D-13).
       • `JS_ELEMENTS = Object.freeze({ nas:'#nas', boca:'#boca', cap:'#robot-cap', antena:'.antena', 'orella-esquerra':'#orella-esquerra', 'orella-dreta':'#orella-dreta', 'ull-esquerre':'.contenidor-ulls .ull:nth-of-type(1)', 'ull-dret':'.contenidor-ulls .ull:nth-of-type(2)' })` (D-14; deriva/cross-checka contra SLOTS[].html — `.antena`/`.ull` no tenen id).
       • `JS_ACTION_KEYS = Object.freeze(['canviar-color','amagar-mostrar','girar','canviar-mida'])` (D-16, claus simples per validació servidor).
       • `JS_COMPOSITE_KEYS = Object.freeze([...])` — claus de les accions compostes (D-17; el catàleg concret és discreció del planner, mínim garantit 2: "acluca-tanca", i una segona p.ex. "ulls-vermells-orelles-grosses"; autoritza 3-5).
       • `JS_ROW_LIMIT = 6`.
    (2) src/shared/effects.js (NOU) — intèrpret pur parent-driven, SENSE eval/Function (03-RESEARCH §Pattern 2). Importa JS_ELEMENTS/JS_EVENTS de robotTemplate.js. Defineix `ACTIONS` (map clau simple → `(el)=>{...}`: canviar-color→`el.style.backgroundColor`, amagar-mostrar→toggle `visibility`, girar→`el.classList.toggle('js-rotate')`, canviar-mida→`el.classList.toggle('js-scale')`) i `COMPOSITES` (map clau composta → llista `[{sel, fn}]`, p.ex. acluca-tanca → aclucar `.ull` + tancar `#boca`). Exporta `applyAction(doc, rule)`: si `rule.accio` és composta → `COMPOSITES[accio].forEach(({sel,fn}) => doc.querySelectorAll(sel).forEach(fn))` (NodeList buida = no-op); altrament `const target = doc.querySelector(JS_ELEMENTS[rule.desti]); const fn = ACTIONS[rule.accio]; if (target && fn) fn(target);` (destí absent o acció desconeguda = no-op). Exporta `attachRule(doc, rule)`: `const origin = doc.querySelector(JS_ELEMENTS[rule.origen]); if (!origin) return; origin.addEventListener(JS_EVENTS[rule.event], () => applyAction(doc, rule));`. CADA lookup null-guardat (GAME-07/D-19).
    (3) gameState.js — a `state.teams.set(...)` afegeix `jsRules: []`. Afegeix `setJsRules(teamId, rules)` (03-RESEARCH §Pattern 4): guard `!team`→false; `state.phase !== 'js' || timerStatus==='frozen'`→false; `!Array.isArray(rules) || rules.length > JS_ROW_LIMIT`→false; per cada regla valida `r.event in JS_EVENTS`, `r.origen in JS_ELEMENTS`, acció ∈ JS_ACTION_KEYS o JS_COMPOSITE_KEYS; si simple → `r.desti in JS_ELEMENTS`; si composta → `r.desti == null` (D-17); anti-repeat amb `Set` de `${event}|${origen}` (D-15). Whole-array replace: `team.jsRules = rules.map(({event,origen,desti,accio})=>({event,origen,desti,accio}))`; return true. Afegeix `getTeamRules(teamId)` → `{ jsRules: [...team.jsRules] }`. Importa el vocabulari de robotTemplate.js. Registra ambdós a l'export.
    (4) events.js — afegeix `TEAM_SET_RULES: 'team:set-rules'` (client→server) i `TEAM_JS_STATE: 'team:js-state'` (server→client).
    (5) socketHandlers.js — handler `TEAM_SET_RULES` calcat de TEAM_SET_CSS: identitat de socket.data.teamId (V4); valida `Array.isArray(payload?.rules)` (V5); si `setJsRules` → emet `TEAM_JS_STATE` dirigit + `SESSION_FULL_STATE` a admin (mai `io.to('session')`). Al bloc de reconnexió afegeix `socket.emit(EVENTS.TEAM_JS_STATE, gameState.getTeamRules(socket.data.teamId));` (dirigit, CORE-03). Embolcalla amb `safeHandler`.
  </action>
  <verify>
    <automated>node --test test/effects.test.js test/jsPhase.test.js  # GREEN</automated>
    <automated>node --test test/cssPhase.test.js test/placement.test.js test/timer.test.js test/monitoring.test.js  # cap regressió (CSS del Pla 01 + fases prèvies)</automated>
  </verify>
  <done>effects.test.js/jsPhase.test.js verds i cap regressió; vocabulari frozen + effects.js + setJsRules/getTeamRules + events dirigits funcionals.</done>
  <acceptance_criteria>
    effects.test.js i jsPhase.test.js passen. `applyAction`/`attachRule` no llencen mai sobre element absent. `setJsRules` rebutja (cap broadcast) anti-repeat, >6 files, vocabulari desconegut, composite amb destí no null, i fora de fase js. Reconnexió recupera jsRules. Cap regressió a CSS/placement/timer/monitoring. El vocabulari JS deriva dels selectors de SLOTS (no hand-list divergent).
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Client — panell de regles JS + reattach sobre srcdoc + "Veure"/"Afegir"</name>
  <files>src/client/client.js, src/client/client.css</files>
  <read_first>
    - src/client/client.js (renderCssPanel/applyAllCssValues/renderActiveSplitScreen del Pla 01 — calca la branca de fase i el patró srcdoc-once; TEAM_CSS_STATE handler)
    - src/shared/effects.js (attachRule/applyAction — Task 2 d'aquest pla)
    - src/shared/robotTemplate.js (JS_EVENTS/JS_ELEMENTS/JS_ACTION_KEYS/JS_COMPOSITE_KEYS per poblar els desplegables)
    - .planning/phases/03-joc-fases-css-i-js/03-UI-SPEC.md (Component Inventory JS, Copywriting Contract, Motion Contract, Color §JS phase — mustard decoratiu, mai únic indicador)
    - .planning/phases/03-joc-fases-css-i-js/03-RESEARCH.md §Pattern 2 (reattach), §Pitfall 3 (listeners obsolets)
  </read_first>
  <behavior>
    - El panell arrenca amb una fila de regla buida; el desplegable mostra la frase natural.
    - En completar una regla i prémer "Veure" → el robot reacciona a la interacció d'aquella regla.
    - "Afegir JavaScript" afegeix una fila; es desactiva al límit 5-6 (D-12).
    - Una parella (event, origen) ja usada apareix desactivada a altres files (D-15); una acció composta desactiva el desplegable destí (D-17).
    - Editar/afegir regles reconstrueix el srcdoc una vegada i reattacha totes les regles a `load` (cap listener obsolet).
    - Regla amb origen/destí absent → no-op silenciós.
  </behavior>
  <action>
    (1) client.css — afegeix estils tokenitzats per `.js-rules` (pila de files + tira d'identitat esquerra `var(--phase-js)`), `.js-rule` (fila de 4 `<select>` + botó "Veure" + afordança de retirar; embolcalla amb gràcia dins del 50%), i `.js-add` (botó "Afegir JavaScript": fons `var(--phase-js)` + vora `var(--color-border)` + etiqueta `var(--color-text)` fosca; estat disabled al límit). Els `<select>` i "Veure" usen estil natiu/neutre amb outline de focus `var(--color-text)` (el mustard té contrast insuficient com a únic indicador — UI-SPEC Checker note). ≥44px àrea d'impacte. Motion en `@media (prefers-reduced-motion: no-preference)` (slide-in fila nova, collapse en retirar). Cap token nou.
    (2) client.js — al `wrapPreview` (reescrit al Pla 01) afegeix les classes d'efecte que l'intèrpret commuta: `.js-rotate` (keyframes de rotació), `.js-scale` (transform scale), i la classe d'aclucar `.js-squint` que usin les compostes — dins del `<style>` del srcdoc, respectant `prefers-reduced-motion`.
    (3) client.js — afegeix `renderJsPanel(jsRules)`: construeix la pila de files des de l'estat; cada fila = frase "Quan [event ▾] a [origen ▾] → a l'element [destí ▾] → Fes [acció ▾]" amb paraules connectores com a text (Body), i 4 `<select>` poblats de JS_EVENTS/JS_ELEMENTS/JS_ACTION_KEYS+JS_COMPOSITE_KEYS. Desactiva opcions (event,origen) ja usades a altres files (D-15, estat disabled, cap color/text d'error). En triar una acció composta, desactiva el `<select>` destí (D-17). Botó "Veure" per fila = preview client-only NOMÉS d'aquella regla. Botó "Afegir JavaScript" afegeix fila buida, disabled a JS_ROW_LIMIT. Afordança de retirar fila (undo sense confirmació). En qualsevol canvi complet de regla, `socket.emit(EVENTS.TEAM_SET_RULES, { rules })` (whole-array). Etiquetes read-only via `textContent` (GAME-06, mai innerHTML).
    (4) client.js — a `renderActiveSplitScreen` afegeix `else if (state.phase === 'js')`: `panel.appendChild(renderJsPanel(latestJsRules))`; construeix el srcdoc UNA vegada (robot completament estilitzat, arrossegant el resultat de la fase CSS via latestCssValues) i a `load` aplica `applyAllCssValues(latestCssValues)` i després reattacha totes les regles amb `attachRule(frame.contentDocument, r)` (Pitfall 3: rebuild-then-reattach evita listeners obsolets). "Veure" reconstrueix i attacha NOMÉS aquella regla. Desactiva controls quan frozen (D-11).
    (5) client.js — declara `let latestJsRules = [];` i `socket.on(EVENTS.TEAM_JS_STATE, ({ jsRules }) => { latestJsRules = jsRules || []; if (latestState?.phase === 'js') { /* rebuild srcdoc + reattach */ } });` (reutilitza el mateix camí de rebuild que la branca js de renderActiveSplitScreen).
  </action>
  <verify>
    <automated>node --test test/effects.test.js test/jsPhase.test.js test/cssPhase.test.js test/placement.test.js  # cap regressió</automated>
    <automated>node -e "import('./src/shared/effects.js').then(m=>{const d={querySelector:()=>null,querySelectorAll:()=>[]}; m.applyAction(d,{origen:'nas',desti:'nas',accio:'canviar-color'}); m.attachRule(d,{event:'click',origen:'nas',desti:'nas',accio:'girar'}); console.log('effects no-op ok')}).catch(e=>{console.error(e);process.exit(1)})"</automated>
    <human-check>
      Local (`npm run dev` + `npm run server`) o a l'aula: com a admin inicia la fase `js` (després d'una fase css). A la pantalla d'equip:
      1. Construeix una regla completa (p.ex. Quan `click` a `nas` → a l'element `#robot-cap` → Fes `Girar`) i prem "Veure" → el robot reacciona a aquella interacció.
      2. Confirma que una parella (event, origen) ja usada queda desactivada a altres files (D-15) i que triar una acció composta desactiva el destí (D-17).
      3. "Afegir JavaScript" arriba al límit de 5-6 files i llavors es desactiva.
      4. Prova una regla amb origen absent (peça HTML no col·locada a la fase anterior) → cap efecte, cap error de consola, la resta del panell funciona (GAME-07/D-19).
      5. F5 a la pantalla d'equip → les regles construïdes es recuperen.
      6. Verifica que el card de l'Admin mostra només estat de connexió (cap comptador) durant la fase js (D-22).
    </human-check>
  </verify>
  <done>El robot reacciona a les regles via "Veure"; anti-repeat/composite/límit per disabled; no-op sobre element absent; recuperació F5; card admin només-connexió.</done>
  <acceptance_criteria>
    effects.test.js/jsPhase.test.js/cssPhase.test.js/placement.test.js verds. Una regla completa + "Veure" fa reaccionar el robot. Anti-repeat (D-15) i composite-disables-destí (D-17) es comuniquen només amb estat disabled (cap error/vermell). "Afegir JavaScript" es desactiva al límit (D-12). Editar regles no acumula listeners obsolets (una interacció dispara l'efecte una sola vegada). Cap error de consola en una regla sobre element absent. Recuperació F5 de jsRules. L'iframe roman scriptless (`allow-same-origin`, mai allow-scripts).
  </acceptance_criteria>
</task>

</tasks>

## Artifacts this phase produces (Pla 02)

**Nous símbols / fitxers:**
- `JS_EVENTS`, `JS_ELEMENTS`, `JS_ACTION_KEYS`, `JS_COMPOSITE_KEYS`, `JS_ROW_LIMIT` (frozen exports, `src/shared/robotTemplate.js`).
- Fitxer nou `src/shared/effects.js`: `ACTIONS`, `COMPOSITES`, `applyAction(doc, rule)`, `attachRule(doc, rule)`.
- Classes CSS d'efecte (al srcdoc de `wrapPreview`): `.js-rotate`, `.js-scale`, `.js-squint`.
- Funcions server (`src/server/gameState.js`): `setJsRules(teamId, rules)`, `getTeamRules(teamId)`; camp `team.jsRules`.
- Socket events (`src/server/events.js`): `TEAM_SET_RULES` (`'team:set-rules'`, client→server), `TEAM_JS_STATE` (`'team:js-state'`, server→client dirigit).
- Funcions client (`src/client/client.js`): `renderJsPanel()`; variable `latestJsRules`; handler `TEAM_JS_STATE`.
- Classes CSS panell (`src/client/client.css`): `.js-rules`, `.js-rule`, `.js-add`.
- Fitxers nous de test: `test/effects.test.js`, `test/jsPhase.test.js`.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → server (socket) | El payload `TEAM_SET_RULES` és input no confiable; identitat de `socket.data.teamId`, mai del payload; vocabulari revalidat contra taules frozen |
| desplegables → intèrpret | Els valors dels `<select>` són NOMÉS claus a taules frozen (`JS_EVENTS`/`JS_ELEMENTS`/`ACTIONS`/`COMPOSITES`); una clau desconeguda és un no-op, mai codi |
| parent → iframe | L'iframe és scriptless (`allow-same-origin`, sense `allow-scripts`); els efectes toquen només `style`/`classList`/`visibility`, mai `innerHTML` |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-07 | Elevation | execució de codi via regles "JS" | high | mitigate | Cap `eval`/`Function`/construcció de codi des dels desplegables; taula de despatx frozen keyed per vocabulari tancat; clau desconeguda = no-op (03-RESEARCH §Pattern 2, §Don't Hand-Roll) |
| T-03-08 | Elevation | iframe unsandboxing | high | mitigate | Mantenir `sandbox="allow-same-origin"` únic; MAI afegir `allow-scripts` (l'intèrpret viu al parent, no dins l'iframe) |
| T-03-09 | Spoofing/Tampering | `setJsRules` cross-team (teamId forjat) | high | mitigate | Identitat de `socket.data.teamId`; el servidor ignora la identitat del payload (mirall TEAM_PLACE_PIECE, V4) |
| T-03-10 | Tampering | vocabulari/estructura de regla forjats | medium | mitigate | Servidor revalida cada camp contra enums frozen + anti-repeat (D-15) + límit ≤6 + composite⇒destí null (D-17) abans d'emmagatzemar (V5) |
| T-03-11 | Tampering | XSS via efectes sobre el DOM del preview | medium | mitigate | Els efectes muten només `style`/`classList`/`visibility`, mai `innerHTML`; DOMPurify sense canvis al markup assemblat |
| T-03-12 | DoS | payload malformat / storm de re-render | medium | mitigate | `safeHandler` embolcalla el handler; `mutation-returns-bool` + emissió dirigida `team:<id>`; whole-array replace (≤6 regles minúscules) |

**Nota (Package Legitimacy Gate):** aquesta fase NO instal·la cap paquet extern (03-RESEARCH §Package Legitimacy Audit: "Not applicable"). Cap checkpoint d'instal·lació requerit.
</threat_model>

<verification>
- `node --test test/effects.test.js test/jsPhase.test.js` — intèrpret no-op + round-trip de regles verds.
- `node --test test/cssPhase.test.js test/placement.test.js test/timer.test.js test/monitoring.test.js test/roundtrip.test.js test/adminAuth.test.js` — cap regressió (CSS del Pla 01 + fases prèvies).
- Comprovació estàtica: `effects.js` no llença sobre un doc stub buit (GAME-07).
- Human-check (fi de fase): regla + "Veure" fa reaccionar el robot; anti-repeat/composite/límit via disabled; no-op sobre element absent; recuperació F5; card admin només-connexió.
</verification>

<success_criteria>
- SC2 (ROADMAP): un equip construeix regles JS amb desplegables i en veu l'efecte aplicat a la preview. ✔ round-trip + human-check.
- SC3 (part JS): la fase JS opera sobre el DOM parcial sense trencar-se (regla sobre element absent = no-op silenciós). ✔ effects.test.js + human-check.
- GAME-06 preservat: cap text lliure esdevé codi; sense eval/Function; iframe scriptless.
- Cap regressió a CSS ni a fases prèvies.
</success_criteria>

<output>
Crea `.planning/phases/03-joc-fases-css-i-js/03-02-SUMMARY.md` en acabar.
</output>