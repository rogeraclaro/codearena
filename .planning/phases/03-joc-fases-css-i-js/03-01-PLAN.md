---
phase: 03-joc-fases-css-i-js
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - test/cssPhase.test.js
  - src/shared/robotTemplate.js
  - src/server/gameState.js
  - src/server/events.js
  - src/server/socketHandlers.js
  - src/client/client.js
  - src/client/client.css
autonomous: true
requirements: [GAME-04, GAME-07]
must_haves:
  truths:
    - "Un equip omple un forat CSS (color picker o slider) sobre sintaxi CSS real i el robot de la preview canvia a l'instant sense recarregar l'iframe (SC1)"
    - "El valor CSS triat sobreviu un F5/reconnexió: es recupera dirigit des del servidor (CORE-03)"
    - "Un forat CSS sobre un element absent (DOM incomplet) és un no-op silenciós: cap error de consola, cap bloqueig del panell (GAME-07/D-19)"
    - "CSS_HOLES cobreix exactament els forats definits element per element (D-04: .orella top/left/right/width; D-06: .ull border-radius; D-07: #nas border-radius/mida), mai sobre estructura fixa com #robot-contenidor o l'antena ::before"
    - "El panell CSS reutilitza el mateix split de pantalla establert a la Fase HTML (D-20), només canviant el contingut de .action-panel"
    - "La divisió de pantalla és exactament 50/50 a totes 3 fases (D-21)"
    - "El card de l'Admin mostra només estat de connexió per a la fase css, sense comptador N/total (D-22)"
  artifacts:
    - path: src/shared/robotTemplate.js
      provides: "CSS_HOLES (frozen: holeId -> {var, selector, control, validate, default}, 16 entrades)"
      min_lines: 180
    - path: src/server/gameState.js
      provides: "team.cssValues, setCssValue() (mutation-returns-bool), getTeamStyle()"
      min_lines: 200
    - path: src/server/events.js
      provides: "TEAM_SET_CSS, TEAM_CSS_STATE"
      min_lines: 28
    - path: src/client/client.js
      provides: "renderCssPanel(), applyCssHole(), applyAllCssValues(); wrapPreview() reescrit amb 16 var() holes"
      min_lines: 940
    - path: src/client/client.css
      provides: ".css-forat-group / .css-forat(--color/--range); .active-split grid 1fr 1fr (D-21)"
      min_lines: 302
    - path: test/cssPhase.test.js
      provides: "round-trip + F5 + validació (harness estil placement.test.js)"
      min_lines: 80
  key_links:
    - "slider `input` -> applyCssHole (CSSOM setProperty local, instantani) ; slider `change` -> TEAM_SET_CSS -> setCssValue -> TEAM_CSS_STATE dirigit -> applyAllCssValues"
    - "reconnexió (connection handler) -> socket.emit TEAM_CSS_STATE dirigit a l'owner amb getTeamStyle()"
    - "wrapPreview construeix el srcdoc UNA vegada per entrada de fase; els canvis viuen via custom properties, mai reassignant srcdoc (Pitfall 1)"
---

<objective>
Entrega la **Fase CSS jugable** de CodeArena com a llesca vertical completa: sobre l'HTML del robot Bender muntat a la Fase 2 (potencialment incomplet), l'equip omple un subconjunt controlat de propietats CSS reals (color pickers + sliders, mai text lliure) i veu el robot adquirir el seu aspecte Bender a l'instant a la preview de la dreta.

Purpose: Compleix GAME-04 (CSS foradat amb sintaxi real i valors controlats) i la part CSS de GAME-07 (opera sobre DOM incomplet sense trencar-se). És el primer dels dos subsistemes de joc d'aquesta fase; el subsistema JS (Pla 02) es construeix a sobre del robot ja estilitzat.

Output: Vocabulari frozen `CSS_HOLES`, estat autoritatiu per equip (`cssValues`) amb mutador `mutation-returns-bool`, events de socket dirigits, panell de forats client amb actualització en viu via CSSOM, i el `wrapPreview()` reescrit amb el CSS definitiu del Bender on cada forat és una custom property.
</objective>

## Phase Goal (MVP user story)

**As a** alumne d'FP sense experiència prèvia (en equip, davant un ordinador d'aula), **I want to** omplir el "codi CSS foradat" del robot amb color pickers i sliders sobre sintaxi CSS real, **so that** entengui que el CSS és l'aparença — veient el robot canviar de forma i color a l'instant a mesura que toco cada valor.

> Story derivada del `**Goal:**` de ROADMAP.md §Phase 3 i dels Success Criteria 1 i 3 (no inventada: rol = equip/alumne del PROJECT.md Core Value, capacitat = CSS foradat GAME-04, outcome = "CSS = aparença" del Core Value).

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-joc-fases-css-i-js/03-CONTEXT.md
@.planning/phases/03-joc-fases-css-i-js/03-RESEARCH.md
@.planning/phases/03-joc-fases-css-i-js/03-PATTERNS.md
@.planning/phases/03-joc-fases-css-i-js/03-UI-SPEC.md

# Analog directes (copiar-ne la forma, no reinventar):
@src/shared/robotTemplate.js
@src/server/gameState.js
@src/server/events.js
@src/server/socketHandlers.js
@src/client/client.js
@src/client/client.css
@test/placement.test.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Test-first — round-trip autoritatiu de la fase CSS (RED)</name>
  <files>test/cssPhase.test.js</files>
  <read_first>
    - test/placement.test.js (tot: harness real-server port-efímer, helpers `once`/`onceOrTimeout`/`connectAndAwait`, casos ordenats round-trip). Copia'n el harness verbatim.
    - src/server/events.js (importa noms d'event, mai literals al test)
    - .planning/phases/03-joc-fases-css-i-js/03-RESEARCH.md §Code Examples (Example 3 handler, Example 4 no-op) i §Pattern 3
  </read_first>
  <action>
    Crea test/cssPhase.test.js copiant el harness de placement.test.js (startServer(0) a `before`, `httpServer.close` a `after`, helpers `once`/`onceOrTimeout`/`connectAndAwait`, setup ordenat que registra equips, dos equips trien, i inicia la fase — però amb `phase: 'css'` en comptes de 'html' al `admin:start-phase`). Importa `EVENTS` d'src/server/events.js — cap literal d'event-name al test. Escriu aquests casos, que han de FALLAR ara (encara no existeixen ni els events ni el mutador):
    - SET-CSS-OK: un `TEAM_SET_CSS` vàlid ({holeId d'un forat color, value hex `#8a9ba8`}) retorna `TEAM_CSS_STATE` dirigit a l'owner amb `cssValues[holeId]===value`.
    - SET-CSS-NOOP: reenviar el MATEIX value ja emmagatzemat NO produeix segon `TEAM_CSS_STATE` (usa `onceOrTimeout` → ha de resoldre `undefined`) — verifica mutation-returns-bool anti-storm.
    - SET-CSS-INVALID: un value hex invàlid (p.ex. `not-a-color`) NO produeix `TEAM_CSS_STATE` (V5 rebuig servidor).
    - SET-CSS-WRONG-PHASE: un `TEAM_SET_CSS` mentre `phase==='html'` NO produeix `TEAM_CSS_STATE` (guard `state.phase !== 'css'`).
    - NO-SESSION-BROADCAST: un segon equip NO rep res del `TEAM_SET_CSS` del primer (emissió dirigida `team:<id>`, Pitfall 1/2).
    - V4-FORGE: forjar `teamId` al payload no muta el board d'un altre equip (identitat de socket.data, no del payload).
    - F5-CSS-RECOVERY: reconnectar amb el token previ (forceNew, mateix auth.token) recupera els `cssValues` emmagatzemats via un `TEAM_CSS_STATE` dirigit en connectar (CORE-03).
    Segueix el patró d'aserció REJECT de placement.test.js: `onceOrTimeout(...)` retorna `undefined` → el cas passa quan NO hi ha broadcast.
  </action>
  <verify>
    <automated>node --test test/cssPhase.test.js  # ha de FALLAR ara (RED): els events/mutador no existeixen encara</automated>
  </verify>
  <done>test/cssPhase.test.js existeix i falla (RED) amb tots els casos de round-trip CSS definits.</done>
  <acceptance_criteria>
    test/cssPhase.test.js existeix, importa EVENTS (cap literal d'event-name), i en executar-se falla amb els casos definits pendents d'implementació (RED verificable). El harness arrenca i tanca el servidor net (sense handles penjats).
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Backend — CSS_HOLES + estat autoritatiu i events dirigits (GREEN)</name>
  <files>src/shared/robotTemplate.js, src/server/gameState.js, src/server/events.js, src/server/socketHandlers.js</files>
  <read_first>
    - src/shared/robotTemplate.js (SLOTS, CONTAINERS, `Object.freeze` idiom, pieceLabel regex-derive-from-html) — deriva selectors, no els repeteixis a mà
    - src/server/gameState.js línies 24-31 (team init), 83-117 (placePiece/removePiece/getTeamBoard — patró mutation-returns-bool), 187-202 (export)
    - src/server/events.js (tot, 26 línies — agrupació client→server / server→client)
    - src/server/socketHandlers.js línies 79-100 (connection recovery) i 226-239 (handler TEAM_PLACE_PIECE)
    - .planning/phases/03-joc-fases-css-i-js/03-RESEARCH.md §Forat→var() Mapping (16 controls + noms var()) i §Pattern 3
    - .planning/phases/03-joc-fases-css-i-js/03-PATTERNS.md (gameState/events/socketHandlers analogs)
  </read_first>
  <behavior>
    Fa passar test/cssPhase.test.js (Task 1):
    - setCssValue amb forat+value vàlid muta i retorna true → handler emet TEAM_CSS_STATE dirigit.
    - value repetit / hex invàlid / holeId desconegut / fora de fase css / timer frozen → retorna false → cap broadcast.
    - reconnexió emet TEAM_CSS_STATE amb cssValues emmagatzemats.
  </behavior>
  <action>
    (1) robotTemplate.js — afegeix `export const CSS_HOLES = Object.freeze({...})`: 16 entrades keyed per holeId, cada valor `Object.freeze({ var: '--nom', selector: '<selector CSS>', control: 'color'|'range', validate(value)->bool, default: '<valor per defecte>' })`. Noms de var() i selectors exactament segons 03-RESEARCH §Forat→var() Mapping: `--antena-bg`, `--antena-border`, `--orella-top`, `--orella-offset`, `--orella-width`, `--ulls-bg`, `--ulls-top`, `--ull-radius`, `--nas-radius`, `--nas-size`, `--boca-height`, `--boca-width`, `--boca-dents`, `--cap-bg`, `--cap-border-color`, `--cap-border-width`. Els `control:'color'` validen `^#[0-9a-fA-F]{6}$`; els `control:'range'` validen numèric-dins-de-rang (parseja el número del value tipus `40px`/`50%` i comprova min/max/step segons UI-SPEC §Forat Inventory — targets locked, ranges tunables). Deriva/cross-checka els selectors dels elements contra `SLOTS[].html`/`CONTAINERS` (nota: `.antena`, `.ull` no tenen id; les orelles i nas/boca sí). Comenta cada forat amb la seva decisió font (D-03…D-09).
    (2) gameState.js — a `state.teams.set(...)` (línia ~29) afegeix `cssValues: {}` al costat de `placement: {}`. Afegeix `setCssValue(teamId, holeId, value)` calcada de `placePiece`: guard `!team`→false; guard `state.phase !== 'css' || state.timerStatus === 'frozen'`→false (GAME-07/D-11); `const hole = CSS_HOLES[holeId]; if (!hole) return false;` (V5 enum); `if (!hole.validate(value)) return false;`; `if (team.cssValues[holeId] === value) return false;` (no-op anti-storm); assigna i retorna true. Afegeix `getTeamStyle(teamId)` calcat de `getTeamBoard`: retorna `{ cssValues: { ...team.cssValues } }` (còpia superficial, mai la referència viva). Importa `CSS_HOLES` de robotTemplate.js. Registra `setCssValue` i `getTeamStyle` a l'objecte `export const gameState`.
    (3) events.js — afegeix `TEAM_SET_CSS: 'team:set-css'` al bloc client→server i `TEAM_CSS_STATE: 'team:css-state'` al bloc server→client (mantén els comentaris de grup i la regla "cap literal d'event-name enlloc més").
    (4) socketHandlers.js — afegeix un handler `socket.on(EVENTS.TEAM_SET_CSS, safeHandler(...))` calcat de TEAM_PLACE_PIECE: `teamId = socket.data.teamId` (V4, mai del payload); valida `holeId`/`value` són strings (V5); si `gameState.setCssValue(teamId, holeId, value)` → `io.to(\`team:${teamId}\`).emit(EVENTS.TEAM_CSS_STATE, gameState.getTeamStyle(teamId))` + `io.to('admin').emit(EVENTS.SESSION_FULL_STATE, gameState.getPublicState())` (només-connexió; MAI `io.to('session')`). Al bloc de reconnexió per equip (~línia 91-93, després del `TEAM_BOARD_STATE`) afegeix `socket.emit(EVENTS.TEAM_CSS_STATE, gameState.getTeamStyle(socket.data.teamId));` (dirigit a l'owner, CORE-03).
    NO toquis `getPublicState().progress` — ja retorna `null` fora de `html` (D-22, verificat).
  </action>
  <verify>
    <automated>node --test test/cssPhase.test.js  # GREEN: tots els casos CSS passen</automated>
    <automated>node --test test/placement.test.js test/timer.test.js test/monitoring.test.js  # cap regressió a fases prèvies</automated>
  </verify>
  <done>test/cssPhase.test.js verd i cap regressió; CSS_HOLES (16) + setCssValue/getTeamStyle + events dirigits funcionals.</done>
  <acceptance_criteria>
    test/cssPhase.test.js passa completament (SET-CSS-OK/NOOP/INVALID/WRONG-PHASE/NO-SESSION-BROADCAST/V4-FORGE/F5-CSS-RECOVERY). Els tests existents (placement/timer/monitoring) segueixen verds. `CSS_HOLES` té 16 entrades frozen amb `validate` funcional; `setCssValue` retorna `false` (cap broadcast) per value repetit, hex invàlid, holeId desconegut, fora de fase css, o timer frozen. `getPublicState().progress` roman `null` a la fase css (sense canvi de codi).
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Client — wrapPreview reescrit (Bender CSS + 16 var() forats) + aplicació CSSOM</name>
  <files>src/client/client.js</files>
  <read_first>
    - src/client/client.js línies 1-27 (imports, EVENTS), 283-287 (latestPlacement), 628-726 (wrapPreview + assemblePreview — el bloc placeholder de depuració a ~692-701 és el que se substitueix), 642-653 (capa #robot-fons — NO és forat, D-10)
    - /Users/rogermasellas/Desktop/imparticio/index.html línies 25-179 (CSS definitiu del Bender — valors fixos font de veritat; llegeix-lo en implementar)
    - .planning/phases/03-joc-fases-css-i-js/03-RESEARCH.md §Pattern 1, §Code Examples 1-2, §Forat→var() Mapping (files ⚠), §Open Questions (RESOLVED) 1-2
    - .planning/phases/03-joc-fases-css-i-js/03-UI-SPEC.md (Forat Inventory, Typography "forat IS real CSS")
  </read_first>
  <behavior>
    - Slider `input` → el robot canvia a l'instant sense recàrrega d'iframe (custom property via CSSOM setProperty).
    - En entrar a la fase css o en F5 → srcdoc construït UNA vegada; els valors emmagatzemats s'apliquen a l'event `load`.
    - Un forat sobre element absent → no-op silenciós (cap error de consola).
  </behavior>
  <action>
    (1) client.js — reescriu `wrapPreview(inner)`: substitueix el bloc placeholder de depuració (~línies 692-701) pel CSS definitiu del Bender copiat de la font de veritat (Desktop/imparticio/index.html 25-179), amb cada un dels 16 forats expressat com var() amb default segons CSS_HOLES. Mantén intacta la capa `#robot-fons` (línies 642-653, D-10 — el fons NO és forat). Resol els 4 forats ⚠ amb aquests defaults raonats (03-RESEARCH §Open Questions (RESOLVED) — no els triïs en silenci, queden documentats aquí i es confirmen a la Task 4 `<human-check>`):
       • `--antena-bg` (D-03): la bola és `.antena::before` amb radial-gradient; el forat aplica un background via var(--antena-bg, radial-gradient(...)) amb el radial-gradient cian de la font com a default — el color triat APLANA el gradient a color pla (s'accepta perdre el matís; l'objectiu pedagògic és "l'alumne posa un valor CSS real i el veu").
       • `--antena-border` (D-03): la font no té border a la bola; afegeix un border via var(--antena-border, transparent) a `.antena::before` (default transparent).
       • `--ulls-top` (D-05): `.contenidor-ulls` és fill flex no posicionat; afegeix `position: relative` i un top via var(--ulls-top, 0px).
       • `--cap-bg` (D-09): el fons és un linear-gradient metàl·lic de 6 stops; el forat aplica un background via var(--cap-bg, linear-gradient(...)) amb el gradient metàl·lic de la font com a default — el color triat APLANA el gradient metàl·lic (s'accepta).
       • Discrepància D-08: el color de les dents té target `#fffcd3` (D-08, locked) però la font usa `#f2e6a8`; usa `#fffcd3` com a target de la Plana Model i `#f2e6a8` NOMÉS com a fallback per defecte del var() dents (ambdós stops clars del repeating-linear-gradient).
       Aplica els valors NOMÉS via CSSOM `setProperty` (mai interpolant text CSS — vector d'injecció, Pitfall 5).
    (2) client.js — afegeix `applyCssHole(holeId, value)` (obté `.preview-frame` contentDocument; si no ready → return; `cssVar = CSS_HOLES[holeId]?.var`; si desconegut → return; `doc.documentElement.style.setProperty(cssVar, value)`) i `applyAllCssValues(cssValues)` (itera i aplica cada valor). Nota especial `--orella-offset`: una sola custom property que el CSS mapeja a `left`/`right` simètric.
  </action>
  <verify>
    <automated>node --test test/cssPhase.test.js test/placement.test.js  # cap regressió; round-trip encara verd</automated>
    <automated>node --eval "import('node:fs').then(fs=>{const s=fs.readFileSync('src/client/client.js','utf8'); const n=(s.match(/var\(--/g)||[]).length; if(n<16){console.error('var(-- count',n);process.exit(1)} console.log('wrapPreview var() holes ok:'+n)})"</automated>
  </verify>
  <done>`wrapPreview` conté el CSS definitiu del Bender amb els 16 forats com a var(); `applyCssHole`/`applyAllCssValues` apliquen valors via CSSOM setProperty sobre element present i són no-op sobre element absent.</done>
  <acceptance_criteria>
    `wrapPreview` construeix el srcdoc amb ≥16 referències var(--…) (una per forat) i la capa `#robot-fons` intacta (D-10). `applyCssHole` és no-op silenciós si el contentDocument no és ready o el holeId és desconegut (cap error de consola). Els 4 forats ⚠ estan expressats amb els defaults documentats. Els valors s'apliquen NOMÉS via `setProperty` (mai text CSS concatenat). Cap regressió a cssPhase/placement.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Client — panell de forats CSS + branca split 50/50 + handler TEAM_CSS_STATE</name>
  <files>src/client/client.js, src/client/client.css</files>
  <read_first>
    - src/client/client.js línies 1-27 (imports, EVENTS), 283-287 (latestPlacement), 775-828 (renderActiveSplitScreen, branca html + iframe scriptless línia 809 — NO afegir mai allow-scripts), 916-933 (socket handlers session:full-state / TEAM_BOARD_STATE), i `wrapPreview`/`applyCssHole`/`applyAllCssValues` (Task 3 d'aquest pla)
    - src/client/client.css línies 34-61 (.active-split / .action-panel / .preview-frame)
    - .planning/phases/03-joc-fases-css-i-js/03-RESEARCH.md §Forat→var() Mapping
    - .planning/phases/03-joc-fases-css-i-js/03-UI-SPEC.md (Component Inventory CSS, Layout Contract, Forat Inventory)
  </read_first>
  <behavior>
    - Slider `change` / color `change` → emet TEAM_SET_CSS; en rebre TEAM_CSS_STATE reconcilia tots els valors.
    - Panell i preview ocupen exactament 50% cadascun a les 3 fases.
    - En entrar a la fase css o en F5 → srcdoc construït UNA vegada (via `wrapPreview` de la Task 3), valors aplicats a l'event `load`.
  </behavior>
  <action>
    (1) client.css — canvia el `grid-template-columns` de `.active-split` (línia 40) de `minmax(280px, 40%) 1fr` a `1fr 1fr` (D-21, exacte 50/50, aplica a html/css/js). Afegeix `.action-panel { max-height: 100vh; overflow-y: auto; }` (overflow vertical per panells densos; la preview no scrolla mai). Afegeix estils tokenitzats per `.css-forat-group` (marc per selector, tira d'identitat esquerra `var(--phase-css)`, etiquetes monospace d'obertura i tancament de bloc via `var(--font-family-mono)`), `.css-forat` / `.css-forat--color` / `.css-forat--range` (fila declaració real: etiqueta propietat monospace + control natiu + readout monospace del valor viu), reutilitzant només tokens `var(--space-*)`/`var(--color-*)`/`var(--phase-css)` existents — cap token nou. Cada control ≥44px d'àrea d'impacte (`--hit-target-min`). Slider fill/thumb en `var(--phase-css)`.
    (2) client.js — afegeix `renderCssPanel(cssValues)` que construeix els grups de selector des de CSS_HOLES (agrupats per element, ordre top→bottom robot-espacial recomanat a UI-SPEC; discreció). Cada forat color → un `input` natiu de tipus color, cada forat range → un `input` natiu de tipus range amb min/max/step de CSS_HOLES i readout monospace del valor viu. Etiquetes de propietat i signes de puntuació són text monospace read-only (via `textContent`, mai `innerHTML`, GAME-06). Cablejat: `input` → `applyCssHole` local instantani; `change` → `socket.emit(EVENTS.TEAM_SET_CSS, { holeId, value })`.
    (3) client.js — a `renderActiveSplitScreen`, afegeix la branca `else if (state.phase === 'css')`: `panel.appendChild(renderCssPanel(latestCssValues))`; construeix el srcdoc del preview UNA vegada (`wrapPreview(assembleRobotMarkup(latestPlacement))` — reutilitza el markup net de `assemblePreview`), i a l'event `load` (`{ once: true }`) crida `applyAllCssValues(latestCssValues)`. NO reassignis srcdoc a cada canvi (Pitfall 1). Mantén l'iframe scriptless (línia 809, `sandbox="allow-same-origin"` sense allow-scripts). Desactiva tots els controls quan `timerStatus==='frozen'` (D-11).
    (4) client.js — declara `let latestCssValues = {};` (costat de latestPlacement) i afegeix un handler `socket.on(EVENTS.TEAM_CSS_STATE, ...)` que desa `cssValues` a `latestCssValues` i, si la fase activa és css, crida `applyAllCssValues(latestCssValues)`.
  </action>
  <verify>
    <automated>node --test test/cssPhase.test.js test/placement.test.js  # cap regressió; round-trip encara verd</automated>
    <automated>node --eval "import('./src/shared/robotTemplate.js').then(m=>{const n=Object.keys(m.CSS_HOLES).length; if(n!==16){console.error('CSS_HOLES count',n);process.exit(1)} const bad=Object.entries(m.CSS_HOLES).filter(([k,h])=>typeof h.validate!=='function'||!h.var.startsWith('--')); if(bad.length){console.error('malformed holes',bad.map(b=>b[0]));process.exit(1)} console.log('CSS_HOLES ok:16')})"</automated>
    <automated>node --eval "import('node:fs').then(fs=>{const s=fs.readFileSync('src/client/client.css','utf8'); if(!/\.active-split[^}]*grid-template-columns:\s*1fr 1fr/.test(s)){console.error('split not 1fr 1fr');process.exit(1)} console.log('split 50/50 ok')})"</automated>
    <human-check>
      A l'aula (o local `npm run dev` + `npm run server`): inicia una sessió, registra un equip, i com a admin inicia la fase `css`. A la pantalla d'equip:
      1. Mou cada slider i tria cada color → el robot de la dreta canvia a l'instant SENSE parpelleig ni recàrrega del fons (`/robot-fons.png` no re-decodeix).
      2. Confirma que amb els valors target el robot arriba a l'aspecte Bender esperat (visor fosc, ulls grocs, dents metàl·liques, cap gris-blau, antena).
      3. **Confirma explícitament el tradeoff dels 4 forats ⚠**: antena-bg i cap-bg APLANEN el gradient a color pla; és acceptable per a l'objectiu pedagògic? (Si no, cal reobrir la decisió.)
      4. F5 a la pantalla d'equip → els valors CSS es recuperen.
      5. Verifica visualment que la divisió és 50/50 exacta a les fases html i css.
    </human-check>
  </verify>
  <done>El panell de forats renderitza; el split és 50/50; `TEAM_CSS_STATE` reconcilia tots els valors; 4 forats ⚠ confirmats al human-check.</done>
  <acceptance_criteria>
    `CSS_HOLES` té 16 forats ben formats. Moure un slider actualitza el robot via CSSOM sense reassignar srcdoc (una sola construcció de srcdoc per entrada de fase / F5). `TEAM_CSS_STATE` reconcilia tots els valors. La divisió és `1fr 1fr` a `.active-split`. Els 4 forats ⚠ estan resolts amb els defaults documentats i confirmats al human-check. Cap error de consola en tocar un forat d'un element absent. L'iframe roman `sandbox="allow-same-origin"` (mai allow-scripts).
  </acceptance_criteria>
</task>

</tasks>

## Artifacts this phase produces (Pla 01)

**Nous símbols / fitxers:**
- `CSS_HOLES` (frozen export, `src/shared/robotTemplate.js`) — 16 forats: keys `antena-bg`, `antena-border`, `orella-top`, `orella-offset`, `orella-width`, `ulls-bg`, `ulls-top`, `ull-radius`, `nas-radius`, `nas-size`, `boca-height`, `boca-width`, `boca-dents`, `cap-bg`, `cap-border-color`, `cap-border-width`.
- CSS custom properties (a `wrapPreview` srcdoc): `--antena-bg`, `--antena-border`, `--orella-top`, `--orella-offset`, `--orella-width`, `--ulls-bg`, `--ulls-top`, `--ull-radius`, `--nas-radius`, `--nas-size`, `--boca-height`, `--boca-width`, `--boca-dents`, `--cap-bg`, `--cap-border-color`, `--cap-border-width`.
- Funcions server (`src/server/gameState.js`): `setCssValue(teamId, holeId, value)`, `getTeamStyle(teamId)`; camp `team.cssValues`.
- Socket events (`src/server/events.js`): `TEAM_SET_CSS` (`'team:set-css'`, client→server), `TEAM_CSS_STATE` (`'team:css-state'`, server→client dirigit).
- Funcions client (`src/client/client.js`): `renderCssPanel()`, `applyCssHole()`, `applyAllCssValues()`; variable `latestCssValues`.
- Classes CSS (`src/client/client.css`): `.css-forat-group`, `.css-forat`, `.css-forat--color`, `.css-forat--range`.
- Fitxer nou: `test/cssPhase.test.js`.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → server (socket) | El payload `TEAM_SET_CSS` és input no confiable; identitat SEMPRE de `socket.data.teamId`, mai del payload |
| parent → iframe (contentDocument) | El codi parent és confiat; l'iframe és scriptless (`allow-same-origin`, sense `allow-scripts`) — pura superfície de render |
| valor de forat → CSSOM | El value ha de passar validació servidor abans d'emmagatzemar-se; s'aplica només via `setProperty` (mai text CSS concatenat) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-01 | Tampering | `TEAM_SET_CSS` value → CSSOM | high | mitigate | Servidor valida color `^#[0-9a-fA-F]{6}$` / slider numèric-dins-rang a `CSS_HOLES[holeId].validate` abans d'emmagatzemar; client aplica NOMÉS via `element.style.setProperty()` (CSSOM rebutja valors invàlids, cap escapada de context de text) — Pitfall 5 |
| T-03-02 | Spoofing/Tampering | `setCssValue` cross-team | high | mitigate | Identitat de `socket.data.teamId` (middleware), mai del payload; un equip no pot mutar els `cssValues` d'un altre (mirall TEAM_PLACE_PIECE, V4) |
| T-03-03 | DoS | broadcast storm en `input` continu | medium | mitigate | `mutation-returns-bool` + emissió dirigida `team:<id>`; emet només en `change` (valor assentat), mai en cada frame `input`; admin només-connexió — Pitfall 2 |
| T-03-04 | Elevation | iframe unsandboxing | high | mitigate | Mantenir `sandbox="allow-same-origin"` únic; MAI afegir `allow-scripts` (combinar-los permet a l'iframe treure's el sandbox — MDN) |
| T-03-05 | Tampering | XSS via markup assemblat | medium | mitigate | DOMPurify sense canvis a `assemblePreview`; els forats només toquen custom properties/`style`, mai `innerHTML` |
| T-03-06 | DoS | payload malformat crashant el procés compartit | medium | mitigate | Embolcallar el handler nou amb `safeHandler` existent; validació typeof abans de `gameState` |

**Nota (Package Legitimacy Gate):** aquesta fase NO instal·la cap paquet extern (03-RESEARCH §Package Legitimacy Audit: "Not applicable"). Cap checkpoint d'instal·lació requerit.
</threat_model>

<verification>
- `node --test test/cssPhase.test.js` — round-trip CSS complet verd (OK/NOOP/INVALID/WRONG-PHASE/NO-SESSION/V4-FORGE/F5).
- `node --test test/placement.test.js test/timer.test.js test/monitoring.test.js test/roundtrip.test.js test/adminAuth.test.js` — cap regressió de fases prèvies (el canvi 50/50 no trenca la fase html).
- Comprovació estàtica: `CSS_HOLES` = 16 forats ben formats; `wrapPreview` conté `var(--` per als forats.
- Human-check (fi de fase): fidelitat visual Bender, 4 forats ⚠ confirmats, 50/50, recuperació F5, no-op sobre element absent.
</verification>

<success_criteria>
- SC1 (ROADMAP): un equip omple el CSS foradat via color pickers i sliders sobre sintaxi CSS real i la preview reflecteix el canvi a l'instant. ✔ verificat per round-trip + human-check.
- SC3 (part CSS): la fase CSS opera sobre el DOM parcial sense trencar-se (forat sobre element absent = no-op). ✔ verificat per no error de consola + guards.
- Cap regressió a les fases html/timer/monitoring.
</success_criteria>

<output>
Crea `.planning/phases/03-joc-fases-css-i-js/03-01-SUMMARY.md` en acabar.
</output>
