---
phase: 02-joc-fase-html-blocs-drag-drop
plan: 03
type: execute
wave: 3
depends_on: ["02-01", "02-02"]
files_modified:
  - src/client/client.js
  - src/client/client.css
  - src/client/admin.js
  - src/client/public/antena.svg
  - src/client/public/orella.svg
  - src/client/public/fons.svg
autonomous: false
requirements: [GAME-03, GAME-06]
user_setup:
  - service: assets d'imatge del robot
    why: "L'art final d'antena/orella/fons el proporciona el projecte (D-01/D-03); els placeholders SVG del Pla 01 són fallback acceptable i no bloquegen la mecànica"
    dashboard_config:
      - task: "Deixar antena.svg / orella.svg / fons.svg (o versions raster amb el mateix path arrel-relatiu) a src/client/public/"
        location: "src/client/public/ (Vite el serveix a l'arrel /)"

must_haves:
  truths:
    - "La pantalla d'equip mostra el progrés propi 'N/8 peces' amb una fila de 8 pips que s'omplen en --phase-html a mesura que es col·loquen peces bones (D-14), derivat del comptador autoritatiu"
    - "La pista inicial ('Arrossega les peces als forats' + fletxa calaix→tauler) es mostra la primera vegada i desapareix permanentment quan el comptador passa de 0 a 1 (D-14)"
    - "El card de l'Admin mostra 'N/8 peces' per equip durant la Fase HTML, omplint l'espai de progrés reservat a la Fase 1 sense refer el disseny del card (D-15)"
    - "El robot renderitzat és visualment correcte: antenes/orelles amb la seva imatge i ids, i 'BEEP BEEP' visibles a l'instant; snap imant, rebot i shake de distractor es llegeixen com pretén el contracte d'UI"
  artifacts:
    - "src/client/client.js — .progress-pieces (pips N/8) + .drag-hint (pista dismissible)"
    - "src/client/client.css — estils de .progress-pieces i .drag-hint (tokens-only)"
    - "src/client/admin.js — team-card-progress omplert amb 'N/8 peces' durant la fase html (D-15)"
  key_links:
    - "team:board-state / session:full-state → recompte de placement → actualitza pips .progress-pieces i, si 0→1, elimina .drag-hint per sempre"
    - "renderAdmin/buildTeamCard → team.progress {placed,total} de getPublicState() → progressEl.textContent = `${placed}/${total} peces`"
---

<objective>
Afegeix la capa de feedback i acabat sobre la mecànica ja funcional (Plans 01-02): el progrés propi N/8 amb pips a la pantalla d'equip i la pista inicial que guia el primer gest (D-14), el progrés N/8 al card de l'Admin perquè el professor compari equips d'un cop d'ull (D-15), i una verificació visual/interactiva humana que confirma que el robot es renderitza amb identitat intacta i que el snap/rebot/shake es llegeixen com el contracte d'UI demana (tanca les assumpcions A1 revert i A2 DOMPurify preserva id/<output>).

Purpose: Convertir la mecànica correcta en una experiència llegible i sense fricció per al públic rector (adults ~40 anys, text mínim, iconografia clara — UX-01), i tancar el llaç de qualitat amb l'única verificació que no es pot automatitzar (percepció visual del gest i del robot real).

Output: indicadors de progrés (equip + admin), pista inicial dismissible, punt d'entrada per a l'art final via user_setup, i un checkpoint humà de validació visual.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-joc-fase-html-blocs-drag-drop/02-CONTEXT.md
@.planning/phases/02-joc-fase-html-blocs-drag-drop/02-UI-SPEC.md
@.planning/phases/02-joc-fase-html-blocs-drag-drop/02-PATTERNS.md
@.planning/phases/02-joc-fase-html-blocs-drag-drop/02-01-PLAN.md
@.planning/phases/02-joc-fase-html-blocs-drag-drop/02-02-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pantalla d'equip — progrés N/8 (pips) + pista inicial dismissible</name>
  <read_first>
    - src/client/client.js (renderActiveSplitScreen + renderBoardAndDrawer del Pla 01; latestPlacement; handler team:board-state; patró Lucide createElement per a la fletxa)
    - src/client/client.css (classes existents del Pla 01/02; capçalera tokens-only 1-3)
    - src/client/shared/tokens.css (--phase-html línia 48; --space-*; patró prefers-reduced-motion 97-101)
    - 02-UI-SPEC.md (§Component Inventory Progress N/8 + Initial hint; §Motion Initial hint loop + dismiss on first placement; §Layout Contract ordre top→bottom; §Color progress fill --phase-html)
    - 02-CONTEXT.md (D-14 layout esquerra: progrés propi + pista)
  </read_first>
  <files>src/client/client.js, src/client/client.css</files>
  <action>
    Progrés N/8 (D-14): sota la capçalera del panell (ordre de UI-SPEC §Layout: capçalera → progrés → calaix → pista → tauler) afegeix un bloc .progress-pieces amb una etiqueta "N/8 peces" (Label) i una fila de 8 pips; el nombre de pips omplerts = Object.keys(latestPlacement).length, i s'omplen amb var(--phase-html). Actualitza'l cada cop que arriba board-state (a renderBoardAndDrawer o al handler team:board-state) i també a l'update quirúrgic si cal. La font del recompte és el placement autoritatiu (no un càlcul divergent).
    Pista inicial (D-14): afegeix .drag-hint entre calaix i tauler amb la micro-còpia "Arrossega les peces als forats" (Body) i una fletxa Lucide (MoveRight o ArrowRight) apuntant del calaix cap als slots, amb un loop suau (translate ±4px) dins @media (prefers-reduced-motion: no-preference). Desapareix PERMANENTMENT la primera vegada que el recompte passa de 0 a 1 (marca un flag de sessió de pàgina perquè no reaparegui en re-renders posteriors). Amb reduced-motion, mostra la fletxa estàtica.
    A client.css afegeix .progress-pieces (fila de pips, pip omplert vs buit amb var(--phase-html)/var(--color-border)) i .drag-hint (text + fletxa), tot tokens-only (var(--*), cap literal). El loop de la fletxa dins prefers-reduced-motion.
  </action>
  <verify>
    <automated>set -o pipefail; cd "$PWD" && npm run build 2>&1 | tail -5 && npm test</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` completa sense errors i `npm test` segueix verd.
    - El recompte de pips deriva de latestPlacement (llegible al codi; no un comptador local independent que pugui divergir).
    - La pista es controla amb un flag que impedeix reaparèixer després del primer placement (llegible al codi).
    - .progress-pieces i .drag-hint no contenen literals hex/px; el loop de la fletxa està dins @media (prefers-reduced-motion: no-preference).
  </acceptance_criteria>
  <done>La pantalla d'equip comunica el progrés propi amb pips en color de fase i guia el primer gest amb una pista que desapareix un cop l'equip entén la mecànica.</done>
</task>

<task type="auto">
  <name>Task 2: Card de l'Admin — progrés 'N/8 peces' per equip durant la Fase HTML (D-15)</name>
  <read_first>
    - src/client/admin.js (buildTeamCard 322-361, progressEl .team-card-progress reservat buit línia 334-335; textContent anti-XSS 328; renderAdmin 363; estils .team-card-progress línia 135)
    - 02-CONTEXT.md (D-15 progrés N/8 al card, D-08 de Fase 1 espai reservat)
    - 02-01-PLAN.md (getPublicState().teams[].progress = {placed,total:8} durant fase html; emès a 'admin' a cada col·locació)
  </read_first>
  <files>src/client/admin.js</files>
  <action>
    A buildTeamCard, omple progressEl (.team-card-progress, avui reservat buit) quan team.progress existeix i té placed/total: progressEl.textContent = `${team.progress.placed}/${team.progress.total} peces` (DOM text API only, anti-XSS — patró textContent de la línia 328). Quan team.progress és null (fora de la fase html), deixa progressEl buit (comportament actual de Fase 1). No refacis el disseny del card ni afegeixis nous elements estructurals: només materialitzes l'espai ja reservat (D-15). El card ja es re-renderitza amb session:full-state (que ara arriba a l'admin a cada col·locació via emissió dirigida del Pla 01).
    Si cal, afegeix un estil mínim a .team-card-progress (a la secció d'estils d'admin.js) per a llegibilitat, tokens-only, sense refer el layout.
  </action>
  <verify>
    <automated>set -o pipefail; cd "$PWD" && npm run build 2>&1 | tail -5 && npm test</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` completa sense errors i `npm test` segueix verd.
    - progressEl s'omple via textContent (no innerHTML) amb el format "N/8 peces" quan team.progress existeix (llegible al codi).
    - Cap element estructural nou al card (només s'omple .team-card-progress existent).
  </acceptance_criteria>
  <done>El professor veu el progrés N/8 de cada equip al panell Admin, comparable d'un cop d'ull, durant la Fase HTML.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Checkpoint humà — validació visual i interactiva de la Fase HTML jugable</name>
  <action>Atura't i demana a l'humà que executi el guió de verificació de sota amb l'app en marxa (Admin + pantalla d'equip). No continuïs fins a rebre "approved" o la llista de problemes.</action>
  <what-built>
    Fase HTML jugable completa: calaix + tauler de slots, drag & drop amb snap imant (emptyInsertThreshold) i rebot natiu, type-check per slot (D-07), treure peça (D-10), distractors que reboten amb shake (D-11), preview del robot real via DOMPurify (D-13), progrés N/8 propi + pista inicial (D-14), i N/8 al card d'Admin (D-15). Assets: placeholders SVG del Pla 01, o l'art final si s'ha deixat a src/client/public/ (user_setup).
  </what-built>
  <how-to-verify>
    1. Arrenca: en un terminal `npm run server`; en un altre `npm run dev`. Obre l'Admin (client admin) i una pantalla d'equip (client) en pestanyes/finestres separades.
    2. A l'Admin: registra 3-4 equips i inicia la Fase HTML.
    3. A la pantalla d'equip: verifica que apareixen el calaix (8 peces bones + 2-3 distractors barrejats), el tauler de slots niats amb etiquetes tag+class en monospace sense angle brackets, el progrés "0/8 peces" i la pista amb fletxa.
    4. Arrossega una antena a prop (no exacte) d'un slot d'antena buit → ha d'entrar sola (imant) i fer el pulse d'acceptació; la pista ha de desaparèixer; el progrés puja a 1/8; a la preview de la dreta apareix la imatge d'antena amb la seva posició.
    5. Arrossega una peça sobre un slot de tipus incorrecte o lluny de tot → ha de tornar suaument al calaix (rebot), sense missatge d'error.
    6. Arrossega un distractor (🍌/🛞/👟) cap a qualsevol slot → sempre rebota + shake, sense error ni color vermell.
    7. Treure: arrossega una peça col·locada de tornada al calaix → el progrés baixa; sense diàleg de confirmació.
    8. Col·loca les 2 antenes i comprova que el calaix ja no ofereix més antenes (inventari); confirma que 'BEEP BEEP' apareix a la preview en col·locar la boca.
    9. F5 a la pantalla d'equip → el robot mig muntat es recupera exactament (mateixes peces col·locades).
    10. A l'Admin: comprova que el card de l'equip mostra "N/8 peces" coherent i que col·locar peces en UN equip no provoca cap salt/re-render a la pantalla d'un ALTRE equip.
    11. Deixa esgotar el timer (o durada curta) → l'overlay de congelat apareix I ja no es pot arrossegar cap peça (drag desactivat); el robot parcial es manté.
    12. Inspecció ràpida (DevTools) del srcdoc de la preview: confirma que els <img> conserven id (p.ex. id="antena-esquerra") i class, i que existeix <output id="boca">BEEP BEEP</output> (assumpció A2 — cal per al scoring de Fase 4).
  </how-to-verify>
  <resume-signal>Escriu "approved" o descriu els problemes observats (imant/rebot/shake, recuperació F5, re-render creuat, identitat del srcdoc, congelació)</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

Aquest pla no introdueix cap frontera de confiança nova. El card de l'Admin renderitza el comptador N/8 ja projectat pel servidor (getPublicState, sense token ni board cru); el progrés d'equip deriva del placement autoritatiu. Les fronteres rellevants (srcdoc, intents de col·locació) es mitiguen als Plans 01-02.

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-07 | Tampering (XSS via card) | admin team-card-progress render | low | mitigate | progressEl.textContent (DOM text API only), mai innerHTML — el nom d'equip ja segueix aquest patró; el comptador és numèric projectat pel servidor |
| T-02-08 | Information Disclosure | progrés N/8 mostrat | low | accept | El comptador N/8 no revela el board d'un altre equip (només el nombre agregat); segur difondre a l'admin per disseny (Pla 01) |
</threat_model>

<verification>
- `npm run build` sense errors; `npm test` verd (cap regressió).
- Checkpoint humà: snap imant + rebot + shake, recuperació F5, absència de re-render creuat, identitat del srcdoc (id/class/<output>), i congelació que atura el drag — tots confirmats visualment.
</verification>

<success_criteria>
- Progrés N/8 propi (pips) i pista inicial dismissible a la pantalla d'equip.
- Progrés N/8 per equip al card de l'Admin.
- Validació humana que el robot es renderitza amb identitat intacta i que el gest es llegeix segons el contracte d'UI (tanca A1/A2).
</success_criteria>

<output>
Create `.planning/phases/02-joc-fase-html-blocs-drag-drop/02-03-SUMMARY.md` when done
</output>
