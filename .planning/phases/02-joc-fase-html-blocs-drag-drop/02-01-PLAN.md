---
phase: 02-joc-fase-html-blocs-drag-drop
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - src/shared/robotTemplate.js
  - src/server/events.js
  - src/server/gameState.js
  - src/server/socketHandlers.js
  - src/client/client.js
  - src/client/client.css
  - src/client/shared/tokens.css
  - src/client/public/antena.svg
  - src/client/public/orella.svg
  - src/client/public/fons.svg
  - test/placement.test.js
autonomous: true
requirements: [GAME-03, GAME-06]
user_setup: []

must_haves:
  truths:
    - "Un equip arrossega una peça bona del calaix; si el tipus coincideix amb un slot buit compatible, hi encaixa amb imant (emptyInsertThreshold); el servidor valida i guarda a team.placement (autoritatiu, GAME-03) — per D-07 la identitat esquerra/dreta la dona el slot, no la peça"
    - "La preview de la dreta es re-renderitza a l'instant amb el robot real assemblat des de la plantilla fixa i sanejat amb DOMPurify preservant id/class/src/alt i <output> (D-13, reutilitza la canonada iframe de Fase 1 — SC-2)"
    - "Les col·locacions es projecten dirigides NOMÉS a team:<id> (board complet) i a 'admin' (comptador N/8), MAI a tota la room 'session' (evita la tempesta de re-render entre equips, Pitfall 1)"
    - "Un F5/reconnexió recupera el robot mig muntat via team:board-state emès en connectar (CORE-03), sense protocol de resync nou"
    - "Cap camí permet escriptura lliure de codi: els chips són read-only i el srcdoc s'assembla EXCLUSIVAMENT des de SLOTS[].html + el mapa d'enums, mai des de text d'usuari (GAME-06)"
    - "robotTemplate.js defineix #robot-cap com a <div> (no <article>) [D-02]"
    - "robotTemplate.js fixa el text predefinit \"BEEP BEEP\" dins <output id=\"boca\"> i src/alt d'imatge predefinits; cap entrada de text lliure [D-04, GAME-06]"
    - "robotTemplate.js inclou <button id=\"nas\"> reservat per a la fase JS (ha d'existir a la plantilla) [D-05]"
    - "robotTemplate.js / servidor pre-construeix el fons fix i TOTS els contenidors (section#robot-contenidor, div#robot-cap); els equips només col·loquen peces de la cara als slots [D-06, Tier A]"
  artifacts:
    - "src/shared/robotTemplate.js — SLOTS/PIECES/DISTRACTORS (font única de la veritat, D-01), Object.freeze"
    - "src/server/events.js — +TEAM_PLACE_PIECE, TEAM_REMOVE_PIECE, TEAM_BOARD_STATE"
    - "src/server/gameState.js — team.placement + placePiece() + getTeamBoard() + progress a getPublicState()"
    - "src/server/socketHandlers.js — handler team:place-piece (emissió dirigida) + emissió team:board-state en connexió"
    - "src/client/client.js — calaix+tauler SortableJS + assemblePreview(DOMPurify) + desacoblament session:full-state (quirúrgic) vs team:board-state (rebuild)"
    - "test/placement.test.js — round-trip place → board-state a l'owner, progress N/8 a l'admin, MAI a 'session'"
  key_links:
    - "onAdd(slot) → socket.emit(EVENTS.TEAM_PLACE_PIECE,{slotId,pieceType}) → gameState.placePiece(socket.data.teamId,...) → io.to(`team:${id}`).emit(TEAM_BOARD_STATE) + io.to('admin').emit(SESSION_FULL_STATE)"
    - "team:board-state → renderBoardAndDrawer(placement) + assemblePreview(placement) → iframe.preview-frame srcdoc (via DOMPurify)"
    - "io.on('connection') branca d'equip → socket.emit(TEAM_BOARD_STATE, getTeamBoard(socket.data.teamId)) (recuperació F5)"
---

<objective>
Entrega la **llesca vertical caminant** de la Fase HTML: un equip pot arrossegar una peça bona del calaix cap al seu slot compatible, el servidor la valida i la guarda com a estat autoritatiu, i la preview de la dreta mostra a l'instant el robot real prenent forma. Aquesta és la primera capacitat end-to-end de la fase (calaix → gest SortableJS → intent → autoritat servidor → projecció dirigida → render tauler + preview).

Purpose: Materialitza GAME-03 (snap fort per type-check + imant) i GAME-06 (cap escriptura lliure) sobre la closca `renderActiveSplitScreen()` que va deixar la Fase 1, establint la plantilla canònica del robot (D-01) i el patró d'emissió dirigida que fa segur que diversos equips juguin alhora sense trepitjar-se.

Output: plantilla compartida, 3 esdeveniments nous de protocol, autoritat de col·locació al servidor amb recuperació F5, i el client que munta peces i renderitza el robot real via DOMPurify. Refinaments (treure peça, distractors, inventari, congelació, progrés N/8, art final, card d'Admin) queden per als Plans 02 i 03.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-joc-fase-html-blocs-drag-drop/02-CONTEXT.md
@.planning/phases/02-joc-fase-html-blocs-drag-drop/02-RESEARCH.md
@.planning/phases/02-joc-fase-html-blocs-drag-drop/02-PATTERNS.md
@.planning/phases/02-joc-fase-html-blocs-drag-drop/02-UI-SPEC.md
</context>

## Phase Goal

**As a** equip d'alumnes sense experiència prèvia, **I want to** muntar l'esquelet HTML del cap de robot arrossegant blocs amb snap fort (sense poder escriure codi), **so that** entenc que l'HTML és l'estructura de la pàgina i veig el robot prendre forma a la preview sense por a equivocar-me.

> Nota de derivació: la línia `**Goal:**` de ROADMAP §Phase 2 està en forma de capacitat ("Els equips poden completar la Fase HTML…") i no en format "As a / I want to / so that". La història d'usuari anterior es deriva 1:1 d'aquesta capacitat + del públic rector definit a CONTEXT.md (adults ~40 anys, zero base). Si es vol la forma canònica a ROADMAP, executar `/gsd mvp-phase 2`.

<tasks>

<task type="auto">
  <name>Task 1: Test RED — round-trip de col·locació (intent → board-state dirigit + progrés N/8, mai a 'session')</name>
  <read_first>
    - test/roundtrip.test.js (harness a copiar: startServer(0), before/after, connectAndAwait race-safe, once/wait; Test C projecció d'estat línia 100-107; Test E negatiu V4 línia 127-137)
    - 02-RESEARCH.md (§Pattern 2 estat autoritatiu + projecció dirigida; §Security V4/V5; §Pitfall 1 tempesta de re-render)
    - 02-PATTERNS.md (§test/placement.test.js — patrons a copiar del harness i noves assercions)
    - 02-CONTEXT.md (D-07 identitat pel slot, D-15 progrés N/8)
  </read_first>
  <files>test/placement.test.js</files>
  <action>
    Crea test/placement.test.js copiant el harness de roundtrip.test.js (servidor real a port efímer, connectAndAwait que registra listeners al mateix tick sincron abans de qualsevol await). Escriu els casos que han de FALLAR ara (encara no hi ha placePiece ni els events):
    - Setup: admin registra equips (admin:register-teams), un client tria un equip (team:select) i n'obté token+teamId; opcionalment l'admin inicia la fase html (admin:start-phase {phase:'html', durationMs:60000}) perquè placePiece l'accepti.
    - Test PLACE-OK: el client emet EVENTS.TEAM_PLACE_PIECE amb {slotId:'antena-esquerra', pieceType:'antena'}; el MATEIX client (owner) rep EVENTS.TEAM_BOARD_STATE amb placement['antena-esquerra']==='antena'. Importa EVENTS des de src/server/events.js per no hard-codar literals.
    - Test PLACE-TYPE-REJECT: emetre {slotId:'nas', pieceType:'antena'} (tipus incompatible) NO produeix cap board-state i placement queda sense 'nas' (type-check server-side, D-07).
    - Test ADMIN-COUNT: després d'un place OK, un socket admin rep session:full-state on l'equip owner té progress {placed:1, total:8}; assereix que token===undefined a la projecció.
    - Test NO-SESSION-BROADCAST: un SEGON equip connectat NO rep cap team:board-state ni cap session:full-state provocat per la col·locació del primer equip (espera ~150ms i comprova que no ha arribat) — garanteix emissió dirigida (Pitfall 1).
    Usa el mateix idioma d'assercions i els helpers once()/wait() del harness existent.
  </action>
  <verify>
    <automated>cd "$PWD" && npm test 2>&1 | grep -q "placement" && echo "placement suite present"</automated>
  </verify>
  <acceptance_criteria>
    - test/placement.test.js existeix i s'executa amb `node --test test/*.test.js`.
    - Abans de la Task 2 els casos PLACE-OK/ADMIN-COUNT/NO-SESSION-BROADCAST FALLEN (no existeix placePiece ni els events) — el test és genuïnament RED.
    - El test importa EVENTS des de src/server/events.js (cap literal d'event-name al test).
  </acceptance_criteria>
  <done>Existeix una suite d'integració socket.io que descriu el round-trip de col·locació i que ara falla per manca d'implementació.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Plantilla canònica + autoritat de col·locació al servidor + emissió dirigida + recuperació F5</name>
  <read_first>
    - src/server/events.js (objecte EVENTS congelat, disposició de dues seccions intent/broadcast)
    - src/server/gameState.js (patró mutation-returns-bool: claimTeam 30-36, pauseTimer 87-93; getPublicState projecció explícita 51-64; export block 134-146)
    - src/server/socketHandlers.js (safeHandler 16-25; branca de connexió d'equip 64-70; ADMIN_START_PHASE forma del handler 122-134; ADMIN_FORCE_RESYNC emissió dirigida a team:<id> 184-193)
    - 02-RESEARCH.md (§Code Examples plantilla del robot línia 346-368; §Pattern 2; §Pitfall 5 inventari)
    - 02-PATTERNS.md (§gameState.js, §socketHandlers.js — patrons exactes a copiar)
  </read_first>
  <files>src/shared/robotTemplate.js, src/server/events.js, src/server/gameState.js, src/server/socketHandlers.js, package.json</files>
  <behavior>
    - test/placement.test.js: PLACE-OK, PLACE-TYPE-REJECT, ADMIN-COUNT i NO-SESSION-BROADCAST passen a VERD.
    - placePiece rebutja (retorna false, sense broadcast) si: fase!='html', timerStatus==='frozen', slotId desconegut, pieceType != slot.accepts, slot ja ocupat, o inventari del tipus esgotat.
    - getPublicState().teams[i].progress === {placed:N, total:8} durant la fase html; null fora de la fase html; el token mai apareix.
  </behavior>
  <action>
    Crea src/shared/robotTemplate.js (directori src/shared/ nou) seguint el patró de mòdul de constants congelades de events.js: exporta SLOTS (8 slots fulla amb {id, accepts, parent, html}), PIECES (inventari del calaix: antena×2, orella×2, ull×2, nas×1, boca×1) i DISTRACTORS (['banana','roda','sabata']); usa el contingut EXACTE draftat a 02-RESEARCH.md §Code Examples (estructura D-01). Cada slot.html porta l'id/class canònic per posició (D-07) i els src d'imatge en path ARREL-relatiu (usa /antena.svg, /orella.svg per casar amb els placeholders de la Task 3; el fons NO és un slot). Aplica Object.freeze.
    A events.js afegeix TEAM_PLACE_PIECE:'team:place-piece' i TEAM_REMOVE_PIECE:'team:remove-piece' a la secció intents, i TEAM_BOARD_STATE:'team:board-state' a la secció broadcasts (declara els tres ara encara que remove s'implementi al Pla 02).
    A gameState.js: importa SLOTS/PIECES de robotTemplate.js; amplia el registre d'equip amb placement:{} (a registerTeams línia 26); afegeix placePiece(teamId, slotId, pieceType) seguint mutation-returns-bool amb TOTES les guardes de <behavior> (inclou un helper countAvailable(team, type) que compta el count de PIECES menys les ocurrències ja a placement — Pitfall 5); afegeix getTeamBoard(teamId) que retorna {placement:{...team.placement}} (còpia superficial, mai la ref viva); amplia getPublicState() perquè progress sigui {placed:Object.keys(team.placement).length, total:SLOTS.length} quan state.phase==='html' i null altrament (mantén l'estil destructure-and-map, no facis spread del team cru); exporta placePiece, getTeamBoard.
    A socketHandlers.js: registra el handler EVENTS.TEAM_PLACE_PIECE amb safeHandler seguint la forma d'ADMIN_START_PHASE però derivant teamId de socket.data.teamId (V4 — MAI del payload; si no hi ha teamId, return), validant que slotId i pieceType són strings (V5); si gameState.placePiece(...) retorna true, emet io.to(`team:${teamId}`).emit(TEAM_BOARD_STATE, getTeamBoard(teamId)) i io.to('admin').emit(SESSION_FULL_STATE, getPublicState()) — divergència crítica respecte als handlers admin: aquí NO s'emet mai a io.to('session') (Pitfall 1). A la branca d'equip de io.on('connection') (després del socket.emit(SESSION_FULL_STATE) existent, línia 69), afegeix socket.emit(TEAM_BOARD_STATE, getTeamBoard(socket.data.teamId)) perquè un F5 reconstrueixi el robot (CORE-03).
    A package.json afegeix a dependencies, fixades exactes sense ^ (estil socket.io:"4.8.3"): "sortablejs":"1.15.7" i "dompurify":"3.4.11"; executa `npm install`. Deixa constància al SUMMARY que s'omet checkpoint:human-verify per a dompurify perquè el flag SUS "too-new" és fals positiu de recència (cure53, ~45M dl/setmana, decisió de stack a CLAUDE.md; sense postinstall — 02-RESEARCH §Package Legitimacy Audit).
  </action>
  <verify>
    <automated>cd "$PWD" && npm install && npm test</automated>
  </verify>
  <acceptance_criteria>
    - `npm test` passa PLACE-OK, PLACE-TYPE-REJECT, ADMIN-COUNT, NO-SESSION-BROADCAST (i les suites existents segueixen verdes).
    - El handler team:place-piece deriva teamId de socket.data.teamId (verificable llegint el handler) i no llegeix cap teamId del payload.
    - robotTemplate.js exporta SLOTS amb exactament 8 entrades i cada slot.html conté el seu id canònic (grep: `node -e` que importi SLOTS i comprovi length===8 i que assereixi la presència de 'antena-esquerra' i '<output id="boca">').
    - package.json té sortablejs 1.15.7 i dompurify 3.4.11 fixats sense rang.
  </acceptance_criteria>
  <done>El servidor accepta intents de col·locació vàlids, els guarda com a estat autoritatiu per equip, projecta el board dirigit a l'owner i el comptador N/8 a l'admin sense soroll cap a la resta d'equips, i recupera el board a la reconnexió.</done>
</task>

<task type="auto">
  <name>Task 3: Client — calaix + tauler SortableJS + preview real via DOMPurify + desacoblament del render</name>
  <read_first>
    - src/client/client.js (renderActiveSplitScreen 142-187 = closca a omplir; .action-panel + iframe.preview-frame sandbox="allow-same-origin" srcdoc buit 166-170; màquina d'estats renderScreenForState 208-243; bootClient socket.on 253-277; patró Lucide createElement 9,178-181; textContent anti-XSS 89)
    - src/client/shared/tokens.css (:root 4-55; --phase-html línia 48 ja existeix; patró prefers-reduced-motion 97-101)
    - src/client/client.css (tokens-only, sense literals; capçalera 1-3)
    - 02-RESEARCH.md (§Pattern 1 un Sortable per slot 172-216; §Pattern 3 assemblatge preview + DOMPurify 262-291; §Desacoblament del render 371-381; §Pitfall 2 preservar id/output)
    - 02-UI-SPEC.md (§Component Inventory, §Layout Contract, §Typography code label mono, §Color empty slot/valid-hover)
  </read_first>
  <files>src/client/client.js, src/client/client.css, src/client/shared/tokens.css, src/client/public/antena.svg, src/client/public/orella.svg, src/client/public/fons.svg</files>
  <action>
    Crea el directori src/client/public/ i tres placeholders SVG simples i reconeixibles: antena.svg, orella.svg, fons.svg (Vite serveix src/client/public a l'arrel /; el fons és la capa de sistema D-03, no puntua ni és peça). Aquests placeholders desbloquegen la mecànica; l'art final es tracta al Pla 03.
    A tokens.css afegeix dins :root, seguint la disposició agrupada-amb-comentari existent: --font-family-mono (ui-monospace, SFMono-Regular, Menlo, Consolas, monospace) i els tokens de moviment --motion-snap:150ms i --motion-ease:cubic-bezier(0.2,0,0.2,1) (UI-SPEC §Motion/§Typography). NO redeclaris --phase-html.
    A client.js importa { EVENTS } des de '../server/events.js' i Sortable des de 'sortablejs' i DOMPurify des de 'dompurify' i { SLOTS, PIECES } des de '../shared/robotTemplate.js'. Afegeix estat de mòdul latestPlacement (inicialment {}).
    Munta el tauler dins renderActiveSplitScreen quan state.phase==='html': construeix dins .action-panel el calaix (.calaix, llista origen SortableJS amb les peces bones de PIECES; per ara, inventari complet — el recompte dinàmic i els distractors són del Pla 02) i el tauler de slots niats (frames .slot-frame pre-construïts per section#robot-contenidor / div#robot-cap / div.contenidor-ulls segons SLOTS[].parent, tots visibles D-08, cada slot fulla una micro-llista .slot amb dataset.slotId i dataset.accepts). Cada chip .piece-chip porta dataset.type genèric (D-07) i una etiqueta tag+class en text mono sense angle brackets (textContent, read-only, D-12); antena/orella mostren el thumbnail SVG, ull/nas/boca un glyph Lucide. Inicialitza SortableJS seguint 02-RESEARCH §Pattern 1: calaix (group robot, sort:false, animation:150, emptyInsertThreshold:40) i un Sortable per slot amb group.put com a FUNCIÓ (accepta només si el slot és buit i dragEl.dataset.type===slot.dataset.accepts) i emptyInsertThreshold:40 (imant D-09). En onAdd d'un slot emet socket.emit(EVENTS.TEAM_PLACE_PIECE,{slotId, pieceType}).
    Afegeix a bootClient() el handler socket.on(EVENTS.TEAM_BOARD_STATE, ({placement}) => { latestPlacement = placement; si latestState?.phase==='html' → renderBoardAndDrawer(placement) + assemblePreview(placement) }).
    Implementa assemblePreview(placement) (02-RESEARCH §Pattern 3): assembla l'string del robot des de SLOTS agrupats per parent, incloent NOMÉS els slots presents a placement, embolcallat en el scaffold fix (section > div#robot-cap > div.contenidor-ulls) i la capa de fons fix (D-03); passa'l per DOMPurify.sanitize amb ALLOWED_ATTR incloent 'src','alt','class','id' i ADD_TAGS:['output'] (Pitfall 2 — id/class/<output> HAN de sobreviure per al scoring de Fase 4); posa el resultat a iframe.preview-frame srcdoc. Mai assemblis des de text d'usuari (GAME-06). Manté l'iframe sandbox="allow-same-origin" sense allow-scripts.
    DESACOBLAMENT del render (Pitfall 1/Anti-Patterns): la primera entrada a la fase html (canvi de fase → interstici → renderActiveSplitScreen) munta la closca + tauler UN cop; durant la fase html un session:full-state posterior NO ha de fer teardown del tauler (destruiria les instàncies SortableJS) — fes una actualització QUIRÚRGICA només del timer (renderCountdown) sense reconstruir el tauler; el (re)build del tauler+preview el fa NOMÉS el handler team:board-state. Ajusta renderScreenForState perquè, estant ja en split html actiu i sense canvi de fase, faci l'update quirúrgic en lloc de renderActiveSplitScreen complet.
  </action>
  <verify>
    <automated>set -o pipefail; cd "$PWD" && npm run build 2>&1 | tail -5 && npm test</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` completa sense errors (SortableJS/DOMPurify/robotTemplate resolts per Vite).
    - client.js importa EVENTS des del mòdul compartit per als nous events (no literals per a place-piece/board-state).
    - assemblePreview passa l'HTML per DOMPurify.sanitize amb ADD_TAGS:['output'] i ALLOWED_ATTR que inclou 'id' i 'class' (llegible al codi).
    - Els placeholders src/client/public/{antena,orella,fons}.svg existeixen.
    - `npm test` segueix verd (les suites de servidor no es trenquen).
  </acceptance_criteria>
  <done>Un equip en fase html veu el calaix i el tauler de slots; en arrossegar una peça bona a un slot compatible s'emet l'intent, i quan arriba el board-state autoritatiu el tauler i la preview real es re-renderitzen sense destruir les instàncies SortableJS d'altres actualitzacions.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| team browser → Socket.io server | Intents de col·locació (team:place-piece) creuen aquí; mai confiar en el payload per a identitat |
| assembled HTML → iframe srcdoc | HTML assemblat al client s'injecta al srcdoc de la preview |
| npm registry → build | sortablejs/dompurify entren a la cadena de build |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-01 | Tampering/Elevation | assemblePreview → iframe srcdoc | medium | mitigate | Contingut 100% des de SLOTS[].html (cap text d'usuari, GAME-06) + DOMPurify.sanitize + iframe sandbox="allow-same-origin" SENSE allow-scripts (cap script s'executaria) |
| T-02-02 | Spoofing/Tampering | team:place-piece handler | medium | mitigate | teamId derivat de socket.data.teamId (middleware d'identitat de Fase 1), MAI del payload — un equip no pot mutar el board d'un altre (V4) |
| T-02-03 | DoS | placePiece / broadcast | low | mitigate | safeHandler embolcalla el handler; validació estricta d'enums (SLOTS/PIECES) abans de mutar; emissió dirigida (mai 'session') limita el radi; no-op (slot ocupat) no re-broadcast |
| T-02-SC | Tampering | npm install sortablejs/dompurify | low | accept | 02-RESEARCH §Package Legitimacy: sortablejs OK, dompurify SUS="too-new" = fals positiu (cure53, ~45M dl/setmana, sense postinstall, decisió de stack a CLAUDE.md). Versions fixades exactes. Checkpoint human-verify omès amb raonament documentat |
</threat_model>

<verification>
- `npm test` verd: suites existents (roundtrip/timer/monitoring) + placement (PLACE-OK, PLACE-TYPE-REJECT, ADMIN-COUNT, NO-SESSION-BROADCAST).
- `npm run build` completa sense errors amb els nous imports.
- El handler de col·locació no emet mai a io.to('session') (verificat per Test NO-SESSION-BROADCAST).
- getPublicState() no exposa mai el token ni la ref viva de placement.
</verification>

<success_criteria>
- Un equip en fase html pot arrossegar una peça bona a un slot compatible i el servidor la guarda; el board-state dirigit reconstrueix tauler + preview real (robot renderitzat via DOMPurify amb ids/classes intactes).
- Diversos equips poden col·locar peces simultàniament sense re-render creuat (emissió dirigida).
- Un F5 recupera el robot mig muntat.
- Cap camí d'escriptura lliure de codi (chips read-only, srcdoc només des de plantilla).
</success_criteria>

<output>
Create `.planning/phases/02-joc-fase-html-blocs-drag-drop/02-01-SUMMARY.md` when done
</output>
