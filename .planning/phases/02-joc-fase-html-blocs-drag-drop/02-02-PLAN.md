---
phase: 02-joc-fase-html-blocs-drag-drop
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - src/server/gameState.js
  - src/server/socketHandlers.js
  - src/client/client.js
  - src/client/client.css
  - test/placement.test.js
autonomous: true
requirements: [GAME-03, GAME-06]
user_setup: []

must_haves:
  truths:
    - "Un equip pot treure una peça ja col·locada (arrossegar-la fora del slot cap al calaix); el servidor la retira de team.placement i el comptador N/8 baixa (D-10) — refer no té fricció ni diàleg de confirmació"
    - "El calaix reflecteix l'inventari restant: col·locar la primera de 2 antenes deixa 1 antena disponible; no es poden col·locar 3 antenes ni omplir un slot amb un tipus esgotat (Pitfall 5)"
    - "Els distractors (banana/roda/sabata) no tenen cap slot que els accepti → SortableJS els reverteix (rebot) sempre al calaix, sense mecànica ni missatge d'error nous (D-11)"
    - "A timerStatus==='frozen' totes les instàncies SortableJS es desactiven (option('disabled',true)) i el robot parcial queda tal com està per viatjar a la Fase 3 (D-11/GAME-07/Pitfall 4)"
    - "Un equip no pot mutar el board d'un altre forjant teamId al payload (V4) — verificat per test negatiu"
  artifacts:
    - "src/server/gameState.js — removePiece() (mutation-returns-bool, no-op si slot buit)"
    - "src/server/socketHandlers.js — handler team:remove-piece (emissió dirigida idèntica a place)"
    - "src/client/client.js — interacció de treure peça + chips distractors + desactivació SortableJS en frozen + calaix conscient de l'inventari"
    - "src/client/client.css — .piece-chip--distractor + shake + estat disabled dels slots"
    - "test/placement.test.js — REMOVE round-trip, INVENTORY cap, V4 forge-teamId negatiu, F5 board recovery"
  key_links:
    - "onAdd(calaix) quan evt.from és un slot → socket.emit(EVENTS.TEAM_REMOVE_PIECE,{slotId}) → gameState.removePiece → io.to(`team:${id}`).emit(TEAM_BOARD_STATE) + io.to('admin').emit(SESSION_FULL_STATE)"
    - "session:full-state amb timerStatus==='frozen' → per cada instància SortableJS registrada: sortable.option('disabled', true) (update quirúrgic, sense teardown del tauler)"
    - "DISTRACTORS de robotTemplate.js → chips amb dataset.type que cap slot.accepts iguala → revert natiu"
---

<objective>
Completa la mecànica del trencaclosques HTML sobre la llesca caminant del Pla 01: fes que treure peces sigui trivial i sense càstig (D-10), que el calaix respecti l'inventari real (2 antenes, 2 orelles, 2 ulls, 1 nas, 1 boca — Pitfall 5), que els distractors còmics rebotin sempre per omissió (D-11), i que la congelació a timer zero aturi de debò el drag (D-11/Pitfall 4, no només l'overlay visual de Fase 1).

Purpose: Aquestes són les refinacions que fan la fase "sense frustració" per al públic rector (adults ~40 anys sense base): poder desfer, no poder-se equivocar d'estructura, i una petita decisió lúdica ("això no hi va") amb gràcia. Tanquen GAME-03 (el rebot com a segona meitat del snap fort) i mantenen GAME-06.

Output: removePiece autoritatiu + handler dirigit, interacció de treure al client, chips distractors, desactivació de SortableJS en frozen, calaix conscient de l'inventari, i extensions del test (remove, inventari, V4 negatiu, recuperació F5).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-joc-fase-html-blocs-drag-drop/02-CONTEXT.md
@.planning/phases/02-joc-fase-html-blocs-drag-drop/02-RESEARCH.md
@.planning/phases/02-joc-fase-html-blocs-drag-drop/02-PATTERNS.md
@.planning/phases/02-joc-fase-html-blocs-drag-drop/02-UI-SPEC.md
@.planning/phases/02-joc-fase-html-blocs-drag-drop/02-01-PLAN.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Servidor — removePiece autoritatiu + handler dirigit + extensió del test</name>
  <read_first>
    - src/server/gameState.js (placePiece i countAvailable del Pla 01; patró mutation-returns-bool; getTeamBoard; export block)
    - src/server/socketHandlers.js (handler team:place-piece del Pla 01 com a plantilla exacta; emissió dirigida team:<id> + admin, mai session)
    - test/placement.test.js (casos del Pla 01; harness connectAndAwait; idioma d'assercions)
    - 02-RESEARCH.md (§Pattern 2 removePiece mirror; §Pitfall 5 inventari; §Security V4)
    - 02-CONTEXT.md (D-10 treure peça, D-11 congelat)
  </read_first>
  <files>src/server/gameState.js, src/server/socketHandlers.js, test/placement.test.js</files>
  <behavior>
    - REMOVE round-trip: amb 'antena-esquerra' ocupada, l'owner emet TEAM_REMOVE_PIECE {slotId:'antena-esquerra'} → rep TEAM_BOARD_STATE sense 'antena-esquerra'; progress a l'admin baixa a {placed:0,total:8}.
    - REMOVE no-op: treure un slot buit NO emet cap board-state (retorna false).
    - INVENTORY cap: col·locar 2 antenes ('antena-esquerra','antena-dreta') OK; un tercer intent d'antena en un slot d'antena (ja tots dos ocupats) o amb inventari esgotat es rebutja.
    - V4 forge: un socket d'equip B que emet TEAM_PLACE_PIECE/TEAM_REMOVE_PIECE amb un teamId de l'equip A al payload NO muta el board de A (el servidor usa socket.data.teamId).
    - F5 recovery: un socket que reconnecta amb el token de l'equip rep TEAM_BOARD_STATE amb el placement previ en connectar.
  </behavior>
  <action>
    A gameState.js afegeix removePiece(teamId, slotId) reflectint placePiece amb el patró mutation-returns-bool: retorna false (no-op, sense broadcast) si l'equip no existeix, si state.phase!='html' o timerStatus==='frozen', o si team.placement[slotId] no existeix; altrament elimina la clau (delete team.placement[slotId]) i retorna true. Afegeix removePiece a l'export block.
    A socketHandlers.js registra el handler EVENTS.TEAM_REMOVE_PIECE amb safeHandler, còpia EXACTA de la forma de team:place-piece del Pla 01: teamId de socket.data.teamId (V4, mai payload), valida slotId string (V5), i si gameState.removePiece(...) retorna true emet io.to(`team:${teamId}`).emit(TEAM_BOARD_STATE, getTeamBoard(teamId)) + io.to('admin').emit(SESSION_FULL_STATE, getPublicState()) — mai io.to('session').
    A test/placement.test.js afegeix els casos de <behavior>: REMOVE round-trip, REMOVE no-op, INVENTORY cap, V4 forge (còpia l'idioma del Test E negatiu de roundtrip.test.js), i F5 recovery (reconnecta amb el token i espera TEAM_BOARD_STATE amb el placement). Reutilitza EVENTS importat i els helpers del harness.
  </action>
  <verify>
    <automated>cd "$PWD" && npm test</automated>
  </verify>
  <acceptance_criteria>
    - `npm test` passa REMOVE round-trip, REMOVE no-op, INVENTORY cap, V4 forge i F5 recovery (a més de tot el que ja passava).
    - removePiece retorna false en slot buit (no-op → cap broadcast) — verificat pel cas REMOVE no-op.
    - El handler team:remove-piece deriva teamId de socket.data.teamId (llegible al codi) i no emet a io.to('session').
  </acceptance_criteria>
  <done>El servidor permet retirar peces col·locades com a estat autoritatiu, respecta l'inventari i la identitat per socket, i cap equip pot mutar el board d'un altre.</done>
</task>

<task type="auto">
  <name>Task 2: Client — treure peça + distractors + congelació desactiva SortableJS + calaix conscient de l'inventari</name>
  <read_first>
    - src/client/client.js (renderBoardAndDrawer + assemblePreview + inicialització SortableJS del Pla 01; handler team:board-state; update quirúrgic de session:full-state; patró Lucide createElement; frozen-overlay 175-183)
    - src/client/client.css (classes .calaix/.slot/.slot-frame/.piece-chip del Pla 01; capçalera tokens-only 1-3)
    - src/client/shared/tokens.css (--phase-html, --motion-snap, --motion-ease; patró prefers-reduced-motion 97-101)
    - src/shared/robotTemplate.js (PIECES per a l'inventari, DISTRACTORS)
    - 02-RESEARCH.md (§Pattern 1 onAdd al calaix per treure; §Pitfall 4 desactivar SortableJS en frozen; §Pitfall 5 inventari derivat del placement)
    - 02-UI-SPEC.md (§Motion distractor shake + rebound; §Component Inventory Distractor chip sense code label; §Color distractor "no color, motion only")
  </read_first>
  <files>src/client/client.js, src/client/client.css</files>
  <action>
    Treure peça (D-10): al Sortable del calaix afegeix onAdd que, quan evt.from sigui un slot (evt.from.dataset.slotId present), emeti socket.emit(EVENTS.TEAM_REMOVE_PIECE,{slotId:evt.from.dataset.slotId}); el board-state autoritatiu reconciliarà. El calaix ha de tenir group.put:true perquè accepti el retorn de peces. No afegeixis cap diàleg de confirmació (UI-SPEC §Copywriting: undo no-frustració).
    Distractors (D-11): en construir el calaix, afegeix els chips de DISTRACTORS barrejats amb les peces bones, amb glyph emoji (🍌 🛞 👟) i SENSE code label (són deliberadament "no HTML"), classe .piece-chip--distractor i dataset.type que cap slot.accepts iguala → revert natiu de SortableJS els retorna sols (cap codi especial). Afegeix un shake breu a la tornada (onEnd si no ha estat acceptat per cap slot) coherent amb UI-SPEC §Motion (translateX ±6px, ~300ms), sense text ni color d'error.
    Congelació (D-11/Pitfall 4): mantén un registre de totes les instàncies SortableJS creades (array a l'àmbit del render). Dins l'update quirúrgic del handler session:full-state, quan state.timerStatus==='frozen' crida sortable.option('disabled', true) a cada instància (l'overlay de Fase 1 és només visual); quan torna a 'running' (p.ex. +1min) torna-les a habilitar. Fes-ho sense teardown del tauler.
    Calaix conscient de l'inventari (Pitfall 5): deriva les peces disponibles al calaix de PIECES menys les ocurrències presents a latestPlacement (a renderBoardAndDrawer), de manera que col·locar la primera de 2 antenes deixi 1 antena i col·locar-les totes dues buidi el tipus. El servidor ja és la font de veritat (rebutja excessos); això manté el calaix coherent visualment.
    A client.css afegeix .piece-chip--distractor i el keyframe de shake dins @media (prefers-reduced-motion: no-preference) (copia el patró de tokens.css:97-101), i un estil per a l'estat disabled dels slots/chips quan frozen (només visual, tokens-only). Cap literal hex/px: usa var(--*).
  </action>
  <verify>
    <automated>cd "$PWD" && npm run build 2>&1 | tail -5 && npm test</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` completa sense errors.
    - El calaix emet TEAM_REMOVE_PIECE quan una peça torna des d'un slot (llegible: onAdd del calaix comprova evt.from.dataset.slotId).
    - Els chips distractors usen dataset.type que no apareix a cap slot.accepts (grep conceptual: els valors de DISTRACTORS no són cap accepts de SLOTS).
    - El shake i qualsevol animació estan dins @media (prefers-reduced-motion: no-preference).
    - client.css nou no conté literals de color/mida (només var(--*)).
  </acceptance_criteria>
  <done>Un equip pot desfer col·locacions sense fricció, els distractors reboten amb gràcia i sense error, la congelació atura de debò el drag, i el calaix mostra sempre l'inventari restant correcte.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| team browser → Socket.io server | Intents team:remove-piece creuen aquí; identitat mai des del payload |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-04 | Spoofing/Tampering | team:remove-piece handler | medium | mitigate | teamId de socket.data.teamId (V4), mai del payload — verificat pel test V4 forge. Mateix patró que place-piece |
| T-02-05 | Tampering (bypass congelació) | drag actiu amb overlay frozen | medium | mitigate | sortable.option('disabled',true) a cada instància quan timerStatus==='frozen' (Pitfall 4) — l'overlay és només pointer-events:none i no bastava per si sol |
| T-02-06 | DoS | remove spam / no-op | low | mitigate | safeHandler + mutation-returns-bool (no-op en slot buit no re-broadcast) + emissió dirigida |
</threat_model>

<verification>
- `npm test` verd amb REMOVE, INVENTORY, V4 forge i F5 recovery afegits.
- `npm run build` sense errors.
- Cap emissió de col·locació/retirada a io.to('session').
- Animacions embolcallades en prefers-reduced-motion; CSS tokens-only.
</verification>

<success_criteria>
- Treure una peça baixa N/8 sense confirmació.
- Inventari respectat (mai 3 antenes; slots omplen fins a exhaurir el tipus).
- Distractors reboten sempre amb shake, sense error.
- Frozen desactiva el drag; el robot parcial persisteix per a la Fase 3.
</success_criteria>

<output>
Create `.planning/phases/02-joc-fase-html-blocs-drag-drop/02-02-SUMMARY.md` when done
</output>
