---
phase: 04.1-millores-operatives-d-admin-tornar-fase-anterior-reset-servi
plan: 03
type: execute
wave: 2
depends_on: ["04.1-01", "04.1-02"]
files_modified:
  - src/server/events.js
  - src/server/socketHandlers.js
  - src/server/index.js
  - src/client/admin.js
autonomous: false
requirements: [D-04, D-05, D-06]

must_haves:
  truths:
    - "L'admin veu un botó «Reset» al panell de control que demana confirmació («Segur que vols reiniciar el servidor?») abans d'actuar (D-04)."
    - "En confirmar, el procés Node surt amb exit code 0 i PM2 el revive net → tot l'estat de joc (equips, tokens, fase) es reinicia (D-04)."
    - "Els clients es reconnecten automàticament al servidor net i tornen a la pantalla de selecció d'equip."
    - "Un client no-admin no pot disparar el reset (re-validació de room server-side) — no és un vector de DoS."
    - "El mateix mecanisme funciona en dev i prod sota PM2 (D-05/D-06), sense branca per NODE_ENV."
  artifacts:
    - "src/server/events.js conté EVENTS.ADMIN_RESET_SERVER i EVENTS.SERVER_RESTARTING"
    - "src/server/socketHandlers.js conté el handler d'EVENTS.ADMIN_RESET_SERVER"
    - "src/client/admin.js conté el botó «Reset»"
  key_links:
    - "botó «Reset» → showConfirm() (de Plan 01) → socket.emit(EVENTS.ADMIN_RESET_SERVER) → handler valida room 'admin' → io.to('session').emit(SERVER_RESTARTING) → setTimeout(io.close()+process.exit(0)) → PM2 autorestart"
---

<objective>
Afegir el botó «Reset» a l'Admin i el handler server-side que fa un restart REAL del procés (process.exit(0) + PM2 revive = wipe total de l'estat en memòria). Cobreix D-04 (restart real amb confirmació), D-05 (dev+prod) i D-06 (mecanisme PM2 únic). Depèn de Plan 02 (PM2 instal·lat i verificat) per poder comprovar que el revive funciona, i de Plan 01 (comparteix events.js/socketHandlers.js/admin.js i reutilitza showConfirm()).

Purpose: Donar al professor una eina d'emergència per reiniciar tota la partida durant una classe real sense accés a la terminal del VPS.
Output: Events ADMIN_RESET_SERVER + SERVER_RESTARTING, handler admin-only, botó «Reset» a l'Admin.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04.1-millores-operatives-d-admin-tornar-fase-anterior-reset-servi/04.1-CONTEXT.md
@.planning/phases/04.1-millores-operatives-d-admin-tornar-fase-anterior-reset-servi/04.1-RESEARCH.md
@.planning/phases/04.1-millores-operatives-d-admin-tornar-fase-anterior-reset-servi/04.1-PATTERNS.md
@.planning/phases/04.1-millores-operatives-d-admin-tornar-fase-anterior-reset-servi/04.1-01-SUMMARY.md
@.planning/phases/04.1-millores-operatives-d-admin-tornar-fase-anterior-reset-servi/04.1-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Events ADMIN_RESET_SERVER + SERVER_RESTARTING + handler admin-only</name>
  <files>src/server/events.js, src/server/socketHandlers.js, src/server/index.js</files>
  <read_first>
    - src/server/events.js (objecte EVENTS; afegeix les noves constants amb el mateix estil kebab-case i comentaris de direcció)
    - src/server/socketHandlers.js (handler ADMIN_FINALIZE_GAME línia 256-263 com a mirall de forma admin-only sense payload; safeHandler línia 16-25; `io` a l'scope de registerSocketHandlers línia 38)
    - src/server/index.js (startServer línia 9; teardown de l'interval `httpServer.once('close', ...)` línia ~35 — verifica que io.close() no deixa intervals penjats, NO editis preventivament)
    - .planning/phases/04.1-millores-operatives-d-admin-tornar-fase-anterior-reset-servi/04.1-PATTERNS.md (secció socketHandlers.js — reset-server handler; secció index.js)
    - .planning/phases/04.1-millores-operatives-d-admin-tornar-fase-anterior-reset-servi/04.1-RESEARCH.md (Pattern 2 + Anti-Patterns: NO emetre session:full-state per al reset)
  </read_first>
  <action>
    A src/server/events.js afegeix a EVENTS: `ADMIN_RESET_SERVER: 'admin:reset-server'` (client→server, admin-only) i `SERVER_RESTARTING: 'server:restarting'` (server→client, avís amable abans del restart). A src/server/socketHandlers.js registra el handler d'EVENTS.ADMIN_RESET_SERVER seguint la forma d'ADMIN_FINALIZE_GAME (admin-only, sense payload), envoltat de `safeHandler`. Dins: primer `if (!socket.rooms.has('admin')) return;` (V4 — impedeix que un no-admin mati el procés, DoS), després `io.to('session').emit(EVENTS.SERVER_RESTARTING);` (avís amable), i finalment un `setTimeout(() => { io.close(); process.exit(0); }, 150)` — el retard deixa sortir el broadcast, io.close() desconnecta net perquè els clients auto-reconnectin, i process.exit(0) mata el procés perquè PM2 el reviu net (D-04). NO emetis SESSION_FULL_STATE per al reset (és un cicle de vida de procés, no un canvi d'estat de joc). A src/server/index.js: VERIFICA (no editis a menys que calgui) que io.close() + process.exit no deixen l'`expiryInterval` penjat — el `httpServer.once('close', () => clearInterval(...))` existent ja hauria de cobrir-ho; si en la verificació observes un interval que sobreviu, afegeix el clearInterval mínim necessari i documenta-ho.
  </action>
  <verify>
    <automated>node -e "const {EVENTS}=await import('./src/server/events.js'); if(EVENTS.ADMIN_RESET_SERVER!=='admin:reset-server'||EVENTS.SERVER_RESTARTING!=='server:restarting'){process.exit(1)}" --input-type=module && node --check src/server/socketHandlers.js && node --check src/server/index.js</automated>
  </verify>
  <acceptance_criteria>
    - Source: EVENTS conté ADMIN_RESET_SERVER='admin:reset-server' i SERVER_RESTARTING='server:restarting'.
    - Source: el handler d'ADMIN_RESET_SERVER té `socket.rooms.has('admin')` com a primera comprovació i crida process.exit(0) dins un setTimeout després d'io.close().
    - Source: el handler NO emet SESSION_FULL_STATE.
    - Source: socketHandlers.js i index.js passen `node --check` (sintaxi vàlida).
  </acceptance_criteria>
  <done>Els events de reset existeixen, el handler admin-only mata el procés de forma controlada, i cap interval queda penjat.</done>
</task>

<task type="auto">
  <name>Task 2: Botó «Reset» al panell Admin</name>
  <files>src/client/admin.js</files>
  <read_first>
    - src/client/admin.js (buildControlBar línia 404+ — on inserir el botó; showConfirm() creat a Plan 01, reutilitzar-lo; import d'EVENTS)
    - src/server/events.js (EVENTS.ADMIN_RESET_SERVER — usar la constant, no el literal)
    - .planning/phases/04.1-millores-operatives-d-admin-tornar-fase-anterior-reset-servi/04.1-01-SUMMARY.md (signatura exacta de showConfirm(message, confirmLabel, destructive))
    - .planning/phases/04.1-millores-operatives-d-admin-tornar-fase-anterior-reset-servi/04.1-PATTERNS.md (secció admin.js — botó Reset)
  </read_first>
  <action>
    A src/client/admin.js, dins buildControlBar, afegeix un botó «Reset» als controls secundaris (p.ex. al costat del botó Pausar/Reprendre). El botó: `type='button'`, `className='btn btn-destructive'` (acció destructiva), `textContent='Reset'`. Ha d'aparèixer sempre (el reset és una eina d'emergència disponible en qualsevol moment, D-05) — no el limitis per fase. Al click (async): `const ok = await showConfirm('Segur que vols reiniciar el servidor?', 'Sí, reinicia', true)` (D-04, reutilitzant la funció showConfirm de Plan 01 amb destructive=true) i, si `ok`, `socket.emit(EVENTS.ADMIN_RESET_SERVER)` (constant, sense payload). NO afegeixis cap segona confirmació ni cap gestió de reconnexió manual — el client socket.io ja auto-reconnecta.
  </action>
  <verify>
    <automated>node -e "import('./src/client/admin.js').then(()=>{}).catch(e=>{ if(!/document|window|import\.meta|navigator/.test(String(e))) { console.error(e); process.exit(1);} })"</automated>
    <human-check>Amb PM2 supervisant el servidor (`npm run server:pm2`), obre l'Admin, prem «Reset», confirma, i verifica: (1) l'avís SERVER_RESTARTING/desconnexió a les pantalles d'equip, (2) `pm2 logs` mostra el procés revivint, (3) després de la reconnexió no queda cap equip registrat (wipe total, comportament correcte de D-04).</human-check>
  </verify>
  <acceptance_criteria>
    - Source: existeix un botó amb textContent 'Reset' i className que inclou 'btn-destructive' dins buildControlBar.
    - Source: el click crida showConfirm(...) amb destructive=true i, si es confirma, emet EVENTS.ADMIN_RESET_SERVER (constant, no literal 'admin:reset-server').
    - Behavior (human-check): confirmar el Reset fa que PM2 reviu el procés i la partida arrenca de zero; cancel·lar no fa res.
  </acceptance_criteria>
  <done>El botó «Reset» demana confirmació (D-04) i dispara el restart real supervisat per PM2; verificat end-to-end amb PM2.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client (browser) → Socket.io server | Un event ADMIN_RESET_SERVER pot matar el procés compartit; el rol admin s'ha de re-validar server-side (fail-closed). |
| procés Node → PM2 (supervisor) | El procés senyala «surt ara» via process.exit(0); PM2 el revive. Responsabilitat de cicle de vida delegada a l'infra. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04.1-03 | Denial of Service/Elevation | handler ADMIN_RESET_SERVER | critical | mitigate | Primera línia `if (!socket.rooms.has('admin')) return;` (fail-closed). Un client no-admin no pot matar el procés compartit. |
| T-04.1-04 | Denial of Service | espameig de Reset → crash-loop | medium | mitigate | Guard de PM2 (`min_uptime: '5s'` / `max_restarts: 30`, de Plan 02) + acció admin-only. Recuperable amb `pm2 restart codearena`. |
| T-04.1-05 | Elevation | ADMIN_SECRET absent després del restart PM2 | medium | transfer | En prod, injectar ADMIN_SECRET via `env` de l'ecosystem/secret manager (documentat per a Fase 5 desplegament); el `console.warn` existent el fa visible si falta. En dev no aplica. |
</threat_model>

<verification>
- EVENTS de reset presents; handler admin-only amb exit controlat; sintaxi vàlida (`node --check`).
- Botó «Reset» emet ADMIN_RESET_SERVER després de confirmació destructiva.
- Verificació end-to-end amb PM2: Reset → revive → wipe total (human-check).
- La suite existent no es trenca: `npm test`.
</verification>

<success_criteria>
- Reset real del procés via process.exit(0) + PM2 (D-04/D-06), admin-only (fail-closed).
- Botó «Reset» amb confirmació (D-04), reutilitzant showConfirm() de Plan 01.
- Disponible en dev i prod sense branca per NODE_ENV (D-05).
</success_criteria>

## Artifacts this phase produces (Plan 03)

| Symbol | Type | File |
|--------|------|------|
| `EVENTS.ADMIN_RESET_SERVER` (`'admin:reset-server'`) | event constant | src/server/events.js |
| `EVENTS.SERVER_RESTARTING` (`'server:restarting'`) | event constant | src/server/events.js |
| handler for `EVENTS.ADMIN_RESET_SERVER` | socket handler | src/server/socketHandlers.js |
| «Reset» button (in `buildControlBar`) | DOM control | src/client/admin.js |

<output>
Create `.planning/phases/04.1-millores-operatives-d-admin-tornar-fase-anterior-reset-servi/04.1-03-SUMMARY.md` when done
</output>
