---
phase: 04.1-millores-operatives-d-admin-tornar-fase-anterior-reset-servi
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - src/client/client.js
  - src/client/client.css
autonomous: true
requirements: [D-07, D-08, D-09, D-10, D-11]

must_haves:
  truths:
    - "Durant la Fase CSS, en interactuar amb un control de forat apareix un requadre a la preview amb el bloc CSS sencer del grup actiu (D-08)."
    - "El requadre mostra NOMÉS el grup del forat que s'està tocant en aquell moment (D-07), no un històric."
    - "El valor del forat que s'està canviant s'actualitza en directe dins el bloc mentre es mou el control (D-08)."
    - "Per sobre del bloc de codi es mostra l'etiqueta HTML de l'element (p.ex. <div id=\"robot-cap\">), derivada de pieceLabel()/containerLabel() tal com són avui (D-09/D-11)."
    - "El requadre apareix/desapareix amb un fade curt (~180ms), no instantani (D-10), i mai tapa ni bloqueja el robot (pointer-events:none)."
  artifacts:
    - "src/client/client.js conté buildOverlayBlock(), el mapa grup→etiqueta HTML, i showOverlay/updateLiveOverlay/hideOverlay"
    - "src/client/client.css conté .css-live-overlay i .css-live-overlay--visible"
  key_links:
    - "renderForatRow input focus → showOverlay(hole.group); input 'input' → updateLiveOverlay(hole.group, holeId, value); input blur → hideOverlay()"
    - "buildOverlayBlock filtra CSS_HOLES per hole.group + latestCssValues → text monospace via textContent"
    - "GROUP_ELEMENT_LABEL[group] → pieceLabel()/containerLabel() (robotTemplate.js) → etiqueta HTML literal (D-09/D-11)"
---

<objective>
Afegir un overlay de codi CSS en directe a la preview de l'equip durant la Fase CSS: en tocar un forat, es mostra el bloc CSS sencer del grup d'aquell forat (D-08) amb el valor actualitzant-se en viu, precedit de l'etiqueta HTML de l'element (D-09), lligat a la interacció (D-07) i amb fade suau (D-10). Tot és client-side i es deriva de dades ja existents (CSS_HOLES, latestCssValues, pieceLabel/containerLabel).

Purpose: Reforçar la connexió element↔selector↔codi perquè alumnes sense experiència vegin exactament quin CSS estan modificant.
Output: Funcions d'overlay + mapa d'etiquetes a client.js, estils .css-live-overlay a client.css.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04.1-millores-operatives-d-admin-tornar-fase-anterior-reset-servi/04.1-CONTEXT.md
@.planning/phases/04.1-millores-operatives-d-admin-tornar-fase-anterior-reset-servi/04.1-PATTERNS.md
@src/shared/robotTemplate.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Builder del bloc CSS + mapa d'etiquetes HTML + estils overlay</name>
  <files>src/client/client.js, src/client/client.css</files>
  <read_first>
    - src/client/client.js (import de CSS_HOLES línia 18; latestCssValues línia 299; renderCssPanel byGroup línia 1074-1078; CSS_GROUP_ORDER línia 998; updateFrozenOverlay línia 1415-1432 i la query de `.active-split`)
    - src/shared/robotTemplate.js (pieceLabel línia 104-130 — retorna l'etiqueta literal, inclou el `src` fals per a img/orella; containerLabel línia 134-137; SLOTS accepts: 'antena-esquerra','orella-esquerra','ull','nas','boca'; CONTAINERS names: 'robot-cap','contenidor-ulls'; CSS_HOLES línia 238 amb camps .group/.prop/.default)
    - src/client/client.css (bloc .frozen-overlay línia 70-88 dins `.active-split` que és position:relative i grid 1fr 1fr; tokens --font-family-mono, --font-size-label, --space-lg, --color-surface, --color-border)
    - .planning/phases/04.1-millores-operatives-d-admin-tornar-fase-anterior-reset-servi/04.1-PATTERNS.md (secció client.js CSS overlay + secció client.css)
  </read_first>
  <action>
    A src/client/client.js afegeix (a prop del panell CSS, després de renderCssPanel) tres peces. (1) El mapa `GROUP_ELEMENT_LABEL` que associa cada grup de CSS_GROUP_ORDER al seu helper i clau: '.antena'→pieceLabel('antena-esquerra'), '.orella'→pieceLabel('orella-esquerra'), '.contenidor-ulls'→containerLabel('contenidor-ulls'), '.ull'→pieceLabel('ull'), '#robot-cap'→containerLabel('robot-cap'), '#nas'→pieceLabel('nas'), '#boca'→pieceLabel('boca'). Reutilitza pieceLabel/containerLabel TAL COM SÓN (D-11) — no facis cap variant «neta»: per a '.orella' l'etiqueta inclou el `src` fals que pieceLabel afegeix avui a les peces img. Importa pieceLabel/containerLabel des de '../shared/robotTemplate.js'. (2) La funció pura `buildOverlayBlock(group, liveValues)`: filtra Object.entries(CSS_HOLES) per `h.group === group`, construeix una línia `  ${h.prop}: ${liveValues[id] ?? h.default};` per forat, i retorna `${group} {\n${lines.join('\n')}\n}` (bloc sencer del grup, D-08). El bloc és TEXT pla, mai markup. (3) La funció `getLiveValues(overrideId, overrideValue)` que retorna una còpia de latestCssValues amb `[overrideId]: overrideValue` aplicat quan es passa un override (valor viu del forat que s'està movent). A src/client/client.css afegeix `.css-live-overlay` mirallant `.frozen-overlay`: position:absolute ancorat a baix de la columna dreta (preview) — usa `bottom: var(--space-lg)`, `right: var(--space-lg)`, `left: calc(50% + var(--space-lg))`; fons `var(--color-surface)`, vora `1px solid var(--color-border)`, `border-radius: 8px`, `padding: var(--space-md)`, `font-family: var(--font-family-mono)`, `font-size: var(--font-size-label)`, `white-space: pre` (respecta els \n), `pointer-events: none` (mai bloqueja el robot), `opacity: 0`, `transition: opacity 180ms ease` (D-10); i `.css-live-overlay--visible { opacity: 1; }`. Usa només tokens, cap literal de color/mida.
  </action>
  <verify>
    <automated>node --check src/client/client.js && node -e "import('./src/shared/robotTemplate.js').then(m=>{const p=m.pieceLabel('ull'), c=m.containerLabel('robot-cap'); if(!p.startsWith('<')||!c.includes('robot-cap')){process.exit(1)}})"</automated>
  </verify>
  <acceptance_criteria>
    - Source: GROUP_ELEMENT_LABEL cobreix EXACTAMENT els 7 grups de CSS_GROUP_ORDER, cadascun mapat a pieceLabel o containerLabel amb la clau indicada.
    - Source: buildOverlayBlock construeix el bloc via concatenació de text i el render final s'assigna amb textContent (mai s'interpreta com a markup); el bloc conté totes les propietats del grup (D-08).
    - Source: client.css conté `.css-live-overlay` amb pointer-events:none, opacity:0, transition opacity 180ms, i `.css-live-overlay--visible` amb opacity:1; només tokens (cap `#hex` ni px literals per color/mida).
    - Behavior: `node --check` passa i pieceLabel('ull')/containerLabel('robot-cap') retornen etiquetes literals reals (no el fallback pelat).
  </acceptance_criteria>
  <done>El builder del bloc de grup, el mapa d'etiquetes HTML (reutilitzant els helpers tal com són, D-11) i els estils de l'overlay amb fade existeixen.</done>
</task>

<task type="auto">
  <name>Task 2: Wiring showOverlay/update/hide a renderForatRow + append a .active-split</name>
  <files>src/client/client.js</files>
  <read_first>
    - src/client/client.js (renderForatRow línia 1004-1064 — els listeners existents: color 'input' línia 1028-1031, range 'input' línia 1045-1049, i els 'change' que emeten TEAM_SET_CSS; updateFrozenOverlay línia 1415-1432 per al patró d'append/remove a `.active-split`)
    - .planning/phases/04.1-millores-operatives-d-admin-tornar-fase-anterior-reset-servi/04.1-PATTERNS.md (secció client.js — wiring a renderForatRow)
  </read_first>
  <action>
    A src/client/client.js implementa showOverlay(group), updateLiveOverlay(group, holeId, value) i hideOverlay(). showOverlay: assegura un únic node `<div class="css-live-overlay">` dins `.active-split` (crea'l si no existeix, mirallant com updateFrozenOverlay fa la query i l'append), fixa el seu textContent a l'etiqueta HTML de GROUP_ELEMENT_LABEL[group] seguida d'un salt de línia i de buildOverlayBlock(group, getLiveValues()), i afegeix la classe `css-live-overlay--visible` (fade in, D-10). updateLiveOverlay: recalcula el textContent amb buildOverlayBlock(group, getLiveValues(holeId, value)) perquè el valor que s'està movent aparegui en viu (D-08). hideOverlay: treu la classe `css-live-overlay--visible` (fade out); no cal esborrar el node. A renderForatRow, dins ELS listeners existents (no dupliquis applyCssHole): al bloc 'input' del color (línia 1028-1031) i del range (línia 1045-1049) afegeix una crida a updateLiveOverlay(hole.group, holeId, <valor formatat que ja es calcula>). A més, afegeix `input.addEventListener('focus', () => showOverlay(hole.group))` i `input.addEventListener('blur', () => hideOverlay())` a la funció (per als dos tipus de control). Mostra només el grup del forat actiu (D-07). Tot el text va per textContent; mai s'interpreten dades com a markup.
  </action>
  <verify>
    <automated>node --check src/client/client.js</automated>
    <human-check>Arrenca l'app (npm run dev + servidor), entra com a equip, avança a la Fase CSS. En enfocar/moure un control de forat: apareix el requadre amb l'etiqueta HTML de l'element + el bloc CSS sencer del seu grup; el valor canvia en viu mentre mous el control; en treure el focus, el requadre desapareix amb fade; el requadre no tapa ni bloqueja el robot; només es veu el grup del forat actiu.</human-check>
  </verify>
  <acceptance_criteria>
    - Source: existeixen showOverlay, updateLiveOverlay i hideOverlay; el node s'afegeix dins `.active-split` (mateixa query que updateFrozenOverlay).
    - Source: renderForatRow té listeners 'focus' i 'blur' que criden showOverlay/hideOverlay, i les crides updateLiveOverlay estan DINS dels blocs 'input' existents sense duplicar applyCssHole.
    - Source: el text de l'overlay s'assigna via textContent (D-08/GAME-06).
    - Behavior (human-check): overlay apareix/actualitza/desapareix amb fade lligat a focus/input/blur; només el grup actiu; no tapa el robot.
  </acceptance_criteria>
  <done>L'overlay es mostra en enfocar un forat, actualitza el valor en viu, i desapareix amb fade en perdre el focus, mostrant només el grup actiu i sense tapar el robot.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dades de forat (valors CSS) → DOM de la preview | Els valors es rendereixen com a text a l'overlay; s'ha d'evitar interpretar-los com a markup. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04.1-06 | Tampering (XSS) | render del bloc CSS de l'overlay | medium | mitigate | Assignar el bloc via `textContent` + `white-space: pre` (mai interpretar dades com a markup), coherent amb GAME-06/V5. Els valors ja passen la validació de CSS_HOLES abans d'arribar aquí. |
</threat_model>

<verification>
- `node --check src/client/client.js` passa.
- pieceLabel/containerLabel retornen etiquetes literals reals per als 7 grups.
- Revisió visual (human-check): overlay lligat a la interacció, valor en viu, fade, sense tapar el robot.
- La suite existent no es trenca: `npm test`.
</verification>

<success_criteria>
- Overlay mostra el bloc CSS sencer del grup actiu (D-08) amb valor en viu, precedit de l'etiqueta HTML (D-09/D-11).
- Només el grup del forat actiu, lligat a focus/input/blur (D-07).
- Fade ~180ms (D-10), pointer-events:none (no tapa el robot).
</success_criteria>

## Artifacts this phase produces (Plan 04)

| Symbol | Type | File |
|--------|------|------|
| `GROUP_ELEMENT_LABEL` | const map (group → HTML label helper) | src/client/client.js |
| `buildOverlayBlock(group, liveValues)` | function | src/client/client.js |
| `getLiveValues(overrideId, overrideValue)` | function | src/client/client.js |
| `showOverlay(group)` | function | src/client/client.js |
| `updateLiveOverlay(group, holeId, value)` | function | src/client/client.js |
| `hideOverlay()` | function | src/client/client.js |
| `.css-live-overlay`, `.css-live-overlay--visible` | CSS classes | src/client/client.css |

<output>
Create `.planning/phases/04.1-millores-operatives-d-admin-tornar-fase-anterior-reset-servi/04.1-04-SUMMARY.md` when done
</output>
