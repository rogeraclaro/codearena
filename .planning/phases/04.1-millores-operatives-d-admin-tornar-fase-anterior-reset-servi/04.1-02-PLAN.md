---
phase: 04.1-millores-operatives-d-admin-tornar-fase-anterior-reset-servi
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - ecosystem.config.cjs
  - package.json
autonomous: false
requirements: [D-05, D-06]
user_setup:
  - service: pm2
    why: "Supervisor de procés que revive el servidor després del Reset (D-04/D-06). NO instal·lat a la màquina de dev."
    install: "npm install -g pm2@7.0.3"

must_haves:
  truths:
    - "Existeix un fitxer de config PM2 amb extensió .cjs que arrenca src/server/index.js sota supervisió (autorestart)."
    - "package.json té scripts server:pm2 i server:pm2-stop per arrencar/aturar el procés supervisat."
    - "PM2 (fixat a 7.0.3) està instal·lat i pot arrencar l'entrypoint ESM sense ERR_REQUIRE_ESM (o bé el fallback CJS documentat funciona)."
    - "El mateix mecanisme PM2 s'usa en dev i prod (D-06) — cap branca per NODE_ENV."
  artifacts:
    - "ecosystem.config.cjs (NOU) a l'arrel del projecte"
    - "scripts server:pm2 / server:pm2-stop a package.json"
    - "server.cjs (NOMÉS si el checkpoint de boot detecta ERR_REQUIRE_ESM)"
  key_links:
    - "npm run server:pm2 → pm2 start ecosystem.config.cjs → PM2 supervisa src/server/index.js (autorestart:true)"
    - "autorestart:true → qualsevol process.exit(0) del Reset (Plan 03) fa que PM2 revive el procés net"
---

<objective>
Establir la infraestructura PM2 que fa possible el Reset real del servidor (Plan 03): un fitxer d'ecosistema `.cjs` obligatori (package.json té `"type": "module"`), scripts npm, i la verificació d'entorn que confirma que PM2 pot arrencar l'entrypoint ESM. Cobreix la part d'infraestructura de D-05 (dev+prod) i D-06 (mateix mecanisme PM2 en tots dos entorns).

Purpose: Aïllar l'única incertesa d'entorn de la fase (PM2 + ESM, obertura A1) i la porta de seguretat de supply-chain (PM2 marcat SUS) abans de construir el botó Reset, per no bloquejar Plan 03 amb sorpreses d'infra.
Output: `ecosystem.config.cjs`, scripts a package.json, PM2 instal·lat i verificat (amb fallback CJS documentat si cal).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04.1-millores-operatives-d-admin-tornar-fase-anterior-reset-servi/04.1-CONTEXT.md
@.planning/phases/04.1-millores-operatives-d-admin-tornar-fase-anterior-reset-servi/04.1-RESEARCH.md
@.planning/phases/04.1-millores-operatives-d-admin-tornar-fase-anterior-reset-servi/04.1-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Crear ecosystem.config.cjs + scripts PM2 a package.json</name>
  <files>ecosystem.config.cjs, package.json</files>
  <read_first>
    - package.json (línia 5 `"type": "module"` — motiu pel qual el config HA de ser .cjs; bloc scripts línia 7-13 per afegir-hi els nous scripts)
    - .planning/phases/04.1-millores-operatives-d-admin-tornar-fase-anterior-reset-servi/04.1-RESEARCH.md (Pattern 2 — camp d'ecosistema; Pitfall 2 — extensió .cjs obligatòria)
    - .planning/phases/04.1-millores-operatives-d-admin-tornar-fase-anterior-reset-servi/04.1-PATTERNS.md (secció ecosystem.config.cjs)
  </read_first>
  <action>
    Crea a l'arrel del projecte el fitxer `ecosystem.config.cjs` (extensió `.cjs` OBLIGATÒRIA perquè package.json té `"type": "module"` — un `.js` fallaria amb ERR_REQUIRE_ESM) que exporta via `module.exports` un objecte amb `apps: [{ name: 'codearena', script: 'src/server/index.js', autorestart: true, min_uptime: '5s', max_restarts: 30, env: { PORT: 3000 } }]`. `autorestart: true` és el que fa que el process.exit(0) del Reset (Plan 03) reviu el procés; `min_uptime`/`max_restarts` són el guard anti-crash-loop (Pitfall 3). A package.json, dins el bloc `scripts` (línia 7-13), afegeix `"server:pm2": "pm2 start ecosystem.config.cjs"` i `"server:pm2-stop": "pm2 delete codearena"`. NO toquis els scripts existents (`dev`, `server`, `build`, `start`, `test`).
  </action>
  <verify>
    <automated>node -e "const c=require('./ecosystem.config.cjs'); if(c.apps[0].script!=='src/server/index.js'||c.apps[0].autorestart!==true){process.exit(1)}" && node -e "const p=require('./package.json'); if(!p.scripts['server:pm2']||!p.scripts['server:pm2-stop']){process.exit(1)}"</automated>
  </verify>
  <acceptance_criteria>
    - Source: existeix ecosystem.config.cjs (extensió .cjs, no .js) i és require-able.
    - Source: c.apps[0] té name 'codearena', script 'src/server/index.js', autorestart true, min_uptime '5s', max_restarts 30.
    - Source: package.json conté els scripts server:pm2 i server:pm2-stop; els scripts previs es mantenen intactes.
  </acceptance_criteria>
  <done>El fitxer d'ecosistema .cjs i els scripts PM2 existeixen i són vàlids; la resta de package.json intacta.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 2: Checkpoint de legitimitat del paquet PM2 (supply-chain)</name>
  <action>Verificar la legitimitat de pm2 a npmjs.com abans d'autoritzar la instal·lació global (T-04.1-SC). No auto-aprovable.</action>
  <what-built>Encara no s'ha instal·lat PM2. Aquest checkpoint valida la legitimitat del paquet abans d'instal·lar-lo (PM2 marcat SUS «too-new» a la Package Legitimacy Audit de RESEARCH.md — fals positiu documentat, però la porta de supply-chain no és auto-aprovable).</what-built>
  <how-to-verify>
    1. Obre https://www.npmjs.com/package/pm2 i confirma: darrera versió 7.x, repo github.com/Unitech/pm2, milions de descàrregues setmanals, sense deprecació.
    2. Confirma que la versió a instal·lar queda FIXADA a `pm2@7.0.3` (no `latest`).
    3. Confirma que NO hi ha cap `postinstall` sospitós al paquet.
  </how-to-verify>
  <resume-signal>Escriu «approved» per autoritzar `npm install -g pm2@7.0.3`, o descriu els problemes trobats.</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Checkpoint de boot PM2 (entrypoint ESM, obertura A1)</name>
  <action>Instal·lar pm2@7.0.3, arrencar `pm2 start ecosystem.config.cjs` i confirmar boot net; aplicar el fallback server.cjs si apareix ERR_REQUIRE_ESM.</action>
  <what-built>Instal·lació de PM2 i verificació que arrenca l'entrypoint ESM src/server/index.js net (obertura A1 — l'únic risc d'entorn de la fase). Inclou el fallback CJS documentat si apareix ERR_REQUIRE_ESM.</what-built>
  <how-to-verify>
    1. Instal·la: `npm install -g pm2@7.0.3` i confirma `pm2 --version` (7.0.3).
    2. Arrenca: `npm run server:pm2` (equival a `pm2 start ecosystem.config.cjs`).
    3. Comprova estat i logs: `pm2 status` (procés `codearena` en estat `online`, no `errored`/looping) i `pm2 logs codearena --lines 30` (arrencada neta, sense `ERR_REQUIRE_ESM`).
    4. Si apareix `ERR_REQUIRE_ESM`: crea el fallback CJS `server.cjs` a l'arrel amb una sola línia `import('./src/server/index.js');`, canvia `script` a `'server.cjs'` a ecosystem.config.cjs, i repeteix els passos 2-3 fins a boot net. Documenta al SUMMARY quina ruta ha quedat (ESM directe o server.cjs).
    5. Neteja opcional: `npm run server:pm2-stop` (deixa l'entorn preparat per a Plan 03, que reprendrà el procés supervisat).
  </how-to-verify>
  <resume-signal>Escriu «approved» quan PM2 arrenqui el servidor net (indica si ha calgut el fallback server.cjs), o descriu l'error.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → màquina (dev/prod) | Instal·lació d'un paquet global (pm2); risc de supply-chain si el paquet fos maliciós. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04.1-SC | Tampering | npm install -g pm2 | high | mitigate | Checkpoint blocking-human de legitimitat (verificar npmjs.com/package/pm2, repo real, sense postinstall) + versió FIXADA `pm2@7.0.3` (no `latest`). No auto-aprovable. |
| T-04.1-07 | Denial of Service | crash-loop de PM2 | low | accept | `min_uptime: '5s'` + `max_restarts: 30` com a guard; en ús real el Reset és esporàdic. Documentat com a recuperable amb `pm2 restart codearena`. |
</threat_model>

<verification>
- `require('./ecosystem.config.cjs')` retorna la config esperada; scripts PM2 presents a package.json.
- Checkpoint de legitimitat aprovat (supply-chain).
- Checkpoint de boot: PM2 7.0.3 arrenca src/server/index.js net (o via fallback server.cjs) — ruta documentada al SUMMARY.
</verification>

<success_criteria>
- ecosystem.config.cjs (.cjs) i scripts server:pm2 / server:pm2-stop existeixen i són vàlids.
- PM2 7.0.3 instal·lat i verificat arrencant l'entrypoint (D-06: mateix mecanisme dev+prod).
- Fallback CJS aplicat i documentat NOMÉS si calia.
</success_criteria>

## Artifacts this phase produces (Plan 02)

| Symbol | Type | File |
|--------|------|------|
| `ecosystem.config.cjs` | PM2 process config (new file) | ecosystem.config.cjs |
| `module.exports.apps[0]` (name `codearena`) | config object | ecosystem.config.cjs |
| `scripts["server:pm2"]` | npm script | package.json |
| `scripts["server:pm2-stop"]` | npm script | package.json |
| `server.cjs` (conditional CJS launcher) | new file — only if ERR_REQUIRE_ESM | server.cjs |

<output>
Create `.planning/phases/04.1-millores-operatives-d-admin-tornar-fase-anterior-reset-servi/04.1-02-SUMMARY.md` when done (documenta si s'ha usat el fallback server.cjs)
</output>
