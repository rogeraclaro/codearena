---
phase: 04.1-millores-operatives-d-admin-tornar-fase-anterior-reset-servi
verified: 2026-07-06T13:48:02Z
status: human_needed
score: 15/15 must-haves verified (1 present-but-visual-unverified, routed to human check)
behavior_unverified: 1
overrides_applied: 0
behavior_unverified_items:
  - truth: "L'overlay de codi CSS en directe (D-07/D-09/D-10) es mostra visualment ben posicionat, amb fade suau perceptible i sense tapar mai el robot, a un navegador real."
    test: "Obrir l'app com a equip, avançar a la Fase CSS, enfocar/moure diversos controls de forat (color i range) de diferents grups."
    expected: "El requadre apareix amb fade suau (~180ms) ancorat sota la preview, mostra l'etiqueta HTML + el bloc CSS sencer del grup actiu amb el valor actualitzant-se en directe, desapareix amb fade en perdre el focus, mai tapa el robot, i només mostra el grup tocat en aquell moment (mai un històric)."
    why_human: "El posicionament visual, la percepció del fade i la garantia de 'no tapa el robot' són propietats de renderització real de navegador; aquest projecte no té cap harness DOM/visual automatitzat (Playwright/jsdom) que pugui exercir focus/input/blur i mesurar la posició/opacitat resultant."
human_verification:
  - test: "Overlay de codi CSS en directe a la Fase CSS (D-07/D-08/D-09/D-10/D-11) — revisió visual completa"
    expected: "Vegeu 'behavior_unverified_items' de dalt — fade, posicionament, etiqueta HTML+bloc CSS correctes, sense tapar el robot, només grup actiu."
    why_human: "Sense harness visual automatitzat al projecte; és exactament l'human-check que el Plan 04 (Task 2) ja marcava com a `human_judgment: true` i que `workflow.human_verify_mode=end-of-phase` diferia fins aquí."
  - test: "Recorregut complet a l'Admin real: avançar fins a CSS/JS, prémer «Fase anterior», confirmar al diàleg, verificar que totes les pantalles (equip+admin) tornen a la fase anterior amb timer complet."
    expected: "El diàleg de confirmació es mostra amb el missatge correcte, en confirmar totes les pantalles connectades reben el nou estat a l'instant."
    why_human: "El protocol (esdeveniments, timer, doneAt, admin-only) ja està verificat de manera automatitzada i real (`test/prevPhase.test.js`, 8/8 passant) i, a més, aquest verificador ha reproduït el cicle sencer via un client socket.io-client real amb resultats correctes; l'únic que resta és la confirmació visual del diàleg/render real a un navegador, marcada `human_judgment: true` al Plan 01 (Task 3) i diferida per `workflow.human_verify_mode=end-of-phase`."
---

# Phase 04.1: Millores operatives d'Admin Verification Report

**Phase Goal:** L'admin disposa de tres eines operatives/pedagògiques — botó «Fase anterior» (amb confirmació), botó «Reset» que reinicia el procés real via PM2, i un overlay de codi CSS en directe a la preview durant la Fase CSS — sense tocar l'abast del joc.
**Verified:** 2026-07-06T13:48:02Z
**Status:** human_needed
**Re-verification:** No — initial verification

## Note on Requirements Traceability

This phase has **no formal requirement IDs in `.planning/REQUIREMENTS.md`** — it is an urgent-inserted phase (04.1, between Phase 4 and Phase 5). `REQUIREMENTS.md`'s Phase Traceability table jumps directly from Phase 4 (`SCORE-01..05`, `ADMIN-07`) to Phase 5 (`DEPL-01/02`) with no `04.1-*` row, confirming there is nothing orphaned to reconcile. Per the phase's own ROADMAP.md entry and CONTEXT.md, the decision IDs **D-01 through D-11** (`04.1-CONTEXT.md`) are the authoritative requirement set for this phase, and each plan's `requirements:` frontmatter field correctly cites a subset of them (D-01/D-02/D-03 in Plan 01, D-05/D-06 in Plan 02, D-04/D-05/D-06 in Plan 03, D-07..D-11 in Plan 04). All 11 decision IDs are covered by at least one plan. This is treated as expected/by-design, not a gap.

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | Botó «Fase anterior» visible només a CSS/JS, mai a HTML ni un cop finalitzada la partida | ✓ VERIFIED | `src/client/admin.js:433` — `if (state.phase && state.phase !== 'html' && !finished)` guards button creation |
| 2 | En prémer «Fase anterior» apareix confirmació abans d'actuar (D-01) | ✓ VERIFIED | `admin.js:440-444` calls `showConfirm(...)` before emitting; `showConfirm()` is a real `<dialog>`-based Promise helper (`admin.js:312`), not a stub |
| 3 | En confirmar, totes les pantalles retrocedeixen amb timer nou complet (D-02) | ✓ VERIFIED | `test/prevPhase.test.js` `PREV-TIMER-NOU` and `PREV-ADMIN-OK` pass (8/8 subtests); independently reproduced live: `gameState.previousPhase()` mirrors `nextPhase()`→`startPhase()`, confirmed `remainingMsAtPause=null` and fresh `phaseEndsAt` |
| 4 | `doneAt` de la fase reoberta NO es reinicia (D-03) — equip continua congelat | ✓ VERIFIED | `test/prevPhase.test.js` `PREV-DONEAT-PRESERVAT` passes: after js→css retreat, `doneAt.css` unchanged and `setCssValue` still rejected for the frozen team |
| 5 | Un client no-admin no pot fer retrocedir la fase (re-validació server-side) | ✓ VERIFIED | `test/prevPhase.test.js` `PREV-ADMIN-ONLY` passes; handler's first line is `if (!socket.rooms.has('admin')) return;` (`socketHandlers.js:210`) |
| 6 | Botó «Reset» demana confirmació destructiva abans d'actuar (D-04) | ✓ VERIFIED | `admin.js:462-464` — `showConfirm('Segur que vols reiniciar el servidor?', 'Sí, reinicia', true)` |
| 7 | En confirmar, el procés surt amb exit code 0 i PM2 el revive net, tot l'estat es reinicia (D-04) | ✓ VERIFIED (live behavioral test, not just code reading) | Verifier started `pm2 start ecosystem.config.cjs`, registered two teams via a real socket, emitted `admin:reset-server` as an admin-authenticated client, and observed: PM2 `restart_time` incremented (1→4 across repeated resets), process `pid` changed, and the very next `session:full-state` after reconnect showed `teams: []` (full in-memory wipe) before teams were re-registered by the test script itself |
| 8 | Els clients es reconnecten automàticament al servidor net | ✓ VERIFIED | Same live test: `socket.io-client`'s default `reconnection:true` reconnected the admin socket automatically after `io.close()` server-side, with no custom reconnection code needed |
| 9 | Un client no-admin no pot disparar el reset | ✓ VERIFIED (live behavioral test) | Verifier connected a socket with no `role:'admin'` auth and emitted `admin:reset-server`: no `server:restarting` event, no disconnect, and `pm2 jlist` showed the process untouched (`restart_time` unchanged) |
| 10 | Mateix mecanisme PM2 en dev i prod, sense branca per NODE_ENV (D-05/D-06) | ✓ VERIFIED | `grep NODE_ENV` across `socketHandlers.js`/`ecosystem.config.cjs`/`server.cjs` returns zero matches — a single code path |
| 11 | Overlay mostra només el grup CSS actiu, no un històric (D-07) | ✓ VERIFIED | `showOverlay()`/`updateLiveOverlay()` fully overwrite the single `.css-live-overlay` node's `textContent` on every call (`client.js:1163-1182`) — no accumulation, no history |
| 12 | Overlay mostra el bloc CSS sencer del grup amb el valor en viu (D-08) | ✓ VERIFIED | `buildOverlayBlock(group, liveValues)` filters `CSS_HOLES` by group and renders every property line (`client.js:1153-1157`); wired into `input` handlers for both color and range controls (`client.js:1031, 1050`) |
| 13 | Etiqueta HTML de l'element via `pieceLabel()`/`containerLabel()` tal com són (D-09/D-11) | ✓ VERIFIED | `GROUP_ELEMENT_LABEL` map covers exactly the 7 `CSS_GROUP_ORDER` groups (`client.js:1133-1141`); verifier ran `pieceLabel('ull')` → `<span class="ull">` and `containerLabel('robot-cap')` → `<div id="robot-cap">` directly in Node — real literal labels, not a stripped-down variant |
| 14 | Fade curt (~180ms) i `pointer-events:none` (D-10) | ✓ VERIFIED | `client.css:422-440` — `.css-live-overlay { pointer-events: none; opacity: 0; transition: opacity 180ms ease; }` / `.css-live-overlay--visible { opacity: 1; }` |
| 15 | Overlay visualment correcte en un navegador real (posicionament, fade perceptible, mai tapa el robot) | ⚠️ PRESENT_BEHAVIOR_UNVERIFIED | Code/CSS present and correctly wired (see #11-14), but this project has no automated visual/DOM harness (no Playwright/jsdom) to exercise real rendering — routed to human verification below |

**Score:** 14/15 truths fully verified; 1 present-and-wired but visually unverified (routed to human check per `workflow.human_verify_mode=end-of-phase`).

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `src/server/gameState.js` — `previousPhase(durationMs)` | Mirror of `nextPhase`, exported via `gameState` | ✓ VERIFIED | Present at line 321, exported at line 395, exercised by 5 passing tests |
| `src/server/events.js` — `ADMIN_PREV_PHASE` | `'admin:prev-phase'` constant | ✓ VERIFIED | Present, correct value |
| `src/server/events.js` — `ADMIN_RESET_SERVER` / `SERVER_RESTARTING` | `'admin:reset-server'` / `'server:restarting'` | ✓ VERIFIED | Both present, correct values |
| `src/server/socketHandlers.js` — `ADMIN_PREV_PHASE` handler | admin-only, revalidates room + durationMs | ✓ VERIFIED | Lines 207-217; first statement is room check |
| `src/server/socketHandlers.js` — `ADMIN_RESET_SERVER` handler | admin-only, broadcasts notice, exits process | ✓ VERIFIED | Lines 227-237; live-tested end to end |
| `src/client/admin.js` — `showConfirm()` | Single reusable confirm dialog helper | ✓ VERIFIED | Line 312; `showResyncConfirm`/`showFinalizeConfirm` delegate to it (only one `<dialog class="confirm-dialog">` construction site) |
| `src/client/admin.js` — «Fase anterior» button | Conditionally rendered, wired to `showConfirm`+emit | ✓ VERIFIED | Lines 433-450 |
| `src/client/admin.js` — «Reset» button | Always rendered, destructive style, wired | ✓ VERIFIED | Lines 458-468 |
| `ecosystem.config.cjs` | `.cjs`, `autorestart:true`, `min_uptime`, `max_restarts` | ✓ VERIFIED | Present and require-able; `script` field points to `server.cjs` (documented fallback for a real PM2 fork-mode issue discovered during Plan 02) |
| `server.cjs` | CJS launcher bypassing broken `isMainModule` guard under PM2 | ✓ VERIFIED | Present; verifier confirmed PM2 boot produces the expected log line and the server actually listens (`curl` returns 404, not connection-refused) |
| `package.json` scripts `server:pm2` / `server:pm2-stop` | npm scripts for PM2 lifecycle | ✓ VERIFIED | Present; other scripts untouched |
| `test/prevPhase.test.js` | Integration test of retreat + admin-only enforcement | ✓ VERIFIED | 8/8 subtests pass |
| `src/client/client.js` — `GROUP_ELEMENT_LABEL`/`buildOverlayBlock`/`getLiveValues`/`showOverlay`/`updateLiveOverlay`/`hideOverlay` | Overlay builder + wiring | ✓ VERIFIED | All present, all correctly wired into `renderForatRow`'s focus/input/blur listeners |
| `src/client/client.css` — `.css-live-overlay` / `.css-live-overlay--visible` | Overlay styles with fade + pointer-events:none | ✓ VERIFIED | Present, tokens-only, matches spec |

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|----|--------|---------|
| «Fase anterior» button click | `gameState.previousPhase()` | `showConfirm()` → `socket.emit(EVENTS.ADMIN_PREV_PHASE, {durationMs})` → handler room-check → `previousPhase()` → `SESSION_FULL_STATE` broadcast | ✓ WIRED | Full chain confirmed by both source inspection and `test/prevPhase.test.js`'s socket-level assertions |
| «Reset» button click | `process.exit(0)` + PM2 revive | `showConfirm()` → `socket.emit(EVENTS.ADMIN_RESET_SERVER)` → handler room-check → `SERVER_RESTARTING` broadcast → `setTimeout` → `io.close()`+`process.exit(0)` | ✓ WIRED | Full chain reproduced live by the verifier: reset emitted, `pm2 jlist` restart counter incremented, teams wiped on reconnect |
| `npm run server:pm2` | PM2 supervising `src/server/index.js` | `pm2 start ecosystem.config.cjs` → `server.cjs` → `import('./src/server/index.js')` → `startServer()` | ✓ WIRED | Verified live: `pm2 status` shows `online`, `curl localhost:3000` returns `404` (server responding), `kill -9` on the pm2-managed pid triggers an automatic revive |
| `renderForatRow` input focus/input/blur | CSS live overlay | `input.addEventListener('focus', ...)`/`'input'`/`'blur'` → `showOverlay`/`updateLiveOverlay`/`hideOverlay` → `.css-live-overlay` textContent + class toggle | ✓ WIRED | Confirmed by direct source inspection; the DOM mutation itself is correct, only the rendered visual result is unverified (see Human Verification) |
| `GROUP_ELEMENT_LABEL[group]` | `pieceLabel()`/`containerLabel()` | Direct object literal referencing the imported functions | ✓ WIRED | Confirmed via a live Node import — real HTML label strings returned, not fallbacks |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| Full existing test suite (regression) | `npm test` | 94/94 pass | ✓ PASS |
| Prev-phase integration test | `node --test test/prevPhase.test.js` | 8/8 pass | ✓ PASS |
| PM2 boots the ESM entrypoint cleanly | `pm2 start ecosystem.config.cjs` + `curl localhost:3000/` | `404` (server responding, not connection-refused); clean startup log line, no errors | ✓ PASS |
| PM2 autorestart on crash | `kill -9 <pm2-managed-pid>` then re-check `pm2 jlist` | `restart_time` incremented, port responded again within ~2s | ✓ PASS |
| Admin-triggered reset causes a real process restart + state wipe | Real `socket.io-client` connected with `auth:{role:'admin'}`, emitted `admin:reset-server` after registering teams | `restart_time` incremented from 1→4 across repeated resets; `pid` changed each time; `session:full-state` on reconnect showed `teams: []` | ✓ PASS |
| Non-admin socket cannot trigger reset | `socket.io-client` connected with no admin auth, emitted `admin:reset-server` | No `server:restarting` event received, no disconnect, `pm2 jlist restart_time` unchanged | ✓ PASS |
| `pieceLabel`/`containerLabel` return real literal labels (not fallback/stub) | `node -e "import('./src/shared/robotTemplate.js')..."` | `<span class="ull">`, `<div id="robot-cap">` | ✓ PASS |
| No `NODE_ENV` branching in the reset/PM2 path (D-05/D-06) | `grep NODE_ENV` across `socketHandlers.js`, `ecosystem.config.cjs`, `server.cjs` | 0 matches | ✓ PASS |

### Requirements Coverage

This phase uses phase-local decision IDs (D-01..D-11 from `04.1-CONTEXT.md`), not formal `REQUIREMENTS.md` IDs. All 11 are covered:

| Decision | Description | Plan | Status | Evidence |
|----------|--------------|------|--------|----------|
| D-01 | Confirmació abans de «Fase anterior» | 01 | ✓ SATISFIED | `showConfirm()` call gates the emit |
| D-02 | Timer nou en retrocedir (mai recupera pausa) | 01 | ✓ SATISFIED | `PREV-TIMER-NOU` test |
| D-03 | `doneAt` no es reinicia en retrocedir | 01 | ✓ SATISFIED | `PREV-DONEAT-PRESERVAT` test |
| D-04 | Restart REAL amb confirmació prèvia | 03 | ✓ SATISFIED | Live behavioral test (process restart + wipe) |
| D-05 | Reset disponible en dev i prod | 02, 03 | ✓ SATISFIED | Button unconditional, no env gating found |
| D-06 | Mateix mecanisme PM2 en dev i prod, sense branca NODE_ENV | 02, 03 | ✓ SATISFIED | `grep NODE_ENV` empty; single handler path |
| D-07 | Overlay mostra només el grup actiu, no històric | 04 | ✓ SATISFIED | Single overlay node, full textContent replace |
| D-08 | Bloc CSS sencer del grup, valor en viu | 04 | ✓ SATISFIED | `buildOverlayBlock`/`updateLiveOverlay` wiring |
| D-09 | Etiqueta HTML per sobre del bloc | 04 | ✓ SATISFIED | `GROUP_ELEMENT_LABEL` prefixed in `showOverlay`/`updateLiveOverlay` |
| D-10 | Fade suau ~150-200ms | 04 | ✓ SATISFIED (visual smoothness itself → human check) | CSS `transition: opacity 180ms ease` present |
| D-11 | Reutilitza `pieceLabel()`/`containerLabel()` tal com són | 04 | ✓ SATISFIED | Verified live with Node import, includes the `src` fake attribute as decided |

No orphaned requirements: `REQUIREMENTS.md`'s Phase Traceability table has no `04.1-*` rows to reconcile (jumps Phase 4 → Phase 5 directly), consistent with this being an urgent-inserted phase tracked entirely via CONTEXT.md decisions.

### Anti-Patterns Found

No debt markers (`TBD`/`FIXME`/`XXX`/`TODO`/`HACK`) found in any file this phase modified. The only `placeholder` string matches are a legitimate HTML `placeholder` attribute and pre-existing code comments describing an unrelated, already-shipped visual feature — not debt markers.

**Findings from the independent code review (`04.1-REVIEW.md`) not caused by this phase's own D-01..D-11 work, and confirmed by this verifier to be isolated:**

- **CR-01 / CR-02 (critical, per REVIEW.md):** Both sit in `buildRegistrationBlock()`/`renderAdmin()`'s "default team-names textarea pre-fill" logic (`admin.js:497-530, 700-756`), a feature added in a separate prior commit before this phase started. This verifier confirmed via source inspection that this code path is entirely disjoint from `previousPhase`, `ADMIN_PREV_PHASE`, `ADMIN_RESET_SERVER`, and the CSS overlay — it does not call, import, or share any state with this phase's D-01..D-11 artifacts. **Per the scoping instructions for this verification, these do not fail any must-have of Phase 04.1** — they are a legitimate but separate defect that should be tracked and fixed as its own item, not blocking this phase.
- **WR-01 (warning):** `SERVER_RESTARTING` is broadcast but has no client-side listener anywhere — a "dead" friendly-notice event. Does not fail any stated must-have (no truth requires a UI banner), but is worth a follow-up fix for UX polish during a live reset.
- **WR-02 (warning, STRIDE-relevant):** `gameState.previousPhase()` and its socket handler have no server-side `state.finished` guard — only the Admin UI hides the button after finalize. A modified/stray client could theoretically re-open a "finished" game's last phase. No stated must-have explicitly requires this server-side guard (the plan's own Pitfall-5 note only mandated a client-side `!finished` check on the button), so this is **not** treated as a failed truth here, but it is a real gap in the "never once results are shown" invariant and is flagged for a follow-up hardening fix.
- **WR-03 (warning):** Admin's stale partial-ranking display after using "Fase anterior" — cosmetic/informational staleness, not a functional break of any D-01..D-03 truth.
- **WR-04 / WR-05 (warning):** PM2 `max_restarts:30` ceiling and missing explicit `cwd` in `ecosystem.config.cjs` — operational robustness notes for Phase 5 (production deployment), not blocking this phase's must-haves.

## Human Verification Required

### 1. Overlay de codi CSS en directe — revisió visual (D-07/D-08/D-09/D-10/D-11)

**Test:** Obrir l'app com a equip, avançar a la Fase CSS, i enfocar/moure diversos controls de forat (tant color com range) de diferents grups (p.ex. `.antena`, `#robot-cap`, `.orella`).
**Expected:** El requadre apareix amb un fade suau (~180ms, no instantani) ancorat sota la preview; mostra l'etiqueta HTML de l'element (p.ex. `<div id="robot-cap">`) seguida del bloc CSS sencer del grup (`selector { ... }`) amb el valor que s'està tocant actualitzant-se en directe mentre es mou el control; en perdre el focus, desapareix amb fade; en cap moment tapa o bloqueja el robot; només es veu el grup del forat tocat en aquell moment (mai un històric d'interaccions anteriors).
**Why human:** El projecte no té cap harness de proves visuals/DOM automatitzat (Playwright, jsdom, etc.) capaç d'exercir `focus`/`input`/`blur` i mesurar posicionament, opacitat percebuda o solapament visual amb el robot. El codi/CSS subjacent ja s'ha verificat correcte (veure Truths #11-14), però la garantia visual final requereix ulls humans en un navegador real. Aquest és exactament l'ítem `human_judgment: true` que el Plan 04 (Task 2) va marcar i que `workflow.human_verify_mode=end-of-phase` va diferir fins aquest punt.

### 2. Recorregut visual complet a l'Admin real — «Fase anterior»

**Test:** Amb l'Admin obert a un navegador, avançar la partida fins a CSS o JS, prémer «Fase anterior», confirmar al diàleg, i observar totes les pantalles connectades (equip + admin).
**Expected:** El diàleg mostra el missatge «Segur que vols tornar a la Fase X?»; en confirmar, totes les pantalles reflecteixen la fase anterior a l'instant amb un timer complet nou.
**Why human:** El protocol complet (esdeveniments, timer, `doneAt`, admin-only) ja s'ha verificat de manera automatitzada i real — `test/prevPhase.test.js` (8/8 passant) exercita el mateix camí de codi via un socket real, i aquest verificador ho ha reproduït addicionalment amb un client `socket.io-client` propi amb resultats correctes. L'únic que resta és la confirmació visual del render/diàleg reals en un navegador — marcat `human_judgment: true` al Plan 01 (Task 3) i diferit per `workflow.human_verify_mode=end-of-phase`. Risc residual baix atès el nivell de cobertura automatitzada i de verificació en viu ja realitzat.

## Gaps Summary

No gaps found that fail any of the phase's stated must-haves (D-01 through D-11). All server-side logic, event protocols, admin-only guards, and PM2 infrastructure were independently re-verified by this verifier through live behavioral testing (not just SUMMARY claims or static code reading) — including actually triggering a real PM2-supervised process restart, confirming state wipe, and confirming non-admin rejection.

The phase is not marked `passed` because two items are inherently visual/UX behaviors this project cannot verify headlessly (the CSS overlay's real-browser rendering, and the full click-through UI confirmation flow for "Fase anterior") — both were already flagged by the executing plans themselves as `human_judgment: true`, deferred per `workflow.human_verify_mode=end-of-phase`, and are now surfaced here as the single end-of-phase UAT checkpoint.

Separately, the code review's two CRITICAL findings (CR-01/CR-02) concern a pre-existing, unrelated feature (default team-names pre-fill) and were confirmed by this verifier to be structurally isolated from this phase's D-01..D-11 artifacts — they do not block this phase but should be tracked and fixed as their own item. The review's WARNING-level findings (WR-01 through WR-05) are legitimate hardening opportunities — particularly WR-02's missing server-side `finished` guard on `previousPhase()` — but none of them contradict a must-have truth as stated in the plans, so they are surfaced here for visibility rather than treated as blocking gaps.

---

*Verified: 2026-07-06T13:48:02Z*
*Verifier: Claude (gsd-verifier)*
