---
phase: 04.1-millores-operatives-d-admin-tornar-fase-anterior-reset-servi
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/server/gameState.js
  - src/server/events.js
  - src/server/socketHandlers.js
  - src/client/admin.js
  - test/prevPhase.test.js
autonomous: true
requirements: [D-01, D-02, D-03]

must_haves:
  truths:
    - "L'admin veu un botó «Fase anterior» al costat de «Següent fase» a totes les fases excepte HTML i excepte partida finalitzada."
    - "En prémer «Fase anterior» apareix una confirmació («Segur que vols tornar a la Fase X?») abans d'actuar (D-01)."
    - "En confirmar, totes les pantalles retrocedeixen a la fase anterior amb un timer nou complet (D-02)."
    - "Un equip que ja havia premut «Finalitzar» a la fase reoberta continua congelat després de retrocedir (D-03)."
    - "Un client no-admin no pot fer retrocedir la fase (re-validació de room server-side)."
  artifacts:
    - "src/server/gameState.js conté i exporta previousPhase()"
    - "src/server/events.js conté EVENTS.ADMIN_PREV_PHASE"
    - "src/server/socketHandlers.js conté el handler d'EVENTS.ADMIN_PREV_PHASE"
    - "src/client/admin.js conté el botó «Fase anterior» i una funció showConfirm() reutilitzable"
    - "test/prevPhase.test.js verifica el retrocés autoritatiu"
  key_links:
    - "botó admin → socket.emit(EVENTS.ADMIN_PREV_PHASE, {durationMs}) → handler valida room 'admin' → gameState.previousPhase() → io.to('session').emit(SESSION_FULL_STATE)"
    - "previousPhase() reutilitza startPhase() → timer nou (D-02) i mai toca team.doneAt (D-03)"
---

<objective>
Afegir el botó «Fase anterior» a l'Admin, simètric a «Següent fase», que fa retrocedir la fase de joc autoritativa amb confirmació prèvia. Cobreix D-01 (confirmació), D-02 (timer nou en retrocedir) i D-03 (doneAt no es reinicia).

Purpose: Permetre que el professor corregeixi un avanç de fase prematur durant la classe sense reiniciar tot.
Output: Nova funció `previousPhase()` a gameState, nou event admin `ADMIN_PREV_PHASE` + handler, botó a l'Admin, funció reutilitzable `showConfirm()`, i un test d'integració del retrocés.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04.1-millores-operatives-d-admin-tornar-fase-anterior-reset-servi/04.1-CONTEXT.md
@.planning/phases/04.1-millores-operatives-d-admin-tornar-fase-anterior-reset-servi/04.1-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Afegir previousPhase() a gameState + event ADMIN_PREV_PHASE</name>
  <files>src/server/gameState.js, src/server/events.js, test/prevPhase.test.js</files>
  <read_first>
    - src/server/gameState.js (llegeix startPhase línia 299-307, nextPhase línia 309-314, i l'objecte export `gameState` línia 363-383 — cal afegir-hi previousPhase)
    - src/server/events.js (llegeix l'objecte EVENTS, entrades ADMIN_NEXT_PHASE línia 10 i el comentari de direcció; afegeix ADMIN_PREV_PHASE amb el mateix estil kebab-case)
    - test/timer.test.js (patró de test d'integració amb socket.io-client contra startServer(0) — mirall exacte per al nou test)
    - .planning/phases/04.1-millores-operatives-d-admin-tornar-fase-anterior-reset-servi/04.1-PATTERNS.md (secció gameState.js — mirall de nextPhase)
  </read_first>
  <behavior>
    - previousPhase(durationMs): a la fase 'css' retorna true i deixa state.phase='html' amb timer nou.
    - previousPhase(durationMs): a la fase 'html' (índex 0) retorna false i NO canvia state.phase (no-op, Pitfall 5).
    - previousPhase(durationMs): a la fase 'js' retorna true i deixa state.phase='css'.
    - Després de previousPhase, state.remainingMsAtPause és null i state.phaseEndsAt és ~now+durationMs (timer nou, D-02).
    - previousPhase NO modifica cap team.doneAt (D-03): un equip amb doneAt.css fixat el manté després de retrocedir de js→css.
  </behavior>
  <action>
    A src/server/gameState.js afegeix la funció `previousPhase(durationMs)` immediatament després de `nextPhase` (línia 314), com a mirall exacte de nextPhase però decrementant l'índex: calcula `currentIndex = state.phase ? PHASE_ORDER.indexOf(state.phase) : -1`, `prevIndex = currentIndex - 1`, retorna `false` si `prevIndex < 0`, i altrament retorna `startPhase(PHASE_ORDER[prevIndex], durationMs)`. Reutilitzar `startPhase` satisfà D-02 (posa `remainingMsAtPause = null`) i D-03 (mai toca `team.doneAt`) de franc — NO afegeixis cap lògica de recuperació de timer ni cap reset de doneAt al camí de retrocés. Registra `previousPhase` a l'objecte export `gameState` (línia 363-383), just al costat de `nextPhase`. A src/server/events.js afegeix a EVENTS la constant `ADMIN_PREV_PHASE: 'admin:prev-phase'` amb un comentari «client→server, admin-only» seguint el mateix estil que ADMIN_NEXT_PHASE. Crea test/prevPhase.test.js mirallant test/timer.test.js: connecta un socket admin (auth amb ADMIN_SECRET com fa timer.test.js), avança fins a 'js', fes que un equip marqui doneAt a css, emet ADMIN_PREV_PHASE amb durationMs vàlid i assereix via SESSION_FULL_STATE que la fase retrocedeix i que el doneAt de l'equip es manté (D-03).
  </action>
  <verify>
    <automated>node --test test/prevPhase.test.js</automated>
  </verify>
  <acceptance_criteria>
    - Source: gameState.js exporta previousPhase (grep `previousPhase` a l'objecte `export const gameState`).
    - Source: events.js conté `ADMIN_PREV_PHASE: 'admin:prev-phase'`.
    - Source: el camí previousPhase NO conté cap assignació a `doneAt` ni cap lectura de `remainingMsAtPause` (grep dins la funció).
    - Behavior: `node --test test/prevPhase.test.js` passa i cobreix el no-op a HTML, el timer nou i la preservació de doneAt.
  </acceptance_criteria>
  <done>previousPhase() existeix, s'exporta, reutilitza startPhase, l'event ADMIN_PREV_PHASE està declarat, i el test d'integració passa verificant D-02 i D-03.</done>
</task>

<task type="auto">
  <name>Task 2: Handler ADMIN_PREV_PHASE (server-side, admin-only)</name>
  <files>src/server/socketHandlers.js</files>
  <read_first>
    - src/server/socketHandlers.js (handler ADMIN_NEXT_PHASE línia 178-186 com a mirall; safeHandler línia 16-25; patró de re-validació `socket.rooms.has('admin')` línia 180; `io` a l'scope de registerSocketHandlers línia 38)
    - src/server/events.js (EVENTS.ADMIN_PREV_PHASE i EVENTS.SESSION_FULL_STATE)
    - .planning/phases/04.1-millores-operatives-d-admin-tornar-fase-anterior-reset-servi/04.1-PATTERNS.md (secció socketHandlers.js — prev-phase handler)
  </read_first>
  <action>
    A src/server/socketHandlers.js registra el handler d'EVENTS.ADMIN_PREV_PHASE just després del handler d'ADMIN_NEXT_PHASE (línia ~186), com a mirall d'aquell PERÒ sense el bloc de partial-ranking (retrocedir no tanca cap fase). Envolta'l amb `safeHandler`. Dins: primer `if (!socket.rooms.has('admin')) return;` (V4, re-validació server-side — mai confiar en un flag del client), després valida el payload `const durationMs = payload?.durationMs; if (!(Number.isFinite(durationMs) && durationMs > 0)) return;` (V5), i finalment `if (gameState.previousPhase(durationMs)) { io.to('session').emit(EVENTS.SESSION_FULL_STATE, gameState.getPublicState()); }`. Segueix el patró mutation-returns-bool → broadcast només si la mutació retorna true.
  </action>
  <verify>
    <automated>node --test test/prevPhase.test.js</automated>
  </verify>
  <acceptance_criteria>
    - Source: existeix `socket.on(EVENTS.ADMIN_PREV_PHASE, safeHandler(...))` amb `socket.rooms.has('admin')` com a primera comprovació.
    - Source: el handler valida durationMs finit i >0 abans d'actuar.
    - Source: emet SESSION_FULL_STATE només dins de l'`if (gameState.previousPhase(durationMs))`.
    - Behavior: `node --test test/prevPhase.test.js` passa (un socket sense room admin no provoca retrocés; un admin sí).
  </acceptance_criteria>
  <done>El handler admin-only ADMIN_PREV_PHASE fa retrocedir la fase i re-difon l'estat autoritatiu; un client no-admin és ignorat (fail-closed).</done>
</task>

<task type="auto">
  <name>Task 3: Botó «Fase anterior» + showConfirm() reutilitzable a l'Admin</name>
  <files>src/client/admin.js</files>
  <read_first>
    - src/client/admin.js (buildControlBar línia 404-448 — CTA i inserció de botons; showResyncConfirm línia 307-345 i showFinalizeConfirm línia 352 — patró `<dialog class="confirm-dialog">` a generalitzar; PHASE_DURATION_MS línia 27; injectStyles .confirm-dialog línia 97-117)
    - src/server/events.js (EVENTS.ADMIN_PREV_PHASE — importar la constant, no el literal)
    - .planning/phases/04.1-millores-operatives-d-admin-tornar-fase-anterior-reset-servi/04.1-PATTERNS.md (secció admin.js — botons + showConfirm)
  </read_first>
  <action>
    A src/client/admin.js generalitza el patró duplicat de showResyncConfirm/showFinalizeConfirm en una única funció `showConfirm(message, confirmLabel, destructive)` que retorna una Promise<boolean>, reutilitzant el mateix `<dialog class="confirm-dialog">` (missatge a `.confirm-message`, botons `.confirm-actions`, cancel amb classe `btn`, confirm amb classe `btn-destructive` si destructive és true altrament `btn`, gestió d'Escape via l'event `cancel`, i cleanup amb dialog.close()+dialog.remove()). Refactoritza showResyncConfirm i showFinalizeConfirm perquè deleguin a showConfirm (no deixis tres còpies). A buildControlBar, després d'afegir el CTA (línia 448) i abans del botó Pausar (línia 450), insereix el botó «Fase anterior» NOMÉS quan `state.phase && state.phase !== 'html' && !finished` (guard de Pitfall 5). El botó: `type='button'`, `className='btn'`, `textContent='Fase anterior'`. Al click (async): calcula la fase destí amb un mapa `PREV_OF = { css: 'html', js: 'css' }` i etiquetes `PHASE_LABELS = { html: 'HTML', css: 'CSS', js: 'JS' }`, crida `await showConfirm(\`Segur que vols tornar a la Fase ${PHASE_LABELS[PREV_OF[state.phase]]}?\`, 'Sí, torna enrere', false)` (D-01) i, si es confirma, `socket.emit(EVENTS.ADMIN_PREV_PHASE, { durationMs: PHASE_DURATION_MS })`. Usa la constant EVENTS.ADMIN_PREV_PHASE (no un literal).
  </action>
  <verify>
    <automated>node -e "import('./src/client/admin.js').then(()=>{}).catch(e=>{ if(!/document|window|import\.meta|navigator/.test(String(e))) { console.error(e); process.exit(1);} })"</automated>
    <human-check>Obre l'Admin, avança a la fase CSS, prem «Fase anterior», confirma al diàleg i verifica que totes les pantalles tornen a HTML amb timer complet.</human-check>
  </verify>
  <acceptance_criteria>
    - Source: existeix una única funció showConfirm(...) i showResyncConfirm/showFinalizeConfirm hi deleguen (no hi ha tres blocs `<dialog class="confirm-dialog">` duplicats — grep del literal ha de retornar una sola construcció de dialog dins showConfirm).
    - Source: el botó «Fase anterior» s'afegeix dins del guard `state.phase !== 'html' && !finished`.
    - Source: el click emet EVENTS.ADMIN_PREV_PHASE (constant, no literal 'admin:prev-phase') amb { durationMs: PHASE_DURATION_MS } després de showConfirm.
    - Behavior (human-check): a CSS/JS el botó apareix; a HTML i a partida finalitzada NO apareix; confirmar fa retrocedir totes les pantalles.
  </acceptance_criteria>
  <done>El botó «Fase anterior» apareix a les fases correctes, demana confirmació (D-01) i dispara el retrocés; el patró de confirmació és una sola funció showConfirm reutilitzable.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client (admin browser) → Socket.io server | Un event ADMIN_PREV_PHASE arriba des d'un socket; el rol admin s'ha de re-validar server-side, mai confiar en el client. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04.1-01 | Elevation/Tampering | handler ADMIN_PREV_PHASE (socketHandlers.js) | high | mitigate | Primera línia del handler `if (!socket.rooms.has('admin')) return;` (fail-closed), idèntic a tots els handlers admin existents. Un socket no-admin no pot fer retrocedir la fase. |
| T-04.1-02 | Tampering | payload durationMs | medium | mitigate | Validar `Number.isFinite(durationMs) && durationMs > 0` abans d'actuar (mirall de ADMIN_NEXT_PHASE); payloads forjats no fixen timers invàlids. |
</threat_model>

<verification>
- `node --test test/prevPhase.test.js` passa (retrocés autoritatiu, no-op a HTML, timer nou D-02, doneAt preservat D-03).
- La suite existent no es trenca: `npm test`.
- Revisió manual: el botó apareix només a CSS/JS i demana confirmació.
</verification>

<success_criteria>
- previousPhase() existeix, s'exporta i reutilitza startPhase (D-02/D-03 de franc).
- ADMIN_PREV_PHASE declarat a EVENTS i gestionat amb re-validació admin server-side.
- Botó «Fase anterior» amb confirmació (D-01) i showConfirm() reutilitzable.
- Tots els tests passen.
</success_criteria>

## Artifacts this phase produces (Plan 01)

| Symbol | Type | File |
|--------|------|------|
| `previousPhase(durationMs)` | function (exported via `gameState` object) | src/server/gameState.js |
| `EVENTS.ADMIN_PREV_PHASE` (`'admin:prev-phase'`) | event constant | src/server/events.js |
| handler for `EVENTS.ADMIN_PREV_PHASE` | socket handler | src/server/socketHandlers.js |
| `showConfirm(message, confirmLabel, destructive)` | function | src/client/admin.js |
| `PREV_OF`, `PHASE_LABELS` (local maps) | const | src/client/admin.js |
| «Fase anterior» button (in `buildControlBar`) | DOM control | src/client/admin.js |
| `test/prevPhase.test.js` | test file (new) | test/prevPhase.test.js |

<output>
Create `.planning/phases/04.1-millores-operatives-d-admin-tornar-fase-anterior-reset-servi/04.1-01-SUMMARY.md` when done
</output>
