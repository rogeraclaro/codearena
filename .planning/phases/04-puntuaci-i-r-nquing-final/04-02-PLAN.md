---
phase: 04-puntuaci-i-r-nquing-final
plan: 02
type: execute
wave: 2
depends_on: ['04-01']
files_modified:
  - src/server/events.js
  - src/server/gameState.js
  - src/server/socketHandlers.js
  - src/client/client.js
  - src/client/admin.js
  - src/client/client.css
  - test/results.test.js
autonomous: true
requirements: [SCORE-04, SCORE-01]
must_haves:
  truths:
    - "El botó 'Finalitzar' de la fase HTML només s'habilita quan l'estructura és 100% correcta (D-07); és l'únic origen de la bonificació de temps (D-06)"
    - "No es renderitza cap botó 'Finalitzar' a CSS ni JS (D-08/D-09), i el servidor rebutja qualsevol mark-done que no sigui de la fase HTML"
    - "En tancar-se cada fase (HTML→CSS, CSS→JS), l'Admin veu un mini-rànquing parcial NOMÉS al seu panell, amb les fases no jugades comptant 0 (D-12/D-13)"
  artifacts:
    - src/server/gameState.js
    - src/server/socketHandlers.js
    - src/client/admin.js
  key_links:
    - "transició de fase (nextPhase) → buildRanking(mask) amb fases pendents = 0 → ADMIN_PARTIAL_RANKING emès NOMÉS a io.to('admin')"
    - "TEAM_MARK_DONE → gate html-only + isHtmlComplete(placement) → doneAt.html (font única de la bonificació de temps)"
---

<objective>
Refinament de la llesca de la Fase 4 sobre el flux DURANT el joc: (a) retrofit dels botons "Finalitzar" per fase — HTML amb gate de correcció 100% (D-07), CSS eliminat (D-08), JS mai renderitzat (D-09), amb enduriment servidor perquè `doneAt` només es pugui escriure a HTML; i (b) el mini-rànquing parcial d'Admin que apareix en tancar-se cada fase (D-12/D-13), calculat amb el MATEIX motor de puntuació del Pla 01 amb les fases no jugades comptant 0.

Purpose: Fa correcte el `doneAt.html` que alimenta la bonificació de temps (D-05/D-06/SCORE-04) i dona al professor visibilitat de standings a cada transició (D-12), sense exposar-ho als equips.
Output: Gate del botó HTML + eliminació del CSS/JS, enduriment de `markPhaseDone`, `buildRanking(mask)` connectat a la transició de fase, i el component `.admin-mini-rank`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-puntuaci-i-r-nquing-final/04-CONTEXT.md
@.planning/phases/04-puntuaci-i-r-nquing-final/04-RESEARCH.md
@.planning/phases/04-puntuaci-i-r-nquing-final/04-PATTERNS.md
@.planning/phases/04-puntuaci-i-r-nquing-final/04-UI-SPEC.md
@.planning/phases/04-puntuaci-i-r-nquing-final/04-01-SUMMARY.md
</context>

<artifacts_this_phase_produces>
- `src/server/events.js`: `ADMIN_PARTIAL_RANKING: 'admin:partial-ranking'`.
- `src/server/gameState.js`: gate de `markPhaseDone` a `phase === 'html'` + `isHtmlComplete(placement)`; ús de `buildRanking(mask)` (creat al Pla 01) amb màscara de fases jugades a la transició.
- `src/server/socketHandlers.js`: gate D-07 al handler `TEAM_MARK_DONE`; emissió del rànquing parcial a `io.to('admin')` al punt de `nextPhase`.
- `src/client/client.js`: gate del botó HTML (`disabled` si no `isHtmlComplete`); eliminació de la crida `renderFinishButton('css')` (D-08).
- `src/client/admin.js`: component `.admin-mini-rank` (llista amb barra fina) + listener `ADMIN_PARTIAL_RANKING`.
- `src/client/client.css`: `.admin-mini-rank`, `.admin-mini-rank__caption`, `.admin-mini-rank__row`.
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Gate 100% del botó HTML (D-07) + eliminació CSS/JS (D-08/D-09) + enduriment servidor</name>
  <files>src/server/gameState.js, src/server/socketHandlers.js, src/client/client.js, src/client/client.css, test/results.test.js</files>
  <read_first>
    - src/server/gameState.js (`markPhaseDone` línies 212-218 — actualment phase-generic; `placePiece` línia 105 per veure com els callers deriven la fase)
    - src/server/socketHandlers.js (`TEAM_MARK_DONE` handler línies 321-331 — deriva la fase de `getPublicState()`, mai del payload)
    - src/client/client.js (`renderFinishButton` línies 1373-1386; la crida `renderFinishButton('html')` línia 1454 i `renderFinishButton('css')` línia 1457 dins el bloc `state.phase === 'css'` línia 1455; `latestPlacement` línia 296)
    - src/client/client.css (`.finish-phase-btn` i `.finish-phase-btn:disabled` línies 312-330)
    - src/shared/scoring.js (`isHtmlComplete` — creat al Pla 01)
    - 04-RESEARCH.md §Pitfall 5 (D-08 deixa un mark-done CSS viu) i §Security (Tampering)
    - 04-PATTERNS.md §src/client/client.js (línies 1450-1458, objectiu de la línia 1457) i §src/server/gameState.js (gate D-07)
  </read_first>
  <behavior>
    Tests a `test/results.test.js` (afegeix-los; RED primer):
    - Gate servidor D-07: amb `state.phase === 'html'` i un placement INCOMPLET (<7 slots), `TEAM_MARK_DONE` NO escriu `doneAt.html` (markPhaseDone retorna false). Amb els 7 slots plens, SÍ l'escriu.
    - Enduriment D-08/D-09: amb `state.phase === 'css'` o `'js'`, `TEAM_MARK_DONE` mai escriu `doneAt.css`/`doneAt.js` (rebutjat — només html). Un payload forjat no ho pot saltar (la fase deriva de `state.phase`).
    - La bonificació de temps segueix alimentant-se NOMÉS de `doneAt.html`.
  </behavior>
  <action>
    A `gameState.js`: endureix `markPhaseDone(teamId, phase)` perquè retorni false llevat que `phase === 'html'` I `isHtmlComplete(team.placement)` (importa `isHtmlComplete` de `../shared/scoring.js`). Així cap `doneAt.css`/`doneAt.js` no es pot escriure mai (D-08/D-09, Pitfall 5) i `doneAt.html` només es registra a correcció 100% (D-07). Mantén la idempotència existent (no-op si ja marcat).

    A `socketHandlers.js`: el handler `TEAM_MARK_DONE` ja deriva la fase de `getPublicState()` — no cal canviar la derivació; el gate viu a `markPhaseDone`. Confirma que continua emetent `TEAM_DONE_STATE` només quan `markPhaseDone` retorna true.

    A `client.js`: a `renderFinishButton('html')`, afegeix `button.disabled = done || !isHtmlComplete(latestPlacement)` (importa `isHtmlComplete` de `../shared/scoring.js`); actualitza l'estat disabled cada cop que arriba un `TEAM_BOARD_STATE` (re-render del botó o toggle de `disabled`). Elimina la crida `renderFinishButton('css')` (línia ~1457) dins el bloc `state.phase === 'css'` (D-08). No afegeixis mai cap botó per JS (D-09 — ja absent). Sense text d'error explicatiu del gate: els pips N/7 existents (`.progress-pieces`) ja comuniquen la proximitat (ètos sense-error).

    A `client.css`: cap selector nou necessari — reutilitza `.finish-phase-btn:disabled` (`--color-muted`) per l'estat bloquejat. (Toca aquest fitxer NOMÉS si cal ajustar l'estat disabled; altrament deixa'l sense canvis i treu-lo de `files` al SUMMARY.)
  </action>
  <verify>
    <automated>node --test test/results.test.js && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `node --test test/results.test.js` passa els nous casos de gate D-07/D-08/D-09.
    - El botó HTML està `disabled` fins que `isHtmlComplete(latestPlacement)` és true; sense text d'error.
    - No es renderitza cap botó "Finalitzar" a les fases CSS ni JS.
    - `markPhaseDone` mai escriu `doneAt.css` ni `doneAt.js`, ni tan sols amb un payload forjat.
    - `npm run build` compila.
  </acceptance_criteria>
  <done>El botó HTML és l'única afordança de finalització, gated a 100%, i el servidor garanteix que `doneAt` només existeix per HTML.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Rànquing parcial d'Admin en tancar cada fase (D-12/D-13)</name>
  <files>src/server/events.js, src/server/gameState.js, src/server/socketHandlers.js, src/client/admin.js, src/client/client.css, test/results.test.js</files>
  <read_first>
    - src/server/events.js (bloc `EVENTS`; afegir `ADMIN_PARTIAL_RANKING`)
    - src/server/socketHandlers.js (`ADMIN_NEXT_PHASE` handler línies 169-179 — punt de transició; `ADMIN_FORCE_RESYNC` per la forma admin-only directed emit)
    - src/server/gameState.js (`nextPhase` línies 240-245; `PHASE_ORDER` línia 23; `buildRanking(mask)` creat al Pla 01)
    - src/client/admin.js (`renderAdmin` línia 405, assemblatge del container línies 422-436, `buildTeamCard`/`team-grid` línies 358-436 per l'estil de construcció DOM; `showResyncConfirm` per patrons)
    - src/client/client.css (`.team-card`/`.team-grid` per vocabulari de tokens; capçalera token-only)
    - 04-UI-SPEC.md §Admin partial mini-ranking (format llista-amb-barra, caption "Rànquing parcial — fase {HTML/CSS} tancada", % en --color-muted "provisional")
    - 04-PATTERNS.md §src/server/socketHandlers.js (partial ranking hook) i §src/client/admin.js (mini-ranking)
  </read_first>
  <behavior>
    Tests a `test/results.test.js` (afegeix-los; RED primer):
    - En emetre `ADMIN_NEXT_PHASE` que tanca la fase HTML (passa a CSS), l'Admin (i NOMÉS l'Admin) rep `ADMIN_PARTIAL_RANKING` amb un rànquing calculat amb `mask = { html:1, css:0, js:0 }` (CSS/JS compten 0, D-13). Cap socket d'equip rep aquest event.
    - En tancar CSS (passa a JS), l'Admin rep el parcial amb `mask = { html:1, css:1, js:0 }`.
    - El parcial usa el MATEIX `buildRanking` que el rànquing final (mateix pipeline, D-13), no un càlcul separat.
  </behavior>
  <action>
    A `events.js`: afegeix `ADMIN_PARTIAL_RANKING: 'admin:partial-ranking'` (Server→Client) dins el mateix `Object.freeze`.

    A `socketHandlers.js`: al handler `ADMIN_NEXT_PHASE`, després que `gameState.nextPhase(durationMs)` retorni true i s'emeti el `SESSION_FULL_STATE` habitual, calcula el rànquing parcial i emet NOMÉS a `io.to('admin')` `ADMIN_PARTIAL_RANKING` amb `{ ranking, closedPhase }`. Deriva la màscara de les fases ja jugades: la fase que s'acaba de tancar i les anteriors compten 1, les futures 0 (D-13). Deriva `closedPhase` de la fase anterior a la transició (la que s'acaba de tancar) per al caption. Mai a `io.to('session')` (D-12 — els equips no ho veuen).

    A `gameState.js`: exposa un helper mínim si cal per obtenir la màscara de fases jugades a partir de `PHASE_ORDER` i la fase resultant (o calcula-la al handler). Reutilitza `buildRanking(mask)` (Pla 01) — no dupliquis el càlcul.

    A `admin.js`: afegeix el listener `socket.on(EVENTS.ADMIN_PARTIAL_RANKING, ...)` que desa el darrer parcial i renderitza `.admin-mini-rank` dins el panell (prop de la control bar / sobre el `.team-grid`), reconstruint-se de forma segura a cada `renderAdmin`. Format: llista compacta amb barra fina per fila (`[rank #] [nom — Label] [barra fina] [% — --color-muted]`), ordre descendent, + caption "Rànquing parcial — fase {HTML/CSS} tancada". Importa `EVENTS` (no literals). No mostra sub-checks (standings, no debrief). Persisteix fins al rànquing final.

    A `client.css`: afegeix `.admin-mini-rank`, `.admin-mini-rank__caption` (Body), `.admin-mini-rank__row` amb barra fina — NOMÉS `var(--*)` tokens: rows `--color-surface`/`--color-border`, %/fill `--color-muted` (provisional), cap `--color-accent` (reservat al CTA).
  </action>
  <verify>
    <automated>node --test test/results.test.js && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `node --test test/results.test.js` passa: el parcial arriba NOMÉS a l'Admin, amb la màscara correcta per cada transició, calculat amb `buildRanking`.
    - Cap socket d'equip rep mai `ADMIN_PARTIAL_RANKING` (D-12), verificat per test.
    - El mini-rànquing usa el mateix pipeline que el final (D-13), no un càlcul paral·lel.
    - `.admin-mini-rank` no usa `--color-accent`; % en `--color-muted`.
    - `npm run build` compila.
  </acceptance_criteria>
  <done>El professor veu standings provisionals a cada transició de fase, privats a l'Admin, amb el mateix motor de puntuació que el rànquing final.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client(equip)→servidor | `TEAM_MARK_DONE` amb payload forjat; la fase deriva SEMPRE de `state.phase`, mai del payload |
| servidor→client | El rànquing parcial és Admin-only; els equips no l'han de rebre mai (D-12) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-03 | Tampering | `TEAM_MARK_DONE` / `markPhaseDone` | high | mitigate | Gate a `phase === 'html'` I `isHtmlComplete(placement)` (D-07); fase derivada de `state.phase`, mai del payload; impedeix registrar un `doneAt` fals ràpid o marcar CSS/JS (Pitfall 5) |
| T-04-06 | Information Disclosure | emissió del rànquing parcial | medium | mitigate | Emissió NOMÉS a `io.to('admin')`; mai a `io.to('session')` — els equips no veuen standings provisionals (D-12); verificat per test |
| T-04-05 | Denial of Service | payload malformat | low | mitigate | Handlers dins `safeHandler` existent |
| T-04-SC | Tampering | instal·lacions npm/pip/cargo | low | accept | N/A — cap `npm install` nou aquesta fase |
</threat_model>

<verification>
- `node --test test/results.test.js` verd amb els nous casos (gate D-07, enduriment D-08/D-09, parcial admin-only D-12/D-13).
- `npm test` complet sense regressions.
- `npm run build` compila.
- Verificació de privacitat: cap event de rànquing parcial arriba a un socket d'equip.
</verification>

<success_criteria>
- El botó "Finalitzar" de HTML només s'habilita a 100% i és l'única afordança de finalització (D-07/D-08/D-09).
- El servidor garanteix que `doneAt` només existeix per HTML (font única de la bonificació de temps, D-06).
- L'Admin veu un mini-rànquing parcial a cada tancament de fase, privat i amb el mateix pipeline que el final (D-12/D-13).
</success_criteria>

<output>
Crea `.planning/phases/04-puntuaci-i-r-nquing-final/04-02-SUMMARY.md` en acabar.
</output>
