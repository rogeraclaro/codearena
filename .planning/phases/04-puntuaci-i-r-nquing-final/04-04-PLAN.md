---
phase: 04-puntuaci-i-r-nquing-final
plan: 04
type: execute
wave: 4
depends_on: ["04-02", "04-03"]
files_modified:
  - src/server/gameState.js
  - src/server/socketHandlers.js
  - src/client/client.js
  - test/results.test.js
autonomous: false
requirements: [ADMIN-07, SCORE-05]
must_haves:
  truths:
    - "Un equip pot prémer 'Finalitzar' a la fase CSS i a la fase JS sense cap gate de correcció (sempre clicable un cop la fase és activa) — D-15."
    - "En prémer 'Finalitzar' a CSS/JS l'equip queda congelat NOMÉS ell: no pot editar més valors CSS ni afegir/editar regles JS; el timer global i la resta d'equips no es veuen afectats — D-15."
    - "Els timestamps doneAt.css/doneAt.js mai alteren cap puntuació: el bonus de temps segueix sent exclusiu de HTML (D-05/D-06 intactes)."
    - "La pantalla de resultats final (equips + Admin) mostra NOMÉS el rànquing + percentatge global de cada equip, sense cap detall de sub-checks per a ningú — D-16."
  artifacts:
    - src/server/gameState.js
    - src/server/socketHandlers.js
    - src/client/client.js
    - test/results.test.js
  key_links:
    - "setCssValue / setJsRules respecten team.doneAt[phase]: rebutgen tota mutació un cop l'equip s'ha congelat (freeze server-side, no de client)."
    - "markPhaseDone escriu doneAt.css/doneAt.js voluntàriament PERÒ htmlTimeBonuses (scoring.js) només llegeix doneAt.html — el bonus mai els veu."
    - "renderFinishButton('css') / renderFinishButton('js') emeten TEAM_MARK_DONE sense gate; renderFinishButton('html') conserva el gate D-07."
    - "El payload de CEREMONY_START / GAME_RESULTS ja no duu ownDetail; renderResultsScreen ja no pinta el detall de sub-checks."
---

<objective>
Addendum de la Fase 4 a partir de feedback en viu (2026-07-06), aplicant D-15 i D-16 que superseeixen D-08/D-09 i D-10/D-11 respectivament. Una única llesca vertical: lògica de congelació al servidor + botó voluntari al client + eliminació del detall de sub-checks a la pantalla de resultats.

- **D-15:** Les fases CSS i JS recuperen un botó "Finalitzar" **voluntari, sense gate de correcció** (a diferència de HTML/D-07, que sí té gate 100%). En prémer-lo l'equip queda **congelat** (`doneAt.css`/`doneAt.js`): no pot seguir editant CSS ni afegint/editant regles JS. El timer global NO es toca. **Sense bonificació de temps** — `doneAt.css`/`doneAt.js` només congelen, mai entren a la fórmula de score (D-05/D-06 exclusius de HTML, intactes).
- **D-16:** La pantalla de resultats NO mostra el detall de sub-checks a NINGÚ (ni al propi equip). Totes les pantalles (equips + Admin) veuen NOMÉS el rànquing + percentatge global. El servidor deixa d'emetre `ownDetail`.

Purpose: Tancar el gir de disseny post-implementació sense tocar el motor de puntuació (scoring.js) ni la cerimònia (04-03).
Output: gameState + socketHandlers endurits, client amb botó CSS/JS i sense detall, tests actualitzats.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Decisions que manen aquest pla (llegir D-15/D-16, que superseeixen D-08/D-09 i D-10/D-11):
@.planning/phases/04-puntuaci-i-r-nquing-final/04-CONTEXT.md

# Estat i handlers ja construïts a 04-01/04-02 que aquest pla modifica:
@.planning/phases/04-puntuaci-i-r-nquing-final/04-02-SUMMARY.md
@.planning/phases/04-puntuaci-i-r-nquing-final/04-01-SUMMARY.md

# Fonts a modificar (identificadors exactes ja verificats):
@src/server/gameState.js
@src/server/socketHandlers.js
@src/client/client.js
@test/results.test.js
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Congelació server-side D-15 + eliminació d'ownDetail D-16 (test-first)</name>
  <files>test/results.test.js, src/server/gameState.js, src/server/socketHandlers.js</files>
  <behavior>
    Escriu/actualitza els tests PRIMER a test/results.test.js, després implementa fins a verd:
    - D-15 congelació: amb state.phase='css', gameState.markPhaseDone(teamId, 'css') retorna true i escriu team.doneAt.css (voluntari, sense gate). Un cop marcat, gameState.setCssValue(teamId, holeId, valorVàlid) retorna false (congelat). Idem per JS: markPhaseDone(teamId, 'js') escriu doneAt.js i setJsRules(teamId, [reglaVàlida]) retorna després false.
    - D-15 scoring intacte: htmlTimeBonuses NOMÉS considera doneAt.html; un equip amb doneAt.css/js però sense doneAt.html no rep cap bonus (reutilitza l'assert existent del comportament html-only).
    - D-15 HTML sense canvis: markPhaseDone(teamId,'html') segueix exigint isHtmlComplete (gate D-07 intacte); un placement incomplet retorna false.
    - D-16: en finalitzar, el payload de CEREMONY_START rebut per un equip NO conté ownDetail (undefined); el ranking difós segueix sent {id,name,globalPct} sense subchecks; l'Admin també rep ranking sense ownDetail. La reconnexió post-finalize (GAME_RESULTS) tampoc duu ownDetail.
    Substitueix el test HARDEN-D08/D09 existent (que afirmava que doneAt.css/js mai s'escriuen) pel nou invariant D-15; actualitza el test FINALIZE-ROUNDTRIP + D-10 i el test F5-RECOVERY perquè assertin l'absència d'ownDetail.
  </behavior>
  <action>
Actualitza test/results.test.js abans d'implementar (RED→GREEN, mateix patró d'executor seqüencial que 04-01/04-02: test + implementació al mateix commit atòmic per tasca):

(1) Reemplaça el test "HARDEN-D08/D09: markPhaseDone mai escriu doneAt.css ni doneAt.js" per un test D-15 anomenat "VOLUNTARY-FREEZE-D15". Ha d'exercitar, amb la fase corresponent activa via els helpers d'arrencada existents (startServer/startPhase igual que la resta del fitxer): (a) marcar CSS fet escriu doneAt.css i deixa setCssValue en no-op (false); (b) marcar JS fet escriu doneAt.js i deixa setJsRules en no-op (false); (c) un equip amb doneAt.css/js però sense doneAt.html no apareix al Map de htmlTimeBonuses (bonus html-only). Manté l'assert que markPhaseDone('html') sobre placement incomplet retorna false (gate D-07).

(2) Al test "FINALIZE-ROUNDTRIP + D-10" (renombra a "FINALIZE-ROUNDTRIP-D16"): elimina els asserts sobre p1.ownDetail; afegeix asserts que p1.ownDetail, p2.ownDetail i pa.ownDetail són tots undefined, i conserva l'assert que el ranking difós no conté la subcadena de subchecks.

(3) Al test "F5-RECOVERY ... GAME_RESULTS": elimina l'assert sobre results.ownDetail; assert que results.ownDetail és undefined i que results.ranking existeix.

Després implementa:

src/server/gameState.js — markPhaseDone (funció actual a l'entorn de la línia 225): afluixa el gate. Manté el gate HTML (retorna false si phase==='html' i !isHtmlComplete(team.placement)). Accepta phase==='css' i phase==='js' sense cap comprovació de correcció (D-15, voluntari). Rebutja qualsevol phase fora de PHASE_ORDER. Conserva la idempotència (return false si team.doneAt[phase] ja existeix). Actualitza el comentari de capçalera de la funció perquè descrigui D-15 (CSS/JS voluntaris, només congelen; el bonus segueix sent html-only via htmlTimeBonuses) en lloc de l'enduriment D-08/D-09. NO toquis src/shared/scoring.js: htmlTimeBonuses ja filtra doneAt.html, així doneAt.css/js mai entren al càlcul.

src/server/gameState.js — setCssValue: afegeix, just després del guard de fase/frozen existent, un guard que retorni false si team.doneAt.css existeix (congelat després de "Finalitzar", D-15). setJsRules: afegeix el guard anàleg que retorni false si team.doneAt.js existeix. Comentari breu que citi D-15.

src/server/socketHandlers.js — handler ADMIN_FINALIZE_GAME (entorn línia 257): elimina el detall privat. En lloc del bucle per-equip que emetia CEREMONY_START amb {ranking, ownDetail}, emet un únic broadcast a io.to('session') amb NOMÉS {ranking} (un sol broadcast → lockstep, coherent amb D-14). Elimina la crida a gameState.getTeamSubchecks. Actualitza el comentari perquè reflecteixi D-16 (cap detall privat; ranking públic idèntic per a tothom).

src/server/socketHandlers.js — bloc de reconnexió post-finalize (entorn línia 107): a l'emissió de GAME_RESULTS, elimina el camp ownDetail; deixa NOMÉS { ranking: gameState.buildRanking() }.

src/server/gameState.js — dead-code hygiene (D-16): si després dels canvis anteriors getTeamSubchecks ja no té cap consumidor, elimina la funció getTeamSubchecks i la seva entrada a l'objecte exportat gameState. No deixis cap comentari que anomeni la funció eliminada. NO eliminis scoreHtml/scoreCss/scoreJs de scoring.js: buildRanking encara els fa servir.
  </action>
  <verify>
    <automated>node --test test/results.test.js test/scoring.test.js</automated>
    <automated>! rtk proxy grep -rIn "getTeamSubchecks" src/ || (echo "getTeamSubchecks encara referenciat" && false)</automated>
  </verify>
  <done>node --test test/results.test.js test/scoring.test.js verd. markPhaseDone escriu doneAt.css/js sense gate però setCssValue/setJsRules els respecten (retornen false un cop congelat). El gate HTML D-07 (isHtmlComplete) intacte. Cap payload (CEREMONY_START/GAME_RESULTS, equips + Admin) conté ownDetail. getTeamSubchecks eliminada si ha quedat sense consumidors. scoring.js sense tocar.</done>
</task>

<task type="auto">
  <name>Task 2: Botó "Finalitzar" voluntari CSS/JS + congelació de panell + retirada del detall de resultats (client)</name>
  <files>src/client/client.js</files>
  <action>
src/client/client.js — renderFinishButton(phase) (entorn línia 1379): fes el gate dependent de la fase. Manté el gate D-07 NOMÉS per a phase==='html' (button.disabled = done || !isHtmlComplete(latestPlacement)). Per a phase==='css' i phase==='js' NO hi ha gate de correcció (D-15): button.disabled = done únicament (sempre clicable mentre no s'hagi finalitzat). El text i el click (emet EVENTS.TEAM_MARK_DONE) es mantenen igual per a totes les fases. Actualitza el comentari de capçalera de la funció perquè descrigui D-15 (CSS/JS voluntaris sense gate) al costat de D-07 (HTML gated).

src/client/client.js — renderActiveSplitScreen (entorn línies 1477-1486): a la branca state.phase==='css', després de panel.appendChild(renderCssPanel(latestCssValues)), afegeix panel.appendChild(renderFinishButton('css')). A la branca state.phase==='js', després de panel.appendChild(renderJsPanel(latestJsRules)), afegeix panel.appendChild(renderFinishButton('js')). Actualitza el comentari D-08 de la branca CSS (línies 1478-1479) perquè reflecteixi D-15 (botó voluntari, congela l'equip) en lloc de "cap botó a CSS".

src/client/client.js — renderJsPanel (entorn línia 1293): amplia el càlcul de frozen perquè inclogui isPhaseDone('js') a més de timerStatus==='frozen' (mirall exacte de renderCssPanel a la línia 1072, que ja inclou isPhaseDone('css')). Així un F5 després d'haver premut "Finalitzar" a JS re-renderitza el panell congelat.

src/client/client.js — listener TEAM_DONE_STATE (entorn línia 1849): afegeix una branca per a phase==='js' anàloga a la de css: deshabilita tots els controls interactius dins de .js-rules (tots els select i tots els button, inclosos "Afegir JavaScript" i "Veure"), mirall de com la branca css deshabilita els .css-panel input. La branca html i la css es mantenen igual.

src/client/client.js — renderResultsScreen (entorn línies 1703-1709): elimina completament el bloc que renderitza el detall de sub-checks (la capçalera "El teu detall" i la crida a buildOwnDetail). Conserva intactes: el rànquing global (buildRankRow / .rank-list) i el bloc "El teu resultat" amb el percentatge global propi (.own-percent) — D-16 manté rànquing + percentatge global. Elimina també la variable local ownDetail si queda sense ús dins la funció.

src/client/client.js — dead-code hygiene (D-16): elimina les funcions ara òrfenes buildOwnDetail, buildSubcheckGroup i buildSubcheckItem, i la constant HTML_SLOT_LABELS, un cop confirmat per grep que no tenen cap altre consumidor. Revisa l'import de lucide (línia amb createElement/Trophy/Check/Circle...): retira NOMÉS les icones que quedin sense ús després de suprimir buildSubcheckItem (Check i Circle si ja no s'usen enlloc); conserva Trophy (encara l'usa buildRankRow) i qualsevol altra icona encara referenciada. No toquis src/client/client.css (els selectors .subcheck-* orfes són inerts; modificar client.css va causar una desviació documentada a 04-02).
  </action>
  <verify>
    <automated>npm run build</automated>
    <automated>! rtk proxy grep -rIn "buildOwnDetail" src/client/client.js || (echo "buildOwnDetail encara present" && false)</automated>
    <human-check>Amb el servidor en marxa i 2 equips: a la fase CSS, cada equip veu el botó "Finalitzar" sempre clicable; en prémer-lo, els seus controls CSS es bloquegen però el timer global i l'altre equip continuen. Igual a la fase JS (selects i botons "Afegir/Veure" bloquejats). En finalitzar la partida, la pantalla de resultats de tots (equips + Admin) mostra NOMÉS el rànquing + el percentatge global propi, sense cap llista de sub-checks HTML/CSS/JS.</human-check>
  </verify>
  <done>npm run build compila sense errors. El botó "Finalitzar" apareix i és sempre clicable a CSS i JS (sense gate); en prémer-lo el panell corresponent queda congelat (F5-safe via isPhaseDone). El detall de sub-checks ha desaparegut de la pantalla de resultats; rànquing + percentatge global es conserven. Funcions/constants/imports orfes eliminats; client.css sense tocar.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Checkpoint humà — congelació voluntària CSS/JS (D-15) + resultats sense detall (D-16)</name>
  <action>Gate de verificació humana (no automatitzable): l'executor atura i demana al desenvolupador que validi end-to-end la congelació voluntària i la pantalla de resultats segons els passos de <how-to-verify>. No continuïs fins rebre "approved".</action>
  <what-built>Congelació voluntària CSS/JS (D-15) end-to-end i pantalla de resultats sense detall de sub-checks (D-16): servidor (gameState freeze + markPhaseDone afluixat + ownDetail retirat), client (botó voluntari + panell congelat + resultats nets) i tests actualitzats.</what-built>
  <how-to-verify>
    1. Arrenca el servidor (npm run dev o l'script habitual del projecte) i obre Admin + 2 pestanyes d'equip.
    2. Registra 2 equips, inicia la fase HTML, avança fins a CSS.
    3. A un equip, toca alguns forats CSS i prem "Finalitzar" → els seus controls CSS es bloquegen; l'altre equip pot seguir editant i el timer global no canvia.
    4. Prova de forçar edició després de congelar (recarrega F5 l'equip congelat) → segueix bloquejat i "✓ Fet" (freeze server-side).
    5. Avança a JS, construeix alguna regla en un equip i prem "Finalitzar" → selects i botons "Afegir JavaScript"/"Veure" bloquejats en aquell equip.
    6. Finalitza la partida des de l'Admin (deixa passar la cerimònia 04-03) → a TOTES les pantalles (equips + Admin) la classificació mostra NOMÉS el rànquing + el percentatge global propi, sense cap secció "El teu detall" / sub-checks.
    7. Confirma que un equip que MAI ha premut "Finalitzar" a HTML no rep bonus de temps i que el rànquing es manté coherent (el temps segueix sent exclusiu de HTML).
  </how-to-verify>
  <resume-signal>Escriu "approved" o descriu els problemes trobats</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → server (Socket.io events) | `team:mark-done`, `team:set-css`, `team:set-rules` són intents no fiables; la identitat SEMPRE ve de `socket.data.teamId`, mai del payload. |
| server → client (broadcasts) | El payload de resultats no ha de filtrar dades privades entre equips. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-15 | Tampering | setCssValue / setJsRules (gameState.js) | high | mitigate | Guard server-side: un cop `team.doneAt.css`/`.js` existeix, setCssValue/setJsRules retornen false i no muten. Un payload `team:set-css`/`team:set-rules` forjat després de "Finalitzar" no pot descongelar l'equip; el freeze viu al mutator autoritatiu, no al client (Task 1). Verificat per test VOLUNTARY-FREEZE-D15. |
| T-04-16 | Tampering | markPhaseDone (gameState.js) | medium | mitigate | La fase es deriva de `state.phase` al handler TEAM_MARK_DONE (mai del payload). markPhaseDone rebutja fases fora de PHASE_ORDER i conserva el gate `isHtmlComplete` per a HTML; doneAt.css/js són inertes per al scoring perquè htmlTimeBonuses només llegeix doneAt.html — un equip no pot inflar el seu score marcant CSS/JS "fet". |
| T-04-17 | Information Disclosure | payload CEREMONY_START / GAME_RESULTS | low | accept | D-16 elimina ownDetail: el broadcast passa a un únic `io.to('session')` amb NOMÉS `{ranking}` públic ({id,name,globalPct}). En retirar el detall privat, la superfície de fuita entre equips desapareix; el test assert que cap payload conté subchecks. |
| T-04-SC | Tampering | npm/pip/cargo installs | low | accept | Cap `npm install` nou en aquest pla (lògica pura + render sobre dependències ja instal·lades); res a auditar. |
</threat_model>

<verification>
- `node --test test/results.test.js test/scoring.test.js` verd (nous invariants D-15 + absència d'ownDetail D-16; scoring html-only intacte).
- `npm test` sense regressions a la resta de la suite (Fases 1-3 + 04-01/04-02).
- `npm run build` compila admin + client sense errors.
- Grep-gate: `getTeamSubchecks` i `buildOwnDetail` sense referències residuals (dead-code hygiene).
- Checkpoint humà: congelació per-equip end-to-end + pantalla de resultats neta a totes les pantalles.
</verification>

<success_criteria>
- CSS i JS tenen botó "Finalitzar" voluntari sense gate; en prémer-lo l'equip queda congelat (server-side) sense afectar el timer global ni els altres equips (D-15).
- doneAt.css/doneAt.js mai alteren cap puntuació (bonus de temps exclusiu de HTML; scoring.js sense tocar).
- La pantalla de resultats (equips + Admin) mostra NOMÉS rànquing + percentatge global; cap detall de sub-checks (D-16); el servidor no emet ownDetail.
- El gate HTML D-07 (isHtmlComplete) i la cerimònia 04-03 romanen intactes.
- Codi mort eliminat (getTeamSubchecks, buildOwnDetail/buildSubcheck*/HTML_SLOT_LABELS, imports orfes); client.css sense tocar.
</success_criteria>

<output>
Create `.planning/phases/04-puntuaci-i-r-nquing-final/04-04-SUMMARY.md` when done
</output>
