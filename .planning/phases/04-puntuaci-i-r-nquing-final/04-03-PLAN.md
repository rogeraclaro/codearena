---
phase: 04-puntuaci-i-r-nquing-final
plan: 03
type: execute
wave: 3
depends_on: ['04-02']
files_modified:
  - src/client/client.js
  - src/client/admin.js
  - src/client/client.css
autonomous: false
requirements: [ADMIN-07]
must_haves:
  truths:
    - "En finalitzar, totes les pantalles (equips + Admin) reprodueixen la cerimònia — compte enrere 5→0, hold 3s a 0, revelació invers últim→primer, confetti — abans de revelar el rànquing (D-14)"
    - "La cerimònia és idèntica i sincronitzada a totes les pantalles perquè la dispara UN sol broadcast (CEREMONY_START), no temporitzadors per client"
    - "Els colors chillón apareixen NOMÉS dins .ceremony-overlay i no persisteixen mai al rànquing; sota prefers-reduced-motion la cerimònia degrada a estat final sense animació"
  artifacts:
    - src/client/client.js
    - src/client/admin.js
    - src/client/client.css
  key_links:
    - "un únic CEREMONY_START → cadena setTimeout idèntica (taula ms d'UI-SPEC) → teardown de l'overlay → pantalla de resultats ja renderitzada (Pla 01)"
    - ".ceremony-overlay es desmunta en completar la revelació → cap color chillón sobreviu (invariant de contenció)"
---

<objective>
Refinament de presentació de la Fase 4: intercepta el broadcast `CEREMONY_START` (creat al Pla 01) i, abans de revelar el rànquing, reprodueix la cerimònia d'entrega de premis D-14 a TOTES les pantalles alhora (equips + Admin): compte enrere animat 5→0 (zoom+fade, un color chillón per número), hold estàtic de 3s a 0, revelació del rànquing en ordre invers (últim→primer amb stagger + pausa dramàtica abans del #1) i confetti a pantalla completa dependency-free. En completar, l'overlay es desmunta i cada pantalla queda a la vista de resultats ja renderitzada al Pla 01.

Purpose: És el "moment de celebració" pedagògic (D-14) que fa aterrar el debrief en una sessió de 15-20 min; l'única animació deliberadament pesada del producte.
Output: Overlay de cerimònia compartit a client i admin, keyframes + confetti a CSS, i degradació sota reduced-motion.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-puntuaci-i-r-nquing-final/04-CONTEXT.md
@.planning/phases/04-puntuaci-i-r-nquing-final/04-RESEARCH.md
@.planning/phases/04-puntuaci-i-r-nquing-final/04-PATTERNS.md
@.planning/phases/04-puntuaci-i-r-nquing-final/04-UI-SPEC.md
@.planning/phases/04-puntuaci-i-r-nquing-final/04-01-SUMMARY.md
@.planning/phases/04-puntuaci-i-r-nquing-final/04-02-SUMMARY.md
</context>

<artifacts_this_phase_produces>
- `src/client/client.js`: mòdul/funció de cerimònia compartida (overlay + compte enrere + revelació invers), interceptant `CEREMONY_START` per animar abans de renderitzar resultats; `fireConfetti(container)` dependency-free.
- `src/client/admin.js`: reutilitza la mateixa cerimònia, disparada pel mateix `CEREMONY_START` (lockstep amb els equips).
- `src/client/client.css`: `.ceremony-overlay`, `.ceremony-count`, `.ceremony-confetti`, `.ceremony-confetti__piece`, keyframes (zoom+fade del compte enrere, fall del confetti) dins `@media (prefers-reduced-motion: no-preference)`.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Overlay de cerimònia compartit — compte enrere + revelació invers (D-14)</name>
  <files>src/client/client.js, src/client/admin.js, src/client/client.css</files>
  <read_first>
    - src/client/client.js (el listener `socket.on(EVENTS.CEREMONY_START, ...)` creat al Pla 01 — ara l'ha d'interceptar per animar; la branca de resultats a `renderScreenForState`; el patró de teardown `clearApp()`)
    - src/client/admin.js (el listener `CEREMONY_START` / render final creat al Pla 01; `renderAdmin` línia 405)
    - src/client/client.css (`@keyframes js-rule-enter` + bloc reduced-motion línies 544-557 — patró exacte a copiar; capçalera token-only)
    - 04-UI-SPEC.md §Motion Contract §Award ceremony timing (taula ms EXACTA: 5-1 a 1000ms cadascun, 0 hold 3000ms, 0 exit 800ms, revelació stagger 600ms + pausa 1200ms abans del #1) i §Color §Award ceremony palette exception (colors chillón: 5 #FF1E56, 4 #FF9F1C, 3 #FFD60A, 2 #2EC4B6, 1 #3A86FF, 0 #C77DFF) i §Component Inventory §Award Ceremony
    - 04-RESEARCH.md §Code Examples (confetti dependency-free) i §Anti-Patterns ("Driving the ceremony from per-client timers")
    - 04-PATTERNS.md §src/client/client.js (cerimònia per un sol broadcast), §src/client/client.css (keyframe + reduced-motion wrapper)
  </read_first>
  <action>
    Crea una funció de cerimònia compartida (defineix-la a `client.js` i reutilitza-la a `admin.js`, o extreu un petit helper importat per tots dos) que rebi el payload `{ ranking }` de `CEREMONY_START` i executi una única cadena `setTimeout` scriptada segons la taula ms d'UI-SPEC — MAI un temporitzador que cada client arrenqui independentment (Anti-Pattern RESEARCH: un sol broadcast → totes les pantalles en lockstep).

    Seqüència: munta `.ceremony-overlay` full-viewport per sobre de tot; mostra `.ceremony-count` centrat, un número a la vegada (5→4→3→2→1) durant 1000ms cadascun amb l'animació zoom-in + fade-out (scale creix per sobre d'1 mentre opacity→0) en el color chillón corresponent; el 0 apareix i es manté ESTÀTIC (sense zoom) 3000ms, després surt amb el mateix zoom+fade 800ms; llavors revela el rànquing en ordre INVERS (últim classificat primer → #1 últim) reutilitzant les files `.rank-row` (amb `Trophy` al #1) amb stagger fix de 600ms i UNA pausa de 1200ms just abans del #1. En completar la revelació, desmunta `.ceremony-overlay` (invariant de contenció: cap color chillón sobreviu) — la pantalla queda a la vista de resultats ja renderitzada al Pla 01.

    A `client.js`: modifica el handler `CEREMONY_START` perquè, en lloc de renderitzar resultats directament (comportament del Pla 01), executi primer la cerimònia i, en acabar, mostri la pantalla de resultats. El handler `GAME_RESULTS` (F5/reconnexió) segueix renderitzant DIRECTAMENT sense cerimònia (Pitfall 4 — no repeteixis la cerimònia a qui es reconnecta).

    A `admin.js`: dispara la mateixa cerimònia amb el mateix `CEREMONY_START`; en acabar, mostra el rànquing final de l'Admin (creat al Pla 01). Admin i equips queden sincronitzats perquè comparteixen el broadcast.

    Colors chillón: literals NOMÉS dins l'àmbit de `.ceremony-overlay` (mai a `tokens.css`, mai a cap altra superfície — invariant de contenció d'UI-SPEC).
  </action>
  <verify>
    <automated>npm run build && node --test test/results.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` compila admin + client.
    - `node --test test/results.test.js` continua verd (la cerimònia no altera el contracte de dades del servidor).
    - La cerimònia es dispara des d'UN sol `CEREMONY_START`; no hi ha temporitzadors per-client independents.
    - `GAME_RESULTS` (reconnexió) renderitza resultats sense cerimònia.
    - Els literals chillón apareixen NOMÉS dins l'àmbit de `.ceremony-overlay`; no s'afegeixen a `tokens.css`.
  </acceptance_criteria>
  <done>En finalitzar, totes les pantalles reprodueixen el compte enrere i la revelació invers en lockstep, i acaben a la vista de resultats sense que cap color de cerimònia hi persisteixi.</done>
</task>

<task type="auto">
  <name>Task 2: Confetti dependency-free + keyframes CSS + degradació reduced-motion</name>
  <files>src/client/client.js, src/client/client.css</files>
  <read_first>
    - src/client/client.css (`@keyframes js-rule-enter` + `@media (prefers-reduced-motion: no-preference)` línies 544-557; capçalera token-only)
    - src/client/client.js (funció de cerimònia de la Task 1; import d'icones línia ~9)
    - 04-RESEARCH.md §Code Examples (cos exacte de `fireConfetti` amb guard `matchMedia('(prefers-reduced-motion: reduce)')`)
    - 04-UI-SPEC.md §Motion Contract (§Confetti implementation note; §Reduced motion — la cerimònia col·lapsa a estat final sense zoom/confetti)
  </read_first>
  <action>
    A `client.js`: implementa `fireConfetti(container, count = 60)` dependency-free (dotzenes de divs `.ceremony-confetti__piece` posicionats absolutament amb `left`/`background`/`animationDelay` aleatoris, del set de sis colors chillón), disparada quan aterra la fila #1 de la revelació; les peces es netegen soles quan es desmunta `.ceremony-overlay`. Guard inicial `if (matchMedia('(prefers-reduced-motion: reduce)').matches) return;`.

    A `client.css`: afegeix `.ceremony-overlay` (full-viewport, per sobre de tot, canvas `--color-bg` o scrim opcional), `.ceremony-count` (Display scale, tabular-nums, centrat), `.ceremony-confetti` i `.ceremony-confetti__piece`, i els keyframes (zoom+fade del compte enrere; `fall` del confetti translateY -10vh→110vh + rotate ~4s). EMBOLICA tota la motion de cerimònia (zoom/fade, stagger de revelació, fall de confetti) dins `@media (prefers-reduced-motion: no-preference)` (còpia l'estructura de les línies 544-557). Sota `prefers-reduced-motion: reduce`: salta zoom/fade i confetti — la seqüència col·lapsa (cada número com a frame estàtic d'1s sense escala, o salt directe al rànquing) i el rànquing es renderitza al seu estat final sense stagger. Els colors chillón viuen NOMÉS dins `.ceremony-overlay` (cap token nou).
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` compila.
    - `fireConfetti` no usa cap dependència npm (dependency-free).
    - Tota la motion de cerimònia està dins `@media (prefers-reduced-motion: no-preference)`; sota reduced-motion no hi ha zoom ni confetti i el rànquing apareix al seu estat final.
    - Els sis colors chillón (`#FF1E56`/`#FF9F1C`/`#FFD60A`/`#2EC4B6`/`#3A86FF`/`#C77DFF`) apareixen només dins `.ceremony-overlay` i no s'afegeixen a `tokens.css`.
  </acceptance_criteria>
  <done>El confetti i tota l'animació de cerimònia són dependency-free, continguts a l'overlay, i degraden netament sota reduced-motion sense perdre cap informació.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Checkpoint humà — verificació visual de la cerimònia D-14</name>
  <action>Gate de verificació humana (no automatitzable): l'executor atura i demana al desenvolupador que validi visualment la cerimònia segons els passos de <how-to-verify>. No continuïs fins rebre "approved".</action>
  <what-built>
    La cerimònia d'entrega de premis D-14 completa: compte enrere 5→0 animat (zoom+fade, colors chillón), hold de 3s a 0, revelació del rànquing en ordre invers (últim→primer amb pausa abans del #1), i confetti final — sincronitzada a totes les pantalles (equips + Admin) des d'un sol broadcast, seguida de la pantalla de resultats.
  </what-built>
  <how-to-verify>
    1. Arrenca l'app: `npm run server` (i `npm run dev` si cal per als assets, o `npm run build` primer). Obre el panell Admin i 2-3 pestanyes d'equip.
    2. Registra equips, juga (o avança ràpidament) fins a la fase JS, i prem "Finalitzar i Mostrar Resultats" a l'Admin; confirma el diàleg.
    3. Verifica a TOTES les pantalles alhora: el compte enrere 5→4→3→2→1 (1s cadascun, zoom+fade, cada número d'un color viu diferent), el 0 quiet 3s, després la revelació del rànquing de l'últim al primer amb una pausa dramàtica abans del #1 (amb icona Trophy), i confetti a pantalla completa.
    4. Verifica que, en acabar, cap color chillón queda a la pantalla de resultats (rànquing en paleta neutra; misses en gris `--color-muted`, no vermell) i que cada equip veu NOMÉS el seu propi detall.
    5. Activa "reduce motion" al SO/navegador i repeteix: la cerimònia ha de degradar (sense zoom/confetti) i mostrar el rànquing final directament.
    6. Fes F5 en una pestanya d'equip després dels resultats: ha de tornar als resultats directament, SENSE repetir la cerimònia.
  </how-to-verify>
  <resume-signal>Escriu "approved" o descriu els problemes observats (timing, colors que persisteixen, desincronització entre pantalles, privacitat de detall, degradació reduced-motion).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| servidor→client | La cerimònia és pura presentació client-side; no s'introdueix cap nou input de client ni cap càlcul autoritatiu |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-07 | Information Disclosure | overlay de revelació reutilitza les files de rànquing | low | mitigate | La revelació reutilitza NOMÉS el `ranking` públic (`{id,name,globalPct}`) del payload; el detall privat (ownDetail) es renderitza a la pantalla de resultats post-cerimònia, no a l'overlay compartit |
| T-04-05 | Denial of Service | cadena setTimeout / munt de nodes de confetti | low | mitigate | Confetti amb comptador fix (~60 peces) i auto-neteja al desmuntar l'overlay; timeline acotada (~12-17s, no rellançable sense un nou finalize idempotent) |
| T-04-SC | Tampering | instal·lacions npm/pip/cargo | low | accept | N/A — confetti i cerimònia dependency-free; cap `npm install` nou |
</threat_model>

<verification>
- `npm run build` compila.
- `node --test test/results.test.js` verd (la cerimònia no toca el contracte de dades).
- Checkpoint humà: cerimònia sincronitzada, contenció de colors chillón, degradació reduced-motion, i F5 sense repetir cerimònia — verificats visualment.
</verification>

<success_criteria>
- En finalitzar, totes les pantalles reprodueixen la cerimònia D-14 sincronitzada abans del rànquing (D-14, ADMIN-07).
- Els colors chillón queden continguts a l'overlay efímer i no persisteixen al rànquing (invariant de contenció).
- Sota prefers-reduced-motion la cerimònia degrada sense perdre informació.
- La reconnexió (F5) mostra resultats directament sense repetir la cerimònia.
</success_criteria>

<output>
Crea `.planning/phases/04-puntuaci-i-r-nquing-final/04-03-SUMMARY.md` en acabar.
</output>
