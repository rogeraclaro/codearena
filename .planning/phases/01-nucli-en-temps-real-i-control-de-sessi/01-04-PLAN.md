---
phase: 01-nucli-en-temps-real-i-control-de-sessi
plan: 04
type: execute
wave: 3
depends_on: ["01-02"]
files_modified:
  - src/client/client.js
  - src/client/client.css
test_note: "Sense test/*.js nou — superfície purament de render de client; verificat per build + checkpoint humà (UI-SPEC)."
autonomous: false
requirements: [GAME-01, GAME-02, UX-01, UX-02]

must_haves:
  truths:
    - "La pantalla d'equip té els estats mutuament exclusius: espera (D-05), interstici entre fases (D-06), split actiu (panell esquerra + preview dreta), i congelat a zero (D-11)"
    - "En fase activa la pantalla es divideix: panell d'acció a l'esquerra i preview en un iframe aïllat (sandbox) a la dreta (GAME-01, GAME-02)"
    - "El llenguatge visual és consistent amb iconografia clara, text mínim i codificació de color de fase HTML/CSS/JS (UX-01, UX-02)"
  artifacts:
    - "Màquina d'estats de pantalla a src/client/client.js (waiting/interstitial/active/frozen)"
    - "src/client/client.css (estils de la pantalla d'equip via tokens)"
    - "iframe de preview amb sandbox i srcdoc buit (closca reutilitzable per Fase 2)"
  key_links:
    - "session:full-state.phase===null → waiting; canvi de phase → interstitial ~1.2s → active split; timerStatus==='frozen' → overlay de bloqueig"
    - "iframe sandbox srcdoc buit ara; Fase 2 hi injectarà contingut (amb DOMPurify)"
---

<objective>
Construir els estats de la pantalla d'equip: la pantalla d'espera dedicada abans de començar (D-05), l'interstici breu entre fases (D-06), el layout split actiu (panell d'acció a l'esquerra + preview iframe aïllat a la dreta, GAME-01/GAME-02), i l'estat congelat a zero (D-11). La preview és una closca buida a la Fase 1 però amb l'estructura que reutilitzaran les Fases 2-5.

Purpose: fer cert el criteri d'èxit 5 del ROADMAP (pantalla d'equip amb split panell/preview, llenguatge visual consistent, iconografia clara, text mínim) i completar el criteri 3 (recuperar exactament l'estat inclou aquests estats de pantalla).
Output: pantalla d'equip completa amb els seus 4 estats, dirigida per l'estat autoritatiu del servidor.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-nucli-en-temps-real-i-control-de-sessi/SKELETON.md
@.planning/phases/01-nucli-en-temps-real-i-control-de-sessi/01-UI-SPEC.md
@.planning/phases/01-nucli-en-temps-real-i-control-de-sessi/01-CONTEXT.md
@.planning/phases/01-nucli-en-temps-real-i-control-de-sessi/01-01-SUMMARY.md
@.planning/phases/01-nucli-en-temps-real-i-control-de-sessi/01-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Màquina d'estats de la pantalla d'equip + layout split amb preview aïllada</name>
  <read_first>
    - 01-UI-SPEC.md (§Layout Contract → Team screen states 1 waiting / 2 interstitial / 3 active split ~40%/60% / frozen overlay; §Typography → team name Heading, secondary Body; §Color → phase color-coding + timer urgency; §Copywriting → "Connectat ✓, esperant el professor", "Ara: Fase {HTML|CSS|JS}")
    - 01-CONTEXT.md (D-05 espera, D-06 interstici ~1-2s, D-07 fase global, D-11 congelat)
    - src/client/client.js (boot + token + waiting + countdown existents dels Plans 01/02)
    - src/client/shared/tokens.css i shared/timer.js
  </read_first>
  <files>src/client/client.js, src/client/client.css</files>
  <action>
    A `client.js` implementa una màquina d'estats de pantalla derivada NOMÉS de `session:full-state` (l'estat autoritatiu; el client no decideix res):
    - `waiting` (D-05): quan `phase===null` — pantalla completa centrada, nom de l'equip (Heading) + "Connectat ✓, esperant el professor" (Body muted). Sense split.
    - `interstitial` (D-06): quan la `phase` rebuda canvia respecte a l'anterior — pantalla completa ~1.2s amb "Ara: Fase {HTML|CSS|JS}" usant el color de fase (`--phase-html/css/js`, UX-02), després revela l'estat `active`. Si en reconnectar ja hi ha fase activa (mateixa phase), salta l'interstici i va directe a `active` (recuperació neta, sense flicker).
    - `active`: layout split via CSS grid ~40% panell d'acció (esquerra) / ~60% preview (dreta). El panell mostra el badge de fase (color) + el compte enrere (via `shared/timer.js`, mateix valor que l'admin). La preview és un `<iframe sandbox="allow-same-origin" srcdoc="">` (closca BUIDA a la Fase 1; Fase 2 hi injectarà contingut amb DOMPurify). El contingut de joc del panell NO és d'aquesta fase.
    - `frozen` (D-11): quan `timerStatus==='frozen'` — sobreposa un overlay de bloqueig d'opacitat reduïda + icona de cadenat (Lucide, sense text) sobre el split; la feina queda visible "tal com està"; no canvia de fase.
    Crea `src/client/client.css` amb els estils d'aquests estats usant EXCLUSIVAMENT tokens (`var(--space-*)`, colors, `--phase-*`); importa'l des de `client.html` o `client.js`. Objectius de clic mínim 44px. Text mínim, iconografia Lucide (UX-01). Cap literal de color/mida.
  </action>
  <verify>
    <automated>cd "$PWD" && npm run build && grep -c "sandbox" src/client/client.js && grep -c "srcdoc" src/client/client.js && grep -c "var(--phase-" src/client/client.css</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` verd.
    - `client.js` conté un `<iframe>` amb atribut `sandbox` i `srcdoc` (grep positiu) per a la preview aïllada (GAME-02).
    - Els 4 estats es deriven de `session:full-state` (phase null → waiting; canvi de phase → interstitial; phase activa → split; timerStatus frozen → overlay).
    - `client.css` referencia colors de fase via `var(--phase-*)` i no conté literals hex (grep negatiu de `#` a valors de color).
    - Layout split via grid ~40%/60% (panell esquerra, preview dreta) — GAME-01.
  </acceptance_criteria>
  <done>La pantalla d'equip transita correctament entre espera, interstici, split actiu i congelat, tot dirigit per l'estat autoritatiu del servidor, amb preview aïllada llesta per a la Fase 2.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verificació visual dels estats de la pantalla d'equip (UI-SPEC)</name>
  <action>Guia l'humà per la verificació visual dels 4 estats de la pantalla d'equip contra el UI-SPEC; espera la seva confirmació o llista de desviacions abans de tancar el pla.</action>
  <what-built>Els 4 estats de la pantalla d'equip: espera, interstici, split actiu amb preview, i congelat.</what-built>
  <how-to-verify>
    1. `npm run server` + `npm run dev`. Registra i tria un equip → veus la pantalla d'espera (nom + "Connectat ✓, esperant el professor"), sense split.
    2. A l'admin, "Iniciar Fase" (HTML) amb una durada curta (ex: 15s) → l'equip mostra breument "Ara: Fase HTML" (color taronja) i després el split: panell esquerra + preview (iframe buit) dreta.
    3. Comprova el compte enrere sincronitzat entre admin i equip; als ≤10s el timer es torna vermell (pols, o només color si `prefers-reduced-motion`).
    4. Deixa arribar a zero → l'equip es congela (overlay + cadenat), sense canviar de fase.
    5. "Següent fase" (CSS) → interstici blau "Ara: Fase CSS", després split de nou.
    6. F5 durant una fase activa → recupera directament el split de la fase actual (sense re-triar, sense interstici duplicat).
    7. Verifica coherència visual amb UI-SPEC: tipografia Inter, colors de fase, iconografia Lucide, text mínim, targets ≥44px.
  </how-to-verify>
  <resume-signal>Escriu "aprovat" o descriu les desviacions respecte al UI-SPEC.</resume-signal>
  <acceptance_criteria>
    - Els 4 estats es mostren correctament i la codificació de color de fase és consistent (UX-02).
    - F5 en fase activa recupera l'estat exacte (SC3).
    - Coherència amb el UI-SPEC (tipografia, color, espaiat, iconografia).
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Contingut de preview → DOM del client | L'iframe de preview aïlla el contingut futur (Fase 2+) del document de l'equip |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-01 | Tampering (XSS) | iframe de preview | medium | mitigate | Preview dins `<iframe sandbox>` amb `srcdoc` (aïllament del document principal); a la Fase 1 el `srcdoc` és buit. DOMPurify s'afegirà a la Fase 2 quan s'hi injecti contingut real (documentat a SKELETON.md, no s'instal·la aquí). |
| T-04-02 | Tampering (XSS) | render del nom d'equip a la pantalla | low | mitigate | Nom inserit via `textContent` (patró del Pla 01), mai `innerHTML`. |
| T-04-03 | Spoofing | estat de pantalla derivat al client | low | accept | El client només renderitza l'estat que li dona el servidor; no pot avançar de fase ni descongelar-se per si mateix (l'estat autoritatiu viu al servidor). |
</threat_model>

<verification>
- `npm run build` verd.
- iframe de preview amb sandbox + srcdoc buit present.
- Checkpoint visual dels 4 estats aprovat (UI-SPEC).
</verification>

<success_criteria>
- SC5 del ROADMAP: pantalla d'equip amb split panell d'acció (esquerra) + preview (dreta), llenguatge visual consistent, iconografia clara, text mínim.
- Completa SC3: la recuperació per F5 restaura també l'estat de pantalla de la fase activa.
</success_criteria>

<output>
Create `.planning/phases/01-nucli-en-temps-real-i-control-de-sessi/01-04-SUMMARY.md` when done
</output>
