---
phase: quick-260707-rz1
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/client/client.js
  - src/client/client.css
autonomous: true
requirements:
  - 260707-rz1-treure-boto-veure-js
must_haves:
  truths:
    - "El botó 'Veure' ja no apareix a cap fila de regla de la Fase JS"
    - "El flux de preview complet de la Fase JS (entrada de fase, F5/resync) segueix funcionant igual"
    - "No queda codi ni CSS mort referent al botó 'Veure'"
  artifacts:
    - src/client/client.js
    - src/client/client.css
  key_links:
    - "rebuildJsPreview segueix cridant-se des dels dos punts de refresc complet (línies ~1591, ~1837) amb el comportament per defecte"
---

<objective>
Treure el botó "Veure" de la UI del constructor de regles de la Fase JS (CodeArena) i eliminar tot el codi/CSS/comentaris que queden morts com a conseqüència directa i inequívoca d'aquesta eliminació.

Purpose: El botó permetia previsualitzar l'efecte d'una sola regla JS abans d'acabar la fase; el propietari vol treure'l. La higiene de codi mort (mandat de CLAUDE.md) obliga a eliminar també el codi inabastable resultant.
Output: `src/client/client.js` i `src/client/client.css` sense cap rastre del botó "Veure", conservant intacte tota la resta de la lògica de la Fase JS.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@src/client/client.js
@src/client/client.css

# Fets verificats durant la planificació (grep):
# - previewSingleRule (client.js ~1420) té UN sol cridador: el listener del botó "Veure" (línia 1337). Queda mort en treure el botó.
# - rebuildJsPreview (client.js ~1405) es crida des de 3 punts: previewSingleRule (immediate:true, s'elimina) i línies ~1591 i ~1837 (per defecte, immediate=false). NO s'ha de tocar la seva firma pública tret del paràmetre `immediate`, que és EXCLUSIU de "Veure".
# - El paràmetre `immediate`, el bloc `if (immediate) rules.forEach(...)` (línia 1414) i el paràgraf de comentari que el descriu (~1401-1404) queden morts en treure previewSingleRule.
# - isJsRowComplete i normalizeJsRow segueixen VIUS via línia 1243 (rules = jsPanelRows.filter(isJsRowComplete).map(normalizeJsRow)). NO eliminar-los.
# - CSS: .js-rule__veure, :focus-visible, :disabled (client.css ~500-518) queden morts. Comentaris amb "Veure" a client.css línies 444 i 459.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Treure el botó "Veure" i el codi JS mort resultant de client.js</name>
  <files>src/client/client.js</files>
  <action>
    A `buildJsRuleRow` (~línies 1332-1338): eliminar tot el bloc que crea el botó `veure` (des de `const veure = document.createElement('button');` fins a `el.appendChild(veure);` inclòs). NO tocar el bloc `remove` que ve just després ni cap altra part de la funció.

    Eliminar completament la funció `previewSingleRule(row)` (~línies 1418-1423) juntament amb el seu comentari de capçalera `// "Veure" (D-12): preview client-only...` (~línies 1418-1419), ja que el seu únic cridador era el botó que s'acaba de treure.

    A `rebuildJsPreview` (~línies 1405-1416): com que previewSingleRule era l'únic que passava `immediate: true`, netejar el paràmetre ara mort: (a) canviar la firma de `function rebuildJsPreview(rules, { immediate = false } = {})` a `function rebuildJsPreview(rules)`; (b) eliminar la línia `if (immediate) rules.forEach((r) => applyAction(doc, r));` (~1414); (c) eliminar del comentari de capçalera de la funció NOMÉS el paràgraf que descriu `immediate=true` (~línies 1401-1404, la frase que comença amb "`immediate=true` (nomes des de \"Veure\"...") — conservar la resta del comentari (Rebuild-then-reattach / Pitfall 3 / iframe scriptless), que descriu comportament encara vigent.

    NO tocar: isJsRowComplete, normalizeJsRow (segueixen vius via línia ~1243), les crides a rebuildJsPreview de ~1591 i ~1837, ni cap lògica D-11/D-12/D-15 d'anti-repetició/composite.
  </action>
  <verify>
    <automated>cd /Users/rogermasellas/AI/Impartició01 && grep -c "previewSingleRule\|js-rule__veure\|immediate" src/client/client.js | grep -qx 0 && npm run build</automated>
  </verify>
  <done>El botó "Veure" ja no es crea, previewSingleRule no existeix, rebuildJsPreview no té paràmetre immediate, i `grep -c "previewSingleRule\|js-rule__veure\|immediate" src/client/client.js` retorna 0. `npm run build` acaba sense errors.</done>
</task>

<task type="auto">
  <name>Task 2: Treure el CSS mort i actualitzar els comentaris de client.css</name>
  <files>src/client/client.css</files>
  <action>
    Eliminar completament els tres blocs de regla del botó "Veure" (~línies 499-518): el comentari `/* "Veure": afordança neutra... */`, `.js-rule__veure { ... }`, `.js-rule__veure:focus-visible { ... }` i `.js-rule__veure:disabled { ... }`.

    Actualitzar NOMÉS les referències a "Veure" dins dels comentaris veïns, sense reescriure la resta del text:
    - Comentari ~línia 444: `Els desplegables i "Veure" usen estil natiu/neutre...` → treure la menció a "Veure" (p. ex. `Els desplegables usen estil natiu/neutre...`), conservant intacta la resta de l'explicació sobre --color-text / --phase-js.
    - Comentari ~línia 459: `Fila de regla: frase natural amb 4 desplegables natius + "Veure" + retirar.` → treure `+ "Veure"` (queda `... amb 4 desplegables natius + retirar.`).

    NO tocar: .js-rules, .js-rule, .js-rule__word, .js-rule__select (+ estats), .js-rule__remove ni cap altre estil.
  </action>
  <verify>
    <automated>cd /Users/rogermasellas/AI/Impartició01 && grep -ci "veure" src/client/client.css | grep -qx 0 && npm run build</automated>
  </verify>
  <done>Cap regla `.js-rule__veure` ni cap menció a "Veure" queda a client.css (`grep -ci "veure" src/client/client.css` retorna 0). Els altres estils de la fila de regla romanen intactes. `npm run build` acaba sense errors.</done>
</task>

</tasks>

<threat_model>
Eliminació de codi/UI existent, sense noves dependències, entrades, ni límits de confiança nous. No s'introdueix superfície d'atac; treure el botó de preview client-only redueix lleugerament la superfície (menys DOM/handlers). Cap amenaça STRIDE nova aplicable.
</threat_model>

<verification>
- `npm run build` (Vite) acaba sense errors després dels dos canvis.
- `grep -c "previewSingleRule\|js-rule__veure\|immediate" src/client/client.js` retorna 0.
- `grep -ci "veure" src/client/client.css` retorna 0.
- Revisió manual: el flux de preview complet de la Fase JS (rebuildJsPreview des de ~1591/~1837) es manté sense canvis de comportament.
</verification>

<success_criteria>
- El botó "Veure" no apareix a cap fila de regla de la Fase JS.
- No queda codi JS ni CSS mort referent a "Veure".
- La resta de la lògica de la Fase JS (afegir/treure fila, D-11/D-12/D-15, preview complet) funciona igual.
- El build passa net.
</success_criteria>

<output>
Create `.planning/quick/260707-rz1-treure-el-boto-veure-de-la-fase-js/260707-rz1-SUMMARY.md` when done
</output>
