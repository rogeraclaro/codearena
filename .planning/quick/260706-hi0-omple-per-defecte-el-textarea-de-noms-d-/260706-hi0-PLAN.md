---
phase: quick-260706-hi0
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/client/admin.js
autonomous: true
requirements: []
must_haves:
  truths:
    - "En obrir l'Admin abans de registrar, el textarea #team-names-input ja mostra els 4 noms reals d'equip, un per línia"
    - "L'admin pot editar, esborrar o triar entre els noms pre-omplerts abans de prémer Registrar"
  artifacts:
    - src/client/admin.js
  key_links:
    - "textarea.value assignat a buildRegistrationBlock (~línia 488), sense tocar el handler de Registrar que fa split('\\n')"
---

<objective>
Pre-omplir per defecte el textarea de noms d'equips a l'Admin amb els 4 noms reals de l'equip, un per línia, perquè l'admin no els hagi de teclejar cada sessió.

Purpose: Reduir la fricció operativa a l'inici de la classe — l'admin obre el panell i els noms ja hi són, només ha de revisar-los i prémer Registrar.
Output: `src/client/admin.js` amb `textarea.value` pre-omplert a `buildRegistrationBlock`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@src/client/admin.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pre-omplir el textarea de noms amb els 4 noms reals</name>
  <files>src/client/admin.js</files>
  <action>
A la funció buildRegistrationBlock (~línia 485-488), després de crear el `textarea` i assignar `textarea.placeholder`, afegeix una assignació `textarea.value` amb els 4 noms d'equip units per '\n' (un per línia), en aquest ordre EXACTE:
1. Els primers no sempre tenen perquè ser els últims
2. Segundas filas siempre fueron buenas
3. Que siguem de tercera fila NO té perquè ser negatiu!
4. Los ultimos de las filas

Manté el `placeholder` existent tal com està (serveix de guia si l'admin ho esborra tot). NO toquis el handler del botó Registrar: ja fa `textarea.value.split('\n')`, filtra buits i emet `admin:register-teams`; el pre-omplert és compatible perquè l'admin pot editar el camp abans de prémer. Defineix els noms com una constant local llegible (p.ex. un array unit amb `.join('\n')`) per claredat.
  </action>
  <verify>
    <automated>node -e "const s=require('fs').readFileSync('src/client/admin.js','utf8'); const names=['Els primers no sempre tenen perquè ser els últims','Segundas filas siempre fueron buenas','Que siguem de tercera fila NO té perquè ser negatiu!','Los ultimos de las filas']; const ok=names.every(n=>s.includes(n)) && /textarea\.value\s*=/.test(s); process.exit(ok?0:1)"</automated>
  </verify>
  <done>
`textarea.value` conté els 4 noms exactes separats per '\n' en l'ordre indicat; el placeholder es manté; el handler de Registrar no s'ha modificat. En obrir l'Admin, el textarea mostra els 4 noms editables abans de registrar.
  </done>
</task>

</tasks>

<verification>
- `src/client/admin.js` conté els 4 noms exactes com a valor per defecte del textarea.
- Obrir l'Admin al navegador: el textarea #team-names-input mostra els 4 noms, un per línia, editables.
- Prémer Registrar sense editar → es registren els 4 equips amb aquests noms.
- Esborrar el contingut i el placeholder original torna a ser visible.
</verification>

<success_criteria>
El textarea de noms d'equips arrenca pre-omplert amb els 4 noms reals (editables), sense cap altre canvi de comportament al registre.
</success_criteria>

<output>
Create `.planning/quick/260706-hi0-omple-per-defecte-el-textarea-de-noms-d-/260706-hi0-SUMMARY.md` when done
</output>
